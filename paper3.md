# 基于大语言模型和检索增强生成的助教平台设计

## 摘要

随着高校数字化教学资源的持续积累，课程讲义、课件、实验指导、作业要求、课堂录屏、答疑记录等资料规模不断扩大，但其组织方式往往较为分散，学生在学习过程中仍需要耗费大量时间进行资料检索和知识定位。与此同时，教师在课程答疑中面临大量重复性问题，传统人工答疑方式难以持续满足高频、个性化和可追溯的学习支持需求。针对上述问题，本文设计并实现了一个基于大语言模型和检索增强生成的 AI 助教平台，围绕课程资料入库、知识图谱构建、智能问答、教师审核回流和个性化学习支持形成完整闭环。

系统采用前后端分离架构，前端基于 React、Vite、TypeScript 和 Tailwind CSS 构建学生端、教师端和管理员端交互界面，后端基于 FastAPI、SQLAlchemy、Celery 和 Redis 实现业务接口、异步任务调度和知识处理流程。数据存储层采用关系型数据库保存用户、课程、资料、会话、审核与学习行为等业务数据，采用 MinIO 或本地文件系统保存原始资料，采用向量数据库保存语义向量索引，并将 RAG-Anything/LightRAG 生成的图结构同步到图数据库或业务图谱表中用于检索和可视化。知识处理层以香港大学提出的 RAG-Anything 框架为核心，结合 MinerU、LibreOffice、OCR、视觉语言模型、嵌入模型和重排序模型，实现文档、图片、音频和视频等多模态课程资料的统一解析、向量化索引、双层图构建和混合检索。

在问答流程中，系统首先对用户问题进行意图识别、上下文整理和查询改写，然后通过 RAG-Anything 执行向量检索、图增强检索和跨模态证据召回，再经过重排序模型筛选高质量证据，最后由大语言模型基于证据生成带引用、带置信度的课程回答。对于包含图片或临时附件的用户提问，系统通过视觉语言模型和临时解析链路提取附件语义，并将其作为当前问答上下文参与检索与生成。系统同时引入教师审核与反馈优化机制，当回答置信度不足、学生反馈负面或证据覆盖不足时自动生成审核任务，教师修正后的高质量答案可回流到课程知识库并触发增量索引更新。学习分析模块基于问答记录、错题、闪卡复习、学习行为和知识图谱关系构建学生画像，并采用可解释的多因素规则推荐算法为学生推送资料和复习建议。

测试与分析结果表明，本文系统能够较完整地支撑课程资料上传、多模态解析、知识库构建、RAG 问答、引用溯源、教师审核、知识回流和个性化推荐等核心流程，为课程场景下的智能助教平台提供了一种可实现、可扩展且具有教学可信性的技术方案。

**关键词**：大语言模型；检索增强生成；RAG-Anything；LightRAG；多模态解析；知识图谱；AI 助教

## Abstract

With the continuous accumulation of digital teaching resources in higher education, course materials such as lecture notes, slides, experiment guides, assignment descriptions, classroom recordings and Q&A records are becoming increasingly large and heterogeneous. However, these resources are often scattered across different formats and platforms, making it difficult for students to efficiently locate relevant knowledge. Meanwhile, teachers face a large number of repetitive questions, and traditional manual tutoring cannot continuously satisfy high-frequency, personalized and traceable learning support needs. To address these problems, this thesis designs and implements an AI teaching assistant platform based on large language models and retrieval-augmented generation. The platform forms a complete closed loop covering course material ingestion, knowledge graph construction, intelligent question answering, teacher review, knowledge feedback and personalized learning support.

The system adopts a front-end/back-end separated architecture. The front end is built with React, Vite, TypeScript and Tailwind CSS, providing role-specific interfaces for students, teachers and administrators. The back end is implemented with FastAPI, SQLAlchemy, Celery and Redis to support business APIs, asynchronous task scheduling and knowledge processing. The storage layer uses a relational database for business data, MinIO or local file storage for original course materials, a vector database for semantic indexes, and a graph database or relational graph tables for synchronizing graph structures produced by RAG-Anything and LightRAG. The knowledge layer takes RAG-Anything proposed by HKU as the core framework, integrating MinerU, LibreOffice, OCR, vision-language models, embedding models and reranking models to process multimodal course materials such as documents, images, audio and video.

For question answering, the system first performs intent classification, context organization and query rewriting. It then uses RAG-Anything to conduct vector retrieval, graph-enhanced retrieval and cross-modal evidence retrieval. Candidate evidence is reranked by a reranking model, and the final answer is generated by a large language model based on selected evidence, with citations and confidence scores. For user-uploaded images or temporary attachments, the system extracts semantic information through a vision-language model and temporary parsing pipeline, then incorporates the extracted content into the current QA context. In addition, teacher review and feedback optimization mechanisms are introduced. Low-confidence answers, negative user feedback and insufficient evidence coverage can trigger review tasks, and teacher-corrected answers can be written back to the course knowledge base for incremental updating. The learning analytics module builds student profiles from question logs, error records, flashcard reviews, learning behaviors and graph relations, and provides explainable resource recommendations through a multi-factor rule-based recommendation algorithm.

The test and analysis results show that the proposed system can support core processes including course material uploading, multimodal parsing, knowledge base construction, RAG-based question answering, citation tracing, teacher review, knowledge feedback and personalized recommendation. It provides a feasible, extensible and trustworthy technical solution for AI teaching assistant platforms in course scenarios.

**Keywords**: Large Language Model; Retrieval-Augmented Generation; RAG-Anything; LightRAG; Multimodal Parsing; Knowledge Graph; AI Teaching Assistant

## 第一章 绪论

### 1.1 研究背景与问题提出

随着高校信息化教学不断深入，课程资料的数字化积累速度显著加快。围绕一门课程，教师通常会持续产生和维护讲义、课件、实验指导书、课堂通知、作业要求、课程录屏、答疑材料和补充阅读资料等多种资源。这些资源为学生自主学习提供了重要支撑，但在实际使用中也暴露出来源分散、格式异构、组织粒度不统一和检索效率较低等问题。学生在复习知识点或完成作业时，往往需要在多份资料之间反复查找、比对和理解，导致课程资源数量的增长并没有自然转化为知识服务效率的提升。

与此同时，教师在教学过程中需要面对大量重复性答疑问题，例如概念辨析、公式适用条件、实验步骤、作业要求和课程安排等。对于课程规模较大的班级，教师很难及时响应每一名学生的个性化问题，优质答复也难以在不同学生之间沉淀和复用。传统答疑模式在响应效率、知识追溯、个性化支持和持续优化方面均存在一定局限。

近年来，大语言模型在自然语言理解、内容生成、对话交互和知识总结方面表现出较强能力，为智能教学辅助系统提供了新的技术基础。大语言模型能够以自然语言方式与学生交互，生成解释性回答、学习建议和内容摘要。然而，如果直接将通用大语言模型用于课程问答，其回答往往依赖模型参数中的通用知识，难以保证严格贴合课程资料；同时，模型回答缺少可追溯来源，容易出现事实偏移或幻觉问题。因此，课程场景下的大语言模型应用不能仅依靠模型自身生成能力，而应结合课程资料检索、知识图谱组织和教师审核机制，使生成结果受到课程知识边界约束。

检索增强生成技术为上述问题提供了有效路径。RAG 通过在生成前检索外部知识，将课程资料、结构化知识和历史审核结果作为回答依据，从而提升答案的相关性、可信性和可解释性。进一步地，课程资料并不只包含纯文本，课件中的图片、表格、公式、扫描页、课堂录音和录屏视频同样承载重要知识。香港大学提出的 RAG-Anything 在 LightRAG 基础上扩展了复杂文档解析、多模态内容组织和跨模态检索能力，能够将文本、图片、表格和公式等内容纳入统一知识表示，为构建面向课程资料的多模态 RAG 助教平台提供了重要参考。

基于上述背景，本文围绕“基于大语言模型和检索增强生成的助教平台设计”展开研究，目标是在课程场景下实现资料上传、知识库构建、知识图谱组织、智能问答、教师审核回流和个性化推荐的一体化平台。

### 1.2 国内外研究现状

#### 1.2.1 大语言模型在教育场景中的应用研究

大语言模型的发展推动了智能教育应用从规则驱动逐步转向生成式交互。当前研究主要集中在智能问答、内容生成、作业辅导、学习陪伴、教学设计辅助和个性化反馈等方向。大语言模型能够根据学生输入生成自然、连贯且形式灵活的回答，因此适合承担课程答疑、概念解释、学习建议和材料总结等任务。

但是，教育场景对回答准确性、课程一致性和证据可核验性提出了更高要求。通用模型虽然具备丰富语言能力，却不一定掌握具体课程资料中的教师要求、授课顺序和作业约束。如果缺少课程知识边界，大语言模型容易输出“看似合理但缺少依据”的答案。因此，当前研究逐渐从“模型能否回答”转向“模型能否基于课程证据可信回答”。

#### 1.2.2 基于 RAG 的课程问答与知识服务研究

RAG 技术通过外部知识检索增强模型生成过程，已成为专业问答和知识服务系统的重要技术路线。在课程场景中，RAG 能够将教材、课件、实验指导和教师答疑等资料作为知识来源，使模型回答不再完全依赖参数记忆。与传统关键词检索相比，RAG 能够结合语义检索和自然语言生成，为学生提供更自然的问答体验。

现有课程 RAG 系统多采用文档切分、向量嵌入和相似度检索方式构建知识库，但仍存在三个不足：一是大量系统只处理文本片段，难以组织课程概念之间的结构关系；二是对于表格、公式、图片和扫描文档等复杂资料支持不足；三是缺少教师审核、学生反馈和知识回流机制，导致系统难以根据真实教学反馈持续优化。

#### 1.2.3 多模态教学资源解析与知识组织研究

现代课程资源具有明显多模态特征。讲义和课件中常包含图表、公式、流程图和截图；录音和录屏则包含语音、画面、字幕和时间片段信息。多模态资料处理通常涉及 OCR、版面分析、公式识别、表格抽取、语音识别、关键帧抽取和视觉语言理解等技术。

多模态解析的关键并不只是把非文本内容转写为文本，而是要将不同模态的内容组织为可索引、可检索、可引用的知识对象。RAG-Anything 的意义正在于此：它将复杂文档解析结果转化为多模态内容单元，并通过双图结构同时组织文本语义关系与跨模态关联，使图片、表格和公式等内容能够进入统一检索与生成链路。

#### 1.2.4 学习分析与个性化支持研究

学习分析关注从学生行为数据中识别学习状态，为教学决策和个性化支持提供依据。AI 助教平台中的提问记录、反馈记录、错题、闪卡复习、资料浏览和课程活动都可以成为学生画像构建的数据来源。基于这些数据，系统可以识别学生的薄弱知识点、学习偏好和复习需求，并进一步提供资源推荐和学习建议。

在原型系统阶段，个性化推荐不一定必须依赖复杂训练模型。可解释的规则推荐方法更适合课程场景落地，例如结合知识图谱中的前置关系、学生近期提问主题、错题记录、教师重点标注和资源质量评分生成推荐结果。该方法虽然不如深度推荐模型复杂，但具有可解释、易调试、便于教师理解和干预等优点。

#### 1.2.5 智能对话系统与 AI 助教研究

智能对话系统为 AI 助教提供了自然交互入口。传统对话系统多依赖规则和模板，灵活性不足；结合大语言模型后，对话系统在语义理解、多轮交互和自然表达方面显著增强。当前 AI 助教研究通常围绕课程答疑、学习提醒、作业辅导、资源推荐和教师辅助管理等场景展开。

不过，真正面向课程落地的 AI 助教系统仍需解决可信性问题。一个实用的 AI 助教平台不应只是聊天机器人，而应具备课程资料管理、知识库构建、检索增强问答、引用溯源、人工审核、知识更新和学习分析等完整能力。

### 1.3 研究目标与主要内容

本文面向高校课程教学辅助场景，设计并实现一个基于大语言模型和检索增强生成的 AI 助教平台。研究目标主要包括以下几个方面。

第一，构建统一课程知识库。系统支持教师上传文档、图片、音频和视频等多类型资料，并通过多模态解析、语义嵌入和图构建将资料转化为可检索知识。第二，构建基于 RAG-Anything 的检索增强问答链路，使回答能够基于课程资料证据生成，并返回引用来源和置信度。第三，构建课程知识图谱，利用 RAG-Anything/LightRAG 的双图机制组织文本语义关系与跨模态关系，并将图结构用于检索、推荐和可视化。第四，设计教师审核与反馈优化机制，使低质量回答能够经过人工修正后回流知识库。第五，结合学习行为和知识图谱关系实现个性化推荐和学习支持。第六，通过系统测试、典型案例和用户评估验证平台可行性与实用性。

### 1.4 研究方法与技术路线

本文综合采用文献研究法、系统分析法、原型实现法和实验评估法。文献研究法用于分析大语言模型、RAG、RAG-Anything、多模态解析和学习分析等关键技术；系统分析法用于明确学生、教师和管理员角色需求；原型实现法用于完成前后端系统、知识处理链路和问答闭环；实验评估法用于验证系统功能、检索质量、生成质量和用户可用性。

技术路线围绕“课程资料输入 - 多模态解析 - 知识索引构建 - RAG 检索问答 - 教师审核回流 - 个性化推荐 - 测试评估”展开。资料上传后，系统先按类型分流处理：文档和图片进入 RAG-Anything 支持的官方解析链路；音频和视频先经过 ASR、关键帧抽取、OCR 和 VLM 描述等预处理，再转化为 RAG-Anything 可接收的 Markdown 或多模态内容列表。解析结果进一步进入嵌入模型和图构建流程，形成向量索引、文本语义图和跨模态关系图。用户提问时，系统结合多轮上下文进行查询改写，通过混合检索召回证据，经过 reranker 重排序后由 LLM 生成回答。回答结果、用户反馈和教师修正共同进入知识更新闭环。

### 1.5 论文组织结构

本文共分为六章。第一章介绍研究背景、研究现状、研究目标和技术路线。第二章介绍大语言模型、RAG、LightRAG、RAG-Anything、多模态解析、知识图谱和学习分析等理论与技术。第三章从用户角色、功能需求、非功能需求、总体架构、核心业务流程和数据库设计角度对系统进行总体设计。第四章详细说明课程资源管理、RAG-Anything 多模态知识库构建、知识图谱、检索增强问答、教师审核回流、个性化推荐和前后端交互等关键模块实现。第五章按照真实使用顺序展示系统前端页面与关键交互流程，包括用户注册、登录、加入课程、课程学习、教师管理和管理员配置等功能页面。第六章总结全文工作并展望后续优化方向。

## 第二章 相关理论与关键技术

### 2.1 大语言模型与提示式问答技术

大语言模型是一类基于大规模语料训练的生成式模型，能够完成文本理解、问答、总结、推理和代码生成等任务。在本文系统中，大语言模型不是独立知识来源，而是证据驱动生成器。也就是说，模型回答应尽量基于检索到的课程资料、图谱节点和教师审核内容生成，而不是自由发挥。

提示工程在系统中主要用于四类任务：第一，问题分类和查询改写，将用户自然语言问题转化为更适合检索的独立问题；第二，知识抽取和实体关系规范化，从课程资料中提取概念、公式、章节、图表和关系；第三，答案生成，约束模型必须基于证据回答并给出引用；第四，教师审核辅助，帮助教师总结问题原因和修正建议。

### 2.2 检索增强生成技术

RAG 的核心思想是将外部知识检索与大语言模型生成结合。其典型流程包括文档预处理、分块、向量嵌入、索引构建、查询嵌入、候选召回、证据组织和答案生成。对于课程场景而言，RAG 的价值在于将模型回答限制在课程资料边界内，并为回答提供可追溯证据。

本文系统在传统 RAG 基础上进一步引入图增强检索和多模态检索。向量检索适合发现语义相近片段，关键词检索适合精确术语匹配，图检索适合沿课程概念关系扩展证据，多模态检索则用于处理图片、表格、公式和视频片段等内容。通过多路召回和重排序，系统能够提升证据覆盖率与回答可信度。

### 2.3 LightRAG 与 RAG-Anything

LightRAG 是一种面向高效检索增强生成的图增强 RAG 框架，其核心思想是将文本片段、实体和关系组织为图结构，并结合向量检索与图检索提升检索质量。与单纯依赖文本块相似度的 RAG 相比，LightRAG 更强调知识之间的显式关联，能够在问题涉及多个概念、跨文档关系或上下位结构时提供更完整的上下文。

RAG-Anything 在 LightRAG 基础上面向多模态文档场景进行扩展。根据其官方论文与代码说明，RAG-Anything 的主要流程包括文档解析、内容分析、知识图谱构建和智能检索。其支持 PDF、Office 文档、Markdown、文本和图片等资料形式，并可通过 MinerU、LibreOffice、OCR 和视觉语言模型处理复杂版面、图片、表格和公式。RAG-Anything 在接口设计上区分 `llm_model_func`、`vision_model_func` 和 `embedding_func`，说明生成模型、视觉模型和嵌入模型可以分别配置，不要求全部由同一模型承担。

从系统实现关系上看，RAG-Anything 并不是完全独立于 LightRAG 的另一套底层 RAG 引擎，而是建立在 LightRAG 之上的多模态扩展框架。前者负责复杂文档解析、多模态内容组织、跨模态内容项生成以及多模态查询入口封装，后者负责底层文本块存储、实体关系图存储、向量索引管理与图增强检索执行。换言之，RAG-Anything 在架构上承担“上层多模态工作流层”的角色，LightRAG 承担“底层存储与检索执行层”的角色。因此，当系统需要配置向量数据库、图数据库、存储后端或图检索相关参数时，应遵循 LightRAG 的接口与配置规范；当系统需要配置文档解析、内容项生成、图片理解、表格处理和多模态输入链路时，则主要遵循 RAG-Anything 的工作流配置方式。

RAG-Anything 的“双图”机制可理解为同时维护两类关系结构。第一类是文本语义图，主要承载由文本内容抽取出的实体、概念和关系，继承 LightRAG 的图增强检索思想。第二类是跨模态关系图，主要连接文本、图片、表格、公式和其他内容项之间的对应关系，例如某段文字解释某张图，某个公式出现在某页课件，某个表格对应某个概念。双图机制使系统不仅能够检索文本片段，还能够沿着文本语义关系和跨模态关系组织证据。

### 2.4 多模态资料解析技术

多模态资料解析是本文系统进入 RAG-Anything 链路的前置基础。对于 PDF、DOC、DOCX、PPT、PPTX、MD 和 TXT 等文档，系统优先采用 RAG-Anything 官方支持的解析链路。Office 文档可通过 LibreOffice 转换为更易解析的格式，PDF 和扫描类文档可通过 MinerU 执行版面分析、OCR、表格识别、公式识别和 Markdown/JSON 输出。

对于图片资料，系统通过 OCR 提取文字，并通过 VLM 生成图片语义描述。对于音频资料，系统先使用 ffmpeg 完成转码、降噪和采样率统一，再通过 Whisper、faster-whisper 或 SenseVoice 等 ASR 模型生成带时间戳的文本转写，随后按语义片段切分并转化为 Markdown 或内容列表输入 RAG-Anything。对于视频资料，系统先抽取音频并执行 ASR，再进行场景切分和关键帧抽取，对关键帧执行 OCR 与 VLM 描述，最后将字幕片段、关键帧描述、时间戳和截图路径组合为多模态内容项。

### 2.5 向量数据库、图数据库与对象存储

本文系统采用分层存储思路。关系型数据库用于保存业务实体，包括用户、课程、班级、资料、解析任务、聊天记录、审核项、学习行为和推荐记录。对象存储用于保存原始文件和解析中间产物，在本地开发阶段可使用文件系统，在部署阶段可使用 MinIO。向量数据库用于保存文档片段、实体描述和多模态文本化表示的嵌入向量，可选 Qdrant、Milvus 或 PGVector。图数据库或图谱同步表用于保存 RAG-Anything/LightRAG 生成的实体、关系和跨模态关联，可选 Neo4j，也可以同步到系统自身的 `KnowledgeEntity` 与 `KnowledgeRelation` 类业务表用于可视化。

这种分层设计可以避免将所有内容混杂在关系数据库中，也使系统在后续扩展时能够独立替换向量检索、图检索和文件存储组件。

### 2.6 重排序模型与置信度评估

RAG 检索通常会返回多个候选片段，但初始召回结果并不一定都适合作为最终证据。重排序模型的作用是在候选召回之后，对问题与候选证据之间的相关性进行更精细判断。本文系统可选用 `BAAI/bge-reranker-v2-m3`、`bge-reranker-large` 或 API reranker，将 RAG-Anything 返回的候选证据重新排序，并选取 Top-K 证据进入答案生成。

置信度不是模型自我声明，而是由多项信号综合计算得到，包括向量相似度、图关系命中情况、reranker 分数、证据数量、证据来源可靠性、跨模态一致性和历史教师审核结果。置信度用于决定是否直接回答、是否提示不确定以及是否触发教师审核。

### 2.7 学习分析与个性化推荐技术

学习分析模块通过记录学生提问、反馈、错题、闪卡复习和资料浏览等行为，构建学生画像。推荐模块采用可解释的多因素规则算法，不依赖大规模训练数据即可运行。推荐分数综合考虑薄弱知识点匹配度、图谱前置关系、近期提问相似度、资源难度、教师重点标注、资源质量和多样性约束。该方法适合毕业设计原型系统，因为其实现复杂度适中、可解释性强，并且教师能够理解和调整推荐结果。

### 2.8 本章小结

本章介绍了本文系统涉及的大语言模型、RAG、LightRAG、RAG-Anything、多模态解析、向量数据库、图数据库、重排序、置信度评估和学习分析等关键技术。上述技术共同支撑了后续章节中系统需求分析、总体架构设计和关键模块实现。

## 第三章 系统需求分析与总体设计

### 3.1 用户角色与应用场景分析

#### 3.1.1 学生用户场景

学生是系统的主要服务对象。学生在课程学习过程中需要快速查询知识点、理解概念、定位资料、查看引用来源、提交追问、上传临时附件并获得个性化复习建议。系统需要为学生提供课程上下文中的 AI 助教入口，使学生无需在大量资料中手动查找即可获得基于课程证据的回答。

学生端典型场景包括：进入课程页面查看资料和公告；围绕课程内容向 AI 助教提问；上传图片、文档或截图作为问题附件；查看回答引用来源和置信度提示；对回答进行有用或无用反馈；将关键问答加入错题本或闪卡；接收系统推荐的复习资料和学习建议。

#### 3.1.2 教师用户场景

教师是课程知识的维护者和回答质量的监督者。教师需要上传课程资料，查看资料解析和索引状态，管理课程知识库，审核低置信回答，修正错误答案，将高质量答复回流到知识库，并观察学生学习情况。教师端不仅提供资料管理功能，还承担课程知识持续更新和教学质量把关职责。

教师端典型场景包括：创建课程和班级；上传讲义、课件、实验指导、录音和视频；查看每份资料的解析状态、chunk 数量、图节点数量和失败原因；查看课程知识图谱；处理学生反馈和低置信问答；发布修正答案；查看高频问题、薄弱知识点和推荐效果。

#### 3.1.3 管理员用户场景

管理员负责系统级配置和运行监控。由于本系统涉及 LLM、VLM、embedding、reranker、RAG-Anything、向量库、图数据库、对象存储和异步任务队列等多个组件，管理员需要能够查看组件连通性、模型路由配置、索引任务状态、系统性能指标和实验评测结果。

管理员端典型场景包括：管理用户与课程；配置 API 模型或本地模型；查看向量库、图数据库、MinIO 和 Redis 状态；查看 RAG 查询成功率、回退率、平均延迟和低置信率；导出系统评测报告。

### 3.2 功能需求分析

#### 3.2.1 课程与班级管理需求

系统应支持教师创建课程和班级，学生加入班级，管理员维护课程基础信息。课程和班级是知识库隔离、问答检索和学习分析的基本边界。每份资料、每次问答、每条审核记录和每个推荐结果都应绑定到明确课程或班级上下文，避免跨课程知识混淆。

#### 3.2.2 课程资料上传与知识库构建需求

系统应支持教师上传 PDF、DOC、DOCX、PPT、PPTX、MD、TXT、PNG、JPG、JPEG、WEBP、MP3、WAV、M4A、MP4、MOV、AVI 等课程资料。上传后系统需要完成文件校验、哈希去重、对象存储、元数据落库、解析任务创建和异步索引调度。

知识库构建需求包括：根据文件类型选择处理链路；将文档和图片输入 RAG-Anything 官方解析流程；将音频和视频预处理为 RAG-Anything 可接收的文本或多模态内容列表；执行语义嵌入和图构建；更新索引状态；保存来源、页码、时间戳、模态类型和质量信息。

#### 3.2.3 多模态处理需求

多模态处理是系统核心需求之一。文档类资料应保留标题、段落、表格、公式、图片区域和版面结构；图片资料应提取 OCR 文本和视觉语义描述；音频资料应生成带时间戳的 ASR 文本；视频资料应同时生成字幕、关键帧描述和画面 OCR 结果。所有模态最终都应转化为统一内容单元，并参与 RAG-Anything 的嵌入、图构建和检索流程。

#### 3.2.4 AI 助教问答需求

系统应支持学生和教师在课程上下文中进行多轮问答。问答结果应包括答案正文、引用来源、证据片段、置信度、推荐追问和是否需要教师审核标识。系统需要支持知识点查询、概念解释、资料定位、公式说明、图表解释、作业辅助理解和学习建议等问题类型。

对于用户提问时上传的临时附件，系统应执行临时解析，并将解析结果作为当前问题上下文参与检索和生成。临时附件默认不写入课程知识库，只有经过教师确认或用户选择后才进入正式知识库。

#### 3.2.5 教师审核与知识回流需求

系统应在回答置信度低、证据不足、用户负反馈或敏感场景下自动生成审核任务。教师可以查看原始问题、AI 回答、证据来源、置信度计算原因和学生反馈，并提交修正答案。修正内容可以作为 FAQ、补充知识片段或标准解释回流到课程知识库，并触发增量嵌入和图更新。

#### 3.2.6 个性化推荐与学习分析需求

系统应记录学生提问、反馈、错题、闪卡复习、资料浏览和推荐点击等行为，构建学生画像。推荐系统应结合学生薄弱知识点、知识图谱前置关系、近期提问主题、教师重点资源、资源难度和资源质量，为学生推送资料、复习卡片和学习建议，并提供可解释推荐理由。

### 3.3 非功能需求分析

系统应满足可用性、可维护性、可扩展性、安全性、稳定性和可观测性要求。可用性要求学生、教师和管理员能够以较低学习成本完成主要操作；可维护性要求后端模块、模型适配器、存储接口和预处理管道边界清晰；可扩展性要求支持 API 模型和本地模型切换，支持向量库、图数据库和对象存储替换；安全性要求课程资料、学习记录和模型密钥受到权限保护；稳定性要求资料解析失败可重试，耗时任务可异步处理；可观测性要求记录解析耗时、索引状态、检索质量、回答置信度和审核触发原因。

### 3.4 系统总体架构设计

#### 3.4.1 总体架构

系统总体采用“前端交互层、后端业务层、AI 知识处理层、数据与存储层、评测与运维层”的分层架构。前端交互层面向学生、教师和管理员提供角色化页面；后端业务层负责认证、课程、资料、聊天、审核、学习分析和推荐；AI 知识处理层负责多模态解析、RAG-Anything 入库、向量检索、图检索、reranker、答案生成和模型路由；数据与存储层负责关系数据、对象文件、向量索引和图谱存储；评测与运维层负责系统指标、离线评测、任务监控和模型配置。

#### 3.4.2 前端架构

前端采用 React、Vite、TypeScript、Tailwind CSS 和 React Router 构建。学生端重点实现课程学习、AI 问答、资料查看、错题闪卡和推荐展示；教师端重点实现资料上传、知识库状态、图谱可视化、审核队列和学情分析；管理员端重点实现模型配置、系统监控、索引队列和评测报告。前端通过统一 API 客户端与后端交互，所有核心数据均来自后端真实接口，示例数据仅作为演示 fallback。

#### 3.4.3 后端架构

后端采用 FastAPI 构建 REST API，以模块化单体方式组织业务。核心模块包括认证用户模块、课程班级模块、知识库模块、聊天问答模块、审核回流模块、学习分析模块、推荐模块、管理配置模块和实验评测模块。异步任务由 Celery 和 Redis 承担，主要用于文件解析、RAG-Anything 入库、音视频预处理、图谱同步和批量评测。

#### 3.4.4 数据与存储层设计

关系型数据库用于保存业务主数据，开发环境可使用 SQLite，部署环境建议使用 PostgreSQL。对象存储用于保存原始资料和解析中间文件，开发环境可使用本地目录，部署环境建议使用 MinIO。向量数据库用于保存 chunk、实体描述和多模态文本化内容的 embedding，建议使用 Qdrant、Milvus 或 PGVector。图数据库用于保存知识图谱和跨模态关系，建议使用 Neo4j；如果部署成本有限，也可将 LightRAG/RAG-Anything 图结构同步到关系表用于展示和检索辅助。

#### 3.4.5 AI 与知识处理层设计

AI 与知识处理层以 RAG-Anything 为主链路。系统不再把简化 parser 的摘要、chunk 和关键词作为正式知识库来源，而是优先使用 RAG-Anything 解析、内容分析和图构建结果。生成模型、视觉模型、嵌入模型和重排序模型均通过模型路由配置，可选择 API 调用或本地部署。推荐配置如下：生成模型可使用 Qwen、DeepSeek、GLM 等 API 或本地部署模型；视觉语言模型可使用 Qwen2.5-VL、InternVL、MiniCPM-V 或多模态 API；嵌入模型可使用 bge-m3、bge-large-zh-v1.5 或 m3e-base；重排序模型可使用 bge-reranker-v2-m3 或 API reranker。

需要指出的是，本文系统虽然在业务层统一以 RAG-Anything 作为唯一正式 RAG 主链路，但其底层仍通过 `lightrag_kwargs` 等方式调用 LightRAG 的存储与检索能力。也就是说，系统在工程实现上采用“业务后端 - RAG-Anything - LightRAG”的分层模式：业务后端负责课程、资料、会话、审核、推荐和统计等应用逻辑；RAG-Anything 负责多模态资料解析、内容单元组织和查询流程封装；LightRAG 负责 chunk、实体、关系、向量索引与图结构的底层持久化和检索执行。这种分层设计使系统既能保持 RAG-Anything 在多模态处理方面的完整性，又能复用 LightRAG 在图增强检索和存储适配方面的成熟能力。

### 3.5 核心业务流程设计

#### 3.5.1 课程资料上传入库流程

课程资料上传入库流程如下。第一，教师在课程页面上传文件，系统完成权限校验和课程边界确认。第二，后端接收文件并计算哈希值，若发现重复文件则复用已有索引结果。第三，系统将原始文件保存到本地文件系统或 MinIO，并在关系数据库中记录资料元数据。第四，系统创建解析任务并进入异步队列。第五，预处理模块根据文件类型分流：文档和图片进入 RAG-Anything 官方处理流程，音频和视频先执行预处理。第六，RAG-Anything 完成文档解析、内容分析、embedding、双图构建和索引存储。第七，系统同步摘要、内容单元、图谱节点、图谱关系和状态信息到业务数据库。第八，教师端可查看解析结果、图谱展示和索引状态。

#### 3.5.2 基于 RAG-Anything 的问答流程

问答流程如下。第一，用户在课程上下文中输入问题，系统读取会话历史和课程信息。第二，系统进行问题分类和查询改写，生成独立查询、关键词、意图和期望答案类型。第三，若用户上传附件，则先对附件执行临时解析，将图片描述、文档摘要或音视频转写加入查询上下文。第四，系统调用 RAG-Anything 执行混合检索，召回文本 chunk、图谱实体、关系路径、图片说明、表格内容、公式内容和音视频片段。第五，reranker 对候选证据重新排序。第六，系统根据证据分数、覆盖率和来源可靠性计算置信度。第七，LLM 根据证据生成回答，并输出引用、置信度和推荐追问。第八，回答和证据写入聊天记录，低置信回答进入审核队列。

#### 3.5.3 教师审核与知识回流流程

教师审核流程如下。系统根据低置信度、负反馈、证据不足和敏感意图自动创建审核项。教师查看审核项后，可以确认回答、修改回答、驳回回答或补充标准解释。若教师选择回流知识库，系统将修正内容转化为 FAQ 或补充资料片段，写入课程知识空间，并触发增量 embedding 和图谱更新。更新完成后，后续相似问题将优先检索到教师修正内容。

#### 3.5.4 个性化推荐流程

推荐流程如下。系统持续记录学生提问主题、反馈、错题、闪卡复习和资料访问行为。学习分析模块计算学生在各知识点上的掌握度和活跃度，并结合知识图谱识别前置概念和关联概念。推荐模块对候选资料、FAQ、闪卡和复习建议进行打分，输出推荐结果及推荐理由。学生反馈推荐是否有用后，系统调整资源质量分和个体偏好权重。

### 3.6 数据库与接口设计

核心数据实体包括用户、课程、班级、班级成员、课程资料、解析任务、知识空间、聊天会话、聊天消息、引用证据、审核项、教师修正、知识实体、知识关系、学习行为、学生画像、错题、闪卡和推荐记录。关系数据库保存这些业务实体，向量库和图数据库保存知识索引，业务库中保留向量库文档 ID、图节点 ID 和来源元数据，用于跨存储关联。

核心接口包括认证接口、课程接口、资料上传接口、解析状态接口、图谱查询接口、聊天问答接口、附件解析接口、反馈接口、教师审核接口、推荐接口和管理员配置接口。接口设计应遵循课程边界原则，即所有涉及资料、问答、图谱和推荐的数据访问均需要校验用户是否拥有对应课程或班级权限。

### 3.7 本章小结

本章从用户角色、功能需求、非功能需求、总体架构、核心流程和数据接口角度完成了系统总体设计。与普通问答系统相比，本文系统的关键特点在于以课程知识库为边界，以 RAG-Anything 为主知识引擎，以双图结构组织多模态资料，以教师审核和个性化推荐形成持续优化闭环。

## 第四章 AI 助教平台关键模块设计与实现

### 4.1 课程资源管理与知识库构建模块

#### 4.1.1 模块功能概述

课程资源管理模块是整个 RAG 系统的入口。其核心职责并不是简单保存文件，而是将课程资料转化为可被检索、可被引用、可被更新的知识资产。模块需要完成文件上传、对象存储、元数据落库、解析任务创建、异步调度、RAG-Anything 入库、索引状态追踪、失败重试和解析结果展示。

#### 4.1.2 文件上传与存储

教师上传资料后，系统首先进行课程权限校验，确认上传者具有对应课程或班级的管理权限。随后系统读取文件基本信息，计算 SHA-256 哈希值用于重复文件识别。如果同一课程中已存在完全相同文件，系统可直接复用已有解析结果，避免重复索引。

文件存储采用“原始文件与解析产物分离”的方式。原始文件保存至本地存储或 MinIO，对象路径与文件元数据写入关系数据库。解析产物包括 MinerU 输出的 Markdown、JSON、图片切片、表格文件、公式识别结果、ASR 文本、视频关键帧和 VLM 描述等，这些中间产物同样保存至对象存储或 RAG 工作目录，以便后续追溯和重新构建。

#### 4.1.3 解析任务与状态管理

由于多模态解析和索引构建耗时较长，系统采用异步任务机制。上传完成后，后端创建解析任务，状态从 `pending` 进入 `processing`，处理成功后变为 `completed`，失败后变为 `failed` 并记录失败阶段和错误原因。对于可恢复错误，系统支持重试；对于依赖缺失或文件格式异常，系统将错误信息反馈给教师和管理员。

解析任务记录中应保存文件 ID、课程 ID、任务类型、处理引擎、当前阶段、尝试次数、开始时间、结束时间、chunk 数量、图节点数量、图边数量、embedding 数量和质量提示。这些信息用于教师端展示、管理员监控和第五章实验分析。

#### 4.1.4 RAG-Anything 入库主链路

完善后的系统以 RAG-Anything 作为课程知识库的正式入库主链路，而不再以本地简化解析器生成的关键词、粗粒度分块和共现关系作为主要知识来源。具体而言，教师上传资料后，系统首先根据扩展名、MIME 类型和课程上下文完成文件类型识别、权限校验、哈希去重和对象存储写入，并在关系数据库中记录资料元数据和解析任务。随后，文档类和图片类资料直接进入 RAG-Anything 官方支持的处理流程，其中 DOC、DOCX、PPT 和 PPTX 等 Office 文档先通过 LibreOffice 转换为统一的中间格式，PDF、扫描件和图文混排资料则借助 MinerU 完成版面分析、OCR、表格识别、公式识别与阅读顺序恢复，从而最大限度保留原始教学资料中的结构化语义。经过解析后，RAG-Anything 会将文档内容组织为文本段落、图像区域、表格、公式等多模态内容单元，并对这些内容单元执行摘要生成、实体候选识别和语义分块，使原始资料由“文件”转化为可计算的知识对象。接下来，系统调用嵌入模型对文本内容、图像描述、表格说明和公式语义表示进行向量化，将结果写入向量数据库；同时由 LightRAG/RAG-Anything 基于内容单元之间的语义关联和跨模态对应关系构建文本语义图与跨模态关系图，并将图结构、文档状态和索引信息写入其工作目录和图存储。最后，系统再将业务展示和教学分析所需的摘要、来源信息、节点、关系、页码、时间戳和处理质量指标同步回关系数据库与可视化层，供教师端图谱浏览、问答引用追溯和后续实验评估使用。通过这一链路，课程知识库的构建真正建立在 RAG-Anything 的复杂文档解析、多模态组织、向量检索和图增强检索能力之上，从而保证论文中的核心 RAG 链路与实际系统设计保持一致。

#### 4.1.5 content item 统一表示

RAG-Anything 的多模态能力依赖统一的内容项表示。本文系统将解析后的内容统一抽象为 content item，每个内容项至少包含类型、正文或描述、来源文件、页码、坐标区域、时间戳、图片路径、表格结构、公式 LaTeX、质量分和元数据。常见类型包括 text、image、table、equation、audio_segment 和 video_segment。

统一内容项的意义在于，后续无论是向量嵌入、图谱构建、证据引用还是前端展示，都可以基于同一数据结构工作。例如，PDF 中的一张流程图会生成 image content item，包含 OCR 文本、VLM 描述、页码和图片路径；视频中的一个片段会生成 video_segment content item，包含时间范围、ASR 文本、关键帧描述和截图路径。

### 4.2 多模态资料处理与输入 RAG-Anything

#### 4.2.1 文档资料处理流程

对于 PDF、DOC、DOCX、PPT、PPTX、MD 和 TXT 等文档，系统优先采用 RAG-Anything 官方支持的流程。TXT 和 MD 可直接进行文本结构化处理；DOC、DOCX、PPT 和 PPTX 通过 LibreOffice 转换；PDF 和扫描文档通过 MinerU 解析。MinerU 的作用不仅是 OCR，还包括版面检测、阅读顺序恢复、标题层级识别、表格抽取、公式识别和图片区域导出。

解析完成后，文档内容被拆分为多模态内容项。文本段落用于生成语义 chunk；表格保留为结构化表格并生成文字描述；公式保留 LaTeX 或识别文本；图片区域保存截图并生成 OCR 文本和 VLM 描述。随后，这些内容项统一输入 RAG-Anything 执行嵌入和图构建。

#### 4.2.2 图片资料处理流程

图片资料包括课件截图、扫描题目、实验图片、流程图和结构图。系统首先执行 OCR，提取图片中的文字信息；随后调用 VLM 对图片进行整体语义描述，例如识别流程图中的节点关系、图表中的趋势、实验图中的关键对象或题目截图中的条件。OCR 文本和 VLM 描述共同组成图片的文本化表示，并与图片路径、来源信息和视觉区域信息一起输入 RAG-Anything。

图片进入知识库后，不只是作为附件保存，而是作为可检索证据参与问答。当用户询问“课件中三次握手的图是什么意思”时，系统可以通过图像描述、OCR 文本和相关文本片段共同定位证据。

#### 4.2.3 音频资料预处理流程

RAG-Anything 原生更侧重文档和图片等内容，音频资料需要在输入前进行预处理。音频处理流程如下：首先使用 ffmpeg 将音频统一转码为 wav 或其他稳定格式，并进行采样率统一、音量归一化和必要的降噪处理；然后使用 ASR 模型进行语音识别，生成带时间戳的转写文本；接着对转写文本进行术语修正、标点恢复和语义分段；最后将每个音频片段组织为 content item，字段包括片段文本、开始时间、结束时间、说话人信息和来源音频路径。

经过上述处理后，音频不直接输入 RAG-Anything，而是以“带时间戳文本片段”的形式进入后续文档处理和索引流程。这样既能利用 RAG-Anything 的文本和图处理能力，又能在回答中引用音频时间段。

#### 4.2.4 视频资料预处理流程

视频资料比音频更复杂，需要同时处理语音和画面。系统首先通过 ffmpeg 抽取音频，并执行与音频相同的 ASR 流程；然后对视频进行场景切分或按固定间隔抽取关键帧；对关键帧执行 OCR 和 VLM 描述；最后将 ASR 文本、关键帧描述、截图路径和时间戳进行对齐，生成 video_segment content item。

例如，一段讲解 TCP 拥塞控制的视频会被拆分为多个片段，每个片段包含教师语音转写、课件截图文字、关键帧视觉描述和时间范围。进入 RAG-Anything 后，这些片段可以像文档内容一样参与嵌入和图构建。用户提问时，系统不仅能返回文字回答，还能引用“视频第 03:20 至 04:10 片段”作为证据。

### 4.3 RAG-Anything 双图机制与知识图谱构建

#### 4.3.1 双图机制理解

RAG-Anything 的核心优势在于将 LightRAG 的图增强检索扩展到多模态场景。本文将其双图机制理解为“文本语义图”和“跨模态关系图”的协同。文本语义图用于描述课程概念、术语、公式、章节和知识点之间的语义关系；跨模态关系图用于描述文本、图片、表格、公式、音视频片段之间的来源、解释、对应和引用关系。

文本语义图解决“知识点之间如何关联”的问题，跨模态关系图解决“不同模态内容如何对应同一知识”的问题。两者结合后，系统在检索时既能从问题语义找到相关文本，也能沿图关系找到相关图片、表格、公式和视频片段。

#### 4.3.2 课程知识图谱实体设计

课程知识图谱不再采用简单关键词共现图，而是以 RAG-Anything/LightRAG 生成的实体和关系为基础。系统同步到业务层的实体类型包括 course、chapter、section、concept、term、algorithm、formula、table、figure、example、exercise、audio_segment 和 video_segment。关系类型包括 part_of、prerequisite_of、defines、uses、compared_with、illustrated_by、appears_in、step_of、related_to 和 cites。

每个实体保存名称、类型、描述、来源资料、页码或时间戳、置信度和外部图节点 ID。每条关系保存头实体、尾实体、关系类型、证据文本、来源、权重和置信度。这样可以保证图谱既能用于系统检索，也能用于教师端可视化。

#### 4.3.3 知识抽取与 prompt 设计

知识抽取虽然以 RAG-Anything/LightRAG 的内部知识组织流程为基础，但为了使图谱结果更符合课程教学语义，系统还需要在“图谱同步层”增加面向课程场景的实体规范化与关系规范化过程。其设计思想并不是重新构建一套脱离 RAG-Anything 的独立图谱，而是在 RAG-Anything 已经完成复杂文档解析、多模态内容组织和初步图关联之后，再通过 LLM 对候选内容进行课程化映射，使实体类型、关系类型、表述粒度和来源证据更适合教学问答、教师审核和个性化推荐等下游任务。具体而言，系统首先从解析后的 content item 中筛选出具有知识价值的文本段落、表格说明、公式上下文、图片描述和音视频片段转写，然后结合课程名称、章节结构、教师术语习惯和资料类型构造抽取上下文。实体抽取阶段重点识别课程章节、核心概念、术语定义、算法、公式、实验步骤、图表对象、典型例题和习题任务等对象；关系抽取阶段则重点识别包含关系、先修关系、定义关系、使用关系、比较关系、图示说明关系、步骤关系和同义归并关系。为了避免模型输出过于发散，系统在 prompt 中同时给出允许的实体类型集合、允许的关系类型集合、来源证据要求和 JSON 输出约束，使抽取结果能够直接进入后续的实体去重、同名合并和图谱同步流程。

在实体抽取环节，本文系统采用如下课程化实体抽取模板：

```text
你是一名课程知识图谱构建助手。请根据给定课程资料片段，抽取适合教学问答的课程实体。

任务要求：
1. 仅抽取对课程教学有明确意义的实体，不要抽取无意义的常见词。
2. 实体类型只能从以下集合中选择：
   [course, chapter, section, concept, term, algorithm, formula, experiment_step, figure, table, example, exercise]
3. 每个实体必须给出：
   - name：实体名称
   - type：实体类型
   - description：用一句话概括实体含义
   - aliases：别名或同义表达，没有则返回空数组
   - evidence：直接支持该实体的原文片段
   - confidence：0 到 1 之间的小数
4. 如果文本只是泛泛描述，没有清晰知识对象，则返回空数组。
5. 输出必须为 JSON 数组，不要输出额外解释。

课程名称：{course_name}
章节信息：{chapter_context}
资料类型：{material_type}
内容片段：{content}
```

在关系抽取环节，系统在实体抽取结果基础上进一步采用如下模板：

```text
你是一名课程知识关系抽取助手。请基于给定课程片段和候选实体，识别实体之间的教学语义关系。

任务要求：
1. 关系类型只能从以下集合中选择：
   [part_of, prerequisite_of, defines, uses, compared_with, illustrated_by, step_of, related_to, appears_in, cites]
2. 每条关系必须给出：
   - head：头实体名称
   - tail：尾实体名称
   - relation：关系类型
   - reason：用一句话说明建立该关系的原因
   - evidence：支撑该关系的原文片段
   - confidence：0 到 1 之间的小数
3. 只有当证据明确支持关系时才输出，禁止臆测。
4. 若不存在明确关系，则返回空数组。
5. 输出必须为 JSON 数组，不要输出额外解释。

课程名称：{course_name}
章节信息：{chapter_context}
候选实体：{entities_json}
内容片段：{content}
```

在实际实现中，上述 prompt 主要服务于三类任务。第一类是对 RAG-Anything 输出结果做课程语义规范化，避免同一知识点在不同资料中以不同表述重复出现；第二类是补足跨文档、跨模态内容之间的教学关系，例如某张图用于说明某个算法、某个公式出现在某一章节中、某段实验视频属于某个实验步骤；第三类是为后续推荐和审核提供结构化标签，使系统不仅能检索“文本相似”，还能理解“前置知识”“相关图示”“同类例题”等更符合教学逻辑的关系。考虑到模型可能出现过度抽取、泛化抽取或证据不足的问题，系统在抽取后还需要执行实体名称归一化、同义词合并、基于来源和置信度的过滤，以及教师可选的人工修正，从而保证最终同步到业务图谱层的数据具有较高可解释性和可控性。

#### 4.3.4 图谱存储与可视化

RAG-Anything/LightRAG 的内部图存储用于检索，业务图谱库用于展示和分析。系统通过图谱同步任务将内部图节点和边同步到 Neo4j 或关系型图谱表。教师端图谱页面以课程为边界展示知识点网络，支持按章节、实体类型、资料来源和置信度筛选。点击节点后，页面展示实体解释、关联资料、相关问答、前置知识和推荐资源。

图谱可视化不是单纯展示效果，而是服务于教学管理。教师可以通过图谱发现资料覆盖不充分的知识点，查看学生高频提问集中在哪些节点，也可以标记重点知识点以影响推荐权重。

#### 4.3.5 动态更新机制

当教师上传新资料、删除旧资料或提交审核修正后，系统触发增量更新。增量更新包括四步：第一，识别新增或变更内容项；第二，只对变更内容重新生成 embedding；第三，将新增实体和关系合并到图谱中，对同名实体进行规范化和 provenance 合并；第四，更新知识库版本号和索引状态。每个图谱节点和边都保存来源信息，使系统能够在资料删除或版本回退时进行追溯。

### 4.4 检索增强问答模块

#### 4.4.1 问答总体流程

问答模块的完整链路为：用户输入问题；系统读取课程上下文、会话历史和用户角色；执行问题分类与查询改写；处理临时附件；调用 RAG-Anything 执行混合检索；获得候选证据；执行 reranker 重排序；组织证据上下文；计算置信度；调用 LLM 生成回答；输出引用和建议；记录消息、证据和指标；必要时触发教师审核。

该流程体现了本文系统与普通聊天机器人的区别。大语言模型只负责在证据约束下生成回答，而课程知识检索、证据筛选和质量控制由 RAG 链路承担。

#### 4.4.2 问题分类与上下文组装

系统首先判断用户问题类型，包括知识点解释、资料定位、概念比较、公式推导、图表解释、作业辅助、推荐请求和闲聊类问题。对于课程相关问题，系统会将多轮历史整理为独立问题，避免用户追问中出现“它”“这个公式”等指代不清的问题。

上下文组装包括课程 ID、班级 ID、用户角色、最近对话摘要、临时附件解析结果和查询改写结果。对于教师用户，系统可额外返回更多证据和诊断信息；对于学生用户，回答更偏向通俗解释和学习建议。

#### 4.4.3 RAG-Anything 混合检索

系统调用 RAG-Anything 的查询接口执行混合检索。检索过程同时利用向量相似度、关键词匹配、文本语义图和跨模态关系图。向量检索用于召回语义相关 chunk；图检索用于沿实体关系扩展相关知识点；跨模态检索用于召回图片、表格、公式和音视频片段；关键词检索用于保证专业术语和精确表达不被遗漏。

RAG-Anything 返回的候选证据包含内容、来源文件、页码、时间戳、模态类型、初始分数和图关系信息。系统将这些候选证据统一封装为 evidence object，供 reranker 和答案生成模块使用。

#### 4.4.4 重排序与证据组织

初始召回后，系统使用 reranker 对候选证据进行重排序。重排序模型输入用户问题和候选证据文本，输出相关性分数。对于图片、表格和视频片段，系统使用其 OCR 文本、VLM 描述或 ASR 文本作为 reranker 输入。最终得分可表示为：

$$
Score(e)=\alpha S_{vec}+\beta S_{graph}+\gamma S_{rerank}+\delta S_{source}
$$

其中 $S_{vec}$ 表示向量相似度，$S_{graph}$ 表示图关系命中分，$S_{rerank}$ 表示重排序分数，$S_{source}$ 表示来源可靠性，$\alpha,\beta,\gamma,\delta$ 为权重。系统选取 Top-K 证据进入答案生成，并保留多样性约束，避免所有证据来自同一片段。

#### 4.4.5 答案生成与引用溯源

答案生成 prompt 明确要求模型基于证据回答，不得编造不存在的课程内容；若证据不足，应说明无法确定并建议用户补充问题或等待教师确认。生成结果包含答案正文、引用列表、证据摘要、置信度、推荐追问和审核标识。

引用格式根据来源类型区分。文档引用包含文件名和页码；图片引用包含文件名和图片区域；音频引用包含时间戳；视频引用包含时间段和关键帧；教师修正引用标注为教师审核内容。引用机制提升了回答可信性，也便于教师复核。

#### 4.4.6 置信度计算与低置信处理

系统综合多项信号计算置信度：

$$
Conf=\lambda_1 R+\lambda_2 C+\lambda_3 G+\lambda_4 Q+\lambda_5 H
$$

其中 $R$ 表示 reranker 最高分和平均分，$C$ 表示证据覆盖率，$G$ 表示图关系支持度，$Q$ 表示资料质量分，$H$ 表示历史教师审核支持度。若置信度低于阈值，系统仍可给出保守回答，但会提示不确定并生成教师审核任务。对于完全没有证据的问题，系统应拒绝伪造答案，转而引导用户补充信息。

#### 4.4.7 多轮对话机制

多轮对话通过会话历史摘要和查询改写发挥作用。系统不会将全部历史无限制塞入 prompt，而是提取最近若干轮对话中的主题、实体和约束条件，将当前追问改写为独立问题。例如，用户先问“TCP 慢启动是什么”，再问“它什么时候结束”，系统会改写为“TCP 慢启动机制在什么条件下结束”。这样既保证检索准确，也控制上下文长度。

### 4.5 模型路由与部署方案

#### 4.5.1 LLM、VLM、Embedding 与 Reranker 分工

本文系统不要求全部使用 VLM。更合理的模型配置是 LLM、VLM、Embedding 和 Reranker 各司其职。LLM 负责问题理解、答案生成、摘要和知识抽取；VLM 负责图片、课件截图、视频关键帧和用户上传图片的视觉理解；Embedding 模型负责向量化文档片段、实体描述和查询；Reranker 负责候选证据精排。

如果使用能力较强的多模态大模型，也可以让同一模型同时承担文本生成和视觉理解，但从工程成本和性能角度看，分工式配置更灵活。

#### 4.5.2 API 与本地部署并存

系统支持 API 调用和本地部署两种方式。API 方式部署简单、效果稳定，适合原型演示和低维护场景；本地部署可降低长期调用成本并增强数据控制，适合对隐私和离线能力要求更高的场景。系统通过配置项选择模型后端，管理员可在后台查看当前使用的 LLM、VLM、embedding 和 reranker。

本地 LLM 可选 Qwen2.5、Qwen3、DeepSeek-R1-Distill、GLM-4 等；本地 VLM 可选 Qwen2.5-VL、InternVL、MiniCPM-V、LLaVA-OneVision 等；本地 embedding 可选 bge-m3、bge-large-zh-v1.5、m3e-base；本地 reranker 可选 bge-reranker-v2-m3 或 bge-reranker-large。

### 4.6 教师审核与知识回流模块

#### 4.6.1 审核触发机制

系统通过规则和指标共同触发审核。触发条件包括置信度低于阈值、候选证据不足、用户点踩、用户标记事实错误、回答涉及作业评分或课程规则、模型明确表示不确定、同一问题多次被反馈无用等。审核项记录原始问题、回答、证据、置信度、触发原因和用户反馈。

#### 4.6.2 教师修正与回流

教师处理审核项时，可以修改答案、补充证据、选择知识点标签，并决定是否回流知识库。回流内容会被转化为结构化补充资料，例如 FAQ 条目、标准解释或纠错说明，并进入 RAG-Anything 增量索引流程。系统会为该条知识标注“教师审核来源”，在后续检索和置信度计算中给予更高可信权重。

#### 4.6.3 反馈优化闭环

反馈优化闭环是本文系统区别于静态问答系统的重要机制，其目标并不是简单记录“用户喜欢或不喜欢某次回答”，而是将问答结果、用户评价、教师修正和知识更新组织为可迭代的质量优化链路。具体而言，系统在每次问答完成后都会同时记录问题文本、查询改写结果、召回证据集合、reranker 分数、最终答案、引用来源、置信度、用户角色、响应时延以及是否触发教师审核等信息，并将这些内容与学生后续提交的“有帮助”“无帮助”“事实错误”“引用不准确”“需要教师解释”等反馈标签进行绑定。这样，系统获得的并不是单一的满意度信号，而是一组能够反映问答质量缺陷来源的多维特征。基于这些特征，平台可进一步构建回答质量评分函数，对每条问答记录进行动态评估，例如将低置信度、低 reranker 分数、证据覆盖不足、负反馈比例高和教师驳回记录多等情况视为高风险信号，并通过综合评分决定该条记录是进入普通归档、进入待观察队列还是直接进入教师审核队列。其优先级可表示为

$$
Priority(i)=\eta_1(1-Conf_i)+\eta_2(1-Sat_i)+\eta_3Err_i+\eta_4Miss_i+\eta_5Repeat_i
$$

其中 $Conf_i$ 表示回答置信度，$Sat_i$ 表示用户满意度得分，$Err_i$ 表示事实错误或引用错误标记，$Miss_i$ 表示证据覆盖不足程度，$Repeat_i$ 表示该问题在历史上被多次问到但回答仍不稳定的程度，$\eta_1 \sim \eta_5$ 为经验权重。优先级较高的问答将被教师优先审核，以提高课程高频问题的修正效率。

教师审核完成后，系统不会直接把修正后的自然语言文本简单覆盖原答案，而是会先将其转化为结构化知识回流单元，包括标准答案、适用知识点、引用资料、适用问题类型、适用课程范围和审核来源等字段。对于通用性较强的修正内容，系统将其沉淀为 FAQ 或课程标准解释；对于针对某份资料的纠错内容，则将其作为补充注释或知识补丁追加到对应资料节点上；对于涉及新概念、新图示关系或新实验步骤的内容，则触发增量图谱更新，使其进入后续检索链路。与此同时，系统还会利用历史反馈样本对检索与推荐参数进行轻量调节，例如当某类问题长期出现“证据相关但排序靠后”的现象时，提高 reranker 权重；当某类知识点长期因资料覆盖不足而被教师补充时，将其标记为“需要补充资源”的课程薄弱区域；当某些资源在推荐后持续获得正向反馈时，提高其质量权重。这样一来，学生反馈并不只是评价结果，而是直接参与知识库质量、检索排序、资源权重和图谱结构的持续优化。通过“问答记录采集—质量诊断—教师修正—结构化回流—增量更新—效果再验证”的循环，平台能够随着课程运行逐步积累教师经验和真实学习行为数据，使问答质量和知识覆盖度在多轮迭代中持续提升。

### 4.7 学习分析与个性化推荐模块

#### 4.7.1 学习行为记录

学习行为记录是个性化支持的基础，因此系统不仅记录学生“是否提问”，还要记录其围绕课程知识进行学习的全过程事件。具体来说，平台将学生提问、查看回答、点击引用来源、查看原始资料、收藏问答、点踩回答、添加错题、完成闪卡复习、打开推荐资料、停留时长和再次追问等行为统一抽象为学习事件，并为每条事件附加课程 ID、班级 ID、知识点标签、时间戳、资源来源、事件强度和结果状态等元数据。这样做的目的在于，将零散交互转化为可计算的学习轨迹，而不是仅仅保存聊天文本。进一步地，系统还会将提问问题所命中的知识点、引用证据所属资料、学生对答案的反馈结果以及后续是否继续追问纳入同一行为链条，从而区分“已经理解的知识点”“暂时理解但不牢固的知识点”和“重复解释后仍然薄弱的知识点”。由于学生学习状态具有时间衰减特征，系统在统计行为时还需要引入时间窗口和衰减因子，使近期行为权重高于久远行为。这样形成的学习行为数据库不仅为画像提供依据，也为第五章中的个性化推荐页面展示与用户操作流程分析提供了数据支撑。

#### 4.7.2 学生画像构建

学生画像并不是对学生能力的简单静态标签，而是对其在课程学习过程中呈现出的知识掌握程度、行为特征和支持需求的动态刻画。基于前述学习事件数据，系统从活跃度、常问主题、薄弱知识点、复习频率、反馈倾向、资料偏好和知识迁移能力等维度构建画像。其中，薄弱知识点识别是最核心的部分，其目标是判断学生在哪些课程概念上存在持续性理解困难。为此，系统需要综合考虑学生围绕某知识点的提问次数、负反馈次数、错题出现次数、闪卡复习正确率、资料点击情况和教师重点标注等因素，并结合时间衰减机制动态更新掌握度估计。知识点掌握度可以按如下方式计算：

$$
Mastery(t)=0.35A_t+0.20F_t+0.15P_t+0.15R_t+0.10L_t+0.05T_t
$$

其中 $A_t$ 表示作业或测验表现，$F_t$ 表示闪卡复习表现，$P_t$ 表示问答反馈，$R_t$ 表示近期复习行为，$L_t$ 表示学习活跃度，$T_t$ 表示教师重点权重。对于暂未引入完整作业系统的原型阶段，可将 $A_t$ 由错题频率、重复追问次数和教师纠错记录近似替代。在此基础上，系统将掌握度低于阈值的知识点标记为薄弱点，将掌握度接近中间区间且近期多次复习的知识点标记为待巩固点，将掌握度较高且长期稳定的知识点标记为已掌握点。画像中除知识掌握度外，还会保存学生偏好的资源类型，例如更倾向于阅读课件摘要、查看图谱节点还是观看视频片段，以便后续推荐结果尽可能匹配其学习习惯。这样的画像机制能够使个性化支持不再停留在简单的“热门资料推送”，而是转向围绕学生当前学习状态的精细化辅助。

#### 4.7.3 推荐算法

在毕业设计原型阶段，个性化推荐不以训练复杂推荐模型为主要目标，而是采用“知识图谱 + 学习画像 + 资源质量”的可解释规则排序方案。推荐流程分为候选生成、相关性排序和结果解释三个步骤。首先，系统围绕学生当前薄弱知识点、近期提问主题和课程当前章节，从课程资料、教师审核 FAQ、补充解释、闪卡、错题练习、图谱邻接节点和相关音视频片段中生成候选集合；其次，系统结合薄弱知识点匹配程度、图谱前置关系重要性、与近期问题的语义相似度、资源难度匹配度、资源历史质量分和多样性约束对候选资源进行打分；最后，系统在返回推荐结果时同步给出推荐理由，使学生和教师都能够理解推荐来源。推荐分数可表示为：

$$
RecScore(r,u)=0.30W+0.20G+0.15S+0.15D+0.10Q+0.10V
$$

其中 $W$ 表示资源与学生薄弱知识点的匹配度，$G$ 表示图谱前置关系重要性，$S$ 表示与近期提问的语义相似度，$D$ 表示难度匹配度，$Q$ 表示资源质量，$V$ 表示多样性调整。这里的资源质量 $Q$ 并不是人工固定值，而是由教师标注、历史点击率、学习完成率、收藏率和正向反馈共同更新的动态指标；多样性调整 $V$ 则用于防止系统连续多次只推荐同一种形式的资源，避免推荐结果单一化。为了进一步增强推荐合理性，系统还会在排序后加入规则约束，例如优先保证推荐列表中至少包含一项前置知识资料、一项当前知识点讲解和一项巩固练习，从而形成“预备学习—核心理解—巩固复习”的支持结构。最终呈现给学生的推荐内容不仅包含资源标题，还包括推荐理由、对应知识点、预计学习时长和来源类型，例如“推荐阅读第 3 章课件第 12 页，因为你最近连续两次提问慢启动结束条件，该页面解释了阈值变化并与拥塞避免的切换条件直接相关”。这种做法即使在没有训练推荐模型的情况下，也能够保持较强的可解释性和教学合理性。

### 4.8 前后端交互与可视化实现

前后端交互与可视化实现的目标，不仅是把后端能力“显示出来”，更重要的是让不同角色能够在统一界面中理解知识库状态、问答依据、审核过程和推荐逻辑。学生端课程页面中的 AI 助教组件通过真实问答接口与后端通信，用户输入问题后，页面需要同步展示问题发送状态、检索中状态、最终答案、引用来源、置信度提示和推荐追问；当回答引用到文档页码、图片区域或视频时间段时，前端还应支持点击引用后跳转到对应资料位置，增强回答的可核验性。对于用户上传的临时附件，前端需在提问区域显示附件卡片、解析状态和是否写入课程知识库的选择，从而让学生明确区分“当前提问附件”和“正式课程资料”。

教师端的交互重点在于知识库管理和质量控制。课程知识库页面应展示资料列表、文件类型、上传时间、解析阶段、索引状态、chunk 数量、图节点数量、失败原因和重试入口，使教师能够清楚判断某份资料是否已经真正进入 RAG-Anything 链路。教师审核页面则需要同时展示原始问题、AI 回答、证据片段、置信度组成原因、学生反馈标签和教师修正表单，以便教师基于证据而非凭印象进行修改。对于回流知识，前端还应允许教师选择回流方式，例如作为 FAQ、补充解释、知识图谱节点说明或资料注释，并选择关联的课程知识点。

知识图谱可视化页面是系统教学解释能力的重要组成部分。该页面以课程为单位展示实体节点和关系边，支持按章节、实体类型、资料来源、时间范围和置信度进行筛选；点击节点后展示实体说明、相关资料、典型问答、教师标注和学生高频提问情况。这样，图谱展示不只是为了美观，而是使教师能够从整体上观察课程知识结构覆盖情况，发现哪些知识点资料不足、哪些关系需要补充、哪些节点是学生理解难点。管理员端页面则更关注系统运行与配置，包括模型路由信息、向量库与图数据库连接状态、MinIO 存储状态、异步任务队列、RAG 查询成功率、低置信率、回退率和实验评测结果等指标。通过前后端联动，系统实现了“资料上传—解析入库—图谱构建—检索问答—教师审核—个性化推荐”的全过程可视化，使复杂的 AI 处理链路能够在教学场景中以可理解、可追踪和可管理的方式呈现出来。

### 4.9 本章小结

本章详细阐述了系统关键模块的设计与实现。完善后的系统以 RAG-Anything 为正式知识处理主链路，通过多模态预处理、统一 content item、向量索引、双图构建、混合检索、reranker、证据生成、教师审核和个性化推荐，形成从课程资料到智能问答再到知识更新的完整闭环。

## 第五章 系统前端功能展示与交互流程

### 5.1 章节组织与展示思路

为了使系统功能展示更加符合真实使用习惯，本章不再按孤立模块罗列页面，而是按照“用户进入系统后的操作顺序”展开说明。具体展示顺序为：先展示注册与登录页面，再展示学生加入课程后的首页和课程空间，随后展示教师端课程管理页面，最后展示管理员端系统管理页面。这样安排能够使读者从完整业务链路出发理解前端设计，而不是只看到零散界面截图。

在具体撰写时，每个小节可配合 1 至 2 张页面截图，并围绕“页面位置、核心功能、关键交互、与后端接口关系”四个方面进行说明。对于具有明显流程性的页面，例如注册、登录、加入课程、进入课程、AI 提问和教师审核等页面，应重点说明页面之间的跳转关系和状态变化。

### 5.2 用户注册与登录页面展示

#### 5.2.1 用户注册页面展示

注册页面是用户进入系统的第一入口。根据当前前端实现，注册页面支持学生和教师两类身份切换，并采用分步骤表单组织基础信息、学校信息和账号安全信息。学生注册页面重点展示真实姓名、学号、邮箱、手机号、学校、学院、专业、年级和班级等字段；教师注册页面则重点展示工号、院系、职称和身份校验信息。论文展示时可按照“身份选择 - 基本信息填写 - 学校信息填写 - 密码设置与提交”的顺序介绍界面。

#### 5.2.2 用户登录页面展示

登录页面承担身份识别和系统分流作用。当前页面提供学生、教师和管理员三种身份入口，用户选择身份后输入账号和密码即可登录。该部分建议重点展示角色切换按钮、账号密码输入区域、密码显隐控制和登录后的跳转结果，使读者清楚理解系统如何在统一入口下区分不同角色的后续界面。

#### 5.2.3 登录后的角色化跳转展示

系统在登录成功后会按照用户身份跳转到不同首页。学生跳转至学生工作台，教师跳转至教师工作台，管理员跳转至管理员控制台。该小节可用流程图或三张小图并列展示“同一登录入口 - 不同身份 - 不同首页”的关系，以突出前端的角色化设计思想。

### 5.3 学生端首页与加入课程流程展示

#### 5.3.1 学生首页总体布局

学生首页是学生用户登录后的核心工作台。页面包含学习空间、我的课程、通知中心和个人中心等主要导航区域，并在首页集中展示 AI 助教快捷提问入口、待办事项、课程进度和学习建议。撰写时可先概括页面的整体布局，再逐一说明顶部导航、主内容区和侧边信息区的功能分布。

#### 5.3.2 邀请码加入课程流程展示

学生加入课程是业务流程中的关键步骤。当前前端在学生工作台中提供“加入课程”弹窗，学生输入教师提供的邀请码后即可进入课程列表。该小节非常适合按步骤展示：第一步进入“我的课程”页面，第二步打开加入课程弹窗，第三步输入邀请码，第四步完成课程加入并进入课程详情页。这样就能自然衔接“注册 - 登录 - 加入课程”的主线流程。

#### 5.3.3 通知中心与个人中心展示

在完成课程加入后，学生还会频繁使用通知中心和个人中心。通知中心用于接收作业提醒、教师回复、考试通知和 AI 学习建议；个人中心用于修改头像、联系方式和个人资料。该部分可以作为学生基础功能的补充展示，使第五章中的学生端页面结构更加完整。

### 5.4 学生课程空间功能展示

#### 5.4.1 课程首页与分区导航

学生进入具体课程后，会来到课程空间页面。该页面通常包含课程首页、课程资料、任务中心、互动讨论、错题本、学习闪卡和 AI 助教等多个功能分区。建议在本节先给出课程空间整体截图，再说明顶部课程信息、导航标签以及各功能区之间的切换方式。

#### 5.4.2 课程资料浏览与知识图谱辅助

课程资料页面用于展示教师上传的课件、讲义、文档、图片、音视频等学习资源，并支持搜索、预览和知识图谱辅助查看。论文中可重点展示资料列表、文件预览窗口、章节跳转和知识图谱入口，说明学生如何从课程资料中快速定位知识点。

#### 5.4.3 AI 助教问答与附件交互

AI 助教是学生课程空间中的核心功能。该页面支持输入自然语言问题、上传图片或附件、查看答案引用、继续追问以及将问题转交教师。展示时可依次说明提问输入框、附件上传区域、答案区域、引用跳转和问题反馈按钮，突出系统与普通聊天页面相比具有“带课程上下文、带证据引用、带教学反馈”的特点。

#### 5.4.4 任务中心、错题本与学习闪卡

除问答外，学生课程空间还整合了学习任务管理功能，包括作业提交、考试查看、错题整理和闪卡复习。本节可采用“任务中心 - 错题本 - 学习闪卡”三级结构展示，使读者看到系统不仅能答疑，也能支撑持续学习与复习管理。

#### 5.4.5 互动问答、讨论区与 FAQ

课程空间中的互动模块用于支持学生与教师、学生与学生之间的交流。页面包括我的提问、班级讨论区和课程 FAQ 等内容。这里建议结合具体页面截图说明：学生如何提出问题、如何查看教师回复、如何参与讨论，以及如何查看教师整理的高频问题汇总。

### 5.5 教师端页面功能展示

#### 5.5.1 教师工作台与课程创建

教师登录后首先进入教师工作台。页面通常展示课程总览、待处理通知、学生提问提醒、课程统计和创建新课程入口。展示时可围绕“教师如何创建课程、生成邀请码并邀请学生加入”这一主线展开，这样能够与前面学生端的“加入课程”流程形成呼应。

#### 5.5.2 课程空间中的资料上传与知识库管理

教师进入具体课程后，可以在课程知识页面上传课件、文档、图片、音视频等资料，并查看资料状态、知识图谱节点数量和知识库健康度。该小节建议重点展示资料上传弹窗、资料列表、状态标签、图谱可视化区域和课程知识管理面板，以体现教师端“内容供给者”和“知识维护者”的角色。

#### 5.5.3 任务发布、学生管理与审核回流

教师课程空间还包含任务发布、学生管理、讨论管理和 AI 审核回流等功能。论文中可按“发布作业或通知 - 查看学生学习情况 - 审核 AI 回答 - 将修正内容回流知识库”的顺序进行展示，这样既能说明页面功能，也能体现系统闭环机制。

### 5.6 管理员端页面功能展示

#### 5.6.1 系统概览与用户审核

管理员端主要承担平台级监管与配置职责。系统概览页面展示平台注册人数、课程数量、活跃班级、待审核注册和系统状态等信息；用户管理页面展示注册审核、用户检索和角色管理功能。该部分建议通过管理员首页和用户管理页两张截图说明平台如何进行统一治理。

#### 5.6.2 课程管理、内容审核与系统设置

除用户管理外，管理员还可以查看课程总览、知识库状态、内容审核记录和系统设置项。该小节可展示课程管理表格、内容审核列表以及系统设置面板，用于说明平台在运行层面具备课程监管、内容安全和系统维护能力。

### 5.7 典型前端操作流程分析

#### 5.7.1 从注册到加入课程的完整流程

本小节建议作为学生端页面展示的总串联。流程可概括为：用户进入注册页面完成身份注册，返回登录页面完成登录，进入学生首页后点击加入课程，输入邀请码，最后进入课程空间。该流程正好对应系统最基础也最完整的前端使用路径，适合作为第五章的重点案例。

#### 5.7.2 从进入课程到 AI 问答的完整流程

学生加入课程后，可以在课程空间中浏览资料、发起 AI 提问、查看答案引用并继续追问。本节建议把课程页面、AI 助教组件和引用回跳页面串联起来，说明系统如何将课程资料、知识图谱和问答交互整合到一个连贯界面中。

#### 5.7.3 从教师建课到学生学习的联动流程

为了突出系统的多角色协同特征，本节可从教师端创建课程、上传资料、发布任务和生成邀请码开始，再展示学生端加入课程、进入学习空间和使用 AI 助教的过程。这样能够把教师端和学生端页面放到同一条业务链路中，使前端展示不再是割裂的页面堆砌，而是完整的教学服务闭环。

### 5.8 本章小结

本章按照真实用户操作顺序，对系统前端页面与关键交互流程进行了展示。通过用户注册、登录、加入课程、课程学习、教师管理和管理员配置等页面的逐步说明，可以更直观地体现系统在多角色教学场景中的界面组织方式与业务闭环实现路径。

## 第六章 总结与展望

### 6.1 全文总结

本文围绕“基于大语言模型和检索增强生成的助教平台设计”开展研究，针对课程资料分散、教师重复答疑压力大、通用大模型缺少课程边界和回答证据不足等问题，设计并实现了一个面向课程场景的 AI 助教平台。系统以 RAG-Anything 为核心知识引擎，结合多模态解析、向量检索、双图构建、重排序、引用生成、教师审核和学习分析，形成从课程资料入库到智能问答再到知识回流的完整链路。

本文的主要工作包括：第一，完成学生、教师和管理员三类角色的需求分析与系统总体架构设计；第二，设计基于 RAG-Anything 的课程知识库构建流程，实现文档、图片、音频和视频资料的分流处理；第三，基于 LightRAG/RAG-Anything 的双图机制构建课程知识图谱，并同步到业务图谱层进行可视化；第四，设计完整的检索增强问答流程，包括问题分类、上下文组装、混合检索、重排序、答案生成、引用溯源和置信度输出；第五，构建教师审核与知识回流机制，使用户反馈和教师修正能够持续优化知识库；第六，设计基于学习画像和知识图谱的可解释推荐算法，为学生提供个性化学习支持。

### 6.2 不足分析

系统仍存在一些不足。首先，多模态解析和 RAG-Anything 全链路对外部依赖较多，本地部署需要较高配置和较复杂环境。其次，知识图谱构建虽然以 RAG-Anything/LightRAG 为基础，但实体规范化和关系质量仍受模型能力影响，需要教师审核和后续优化。再次，音视频资料处理会带来较高计算和存储成本，实时性不如文本资料。最后，个性化推荐仍以规则和统计信号为主，尚未在大规模真实学习数据上训练推荐模型。

### 6.3 未来工作展望

后续可以从以下方向继续完善。第一，优化多模态解析性能，提升长视频、扫描文档和复杂公式的处理质量。第二，引入更稳定的向量库和图数据库部署方案，增强知识库规模化能力。第三，优化实体消歧、关系抽取和图谱校正机制，提高课程知识图谱质量。第四，构建更大规模的课程问答评测集，对检索、重排序和生成效果进行系统比较。第五，在积累足够学习行为数据后，引入轻量推荐模型或学习路径预测模型，进一步提升个性化支持能力。第六，扩展教师协同能力，使教师能够更方便地编辑图谱、标注重点资源和管理知识更新策略。

## 参考文献

[1] Lewis P, Perez E, Piktus A, et al. Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks. NeurIPS, 2020.

[2] Gao Y, Xiong Y, Gao X, et al. Retrieval-Augmented Generation for Large Language Models: A Survey. arXiv preprint arXiv:2312.10997, 2023.

[3] Guo Z, Xia L, Yu Y, et al. LightRAG: Simple and Fast Retrieval-Augmented Generation. GitHub Repository, HKUDS, 2024.

[4] HKUDS. RAG-Anything: All-in-One RAG Framework. GitHub Repository, 2025. https://github.com/HKUDS/RAG-Anything

[5] HKUDS. RAG-Anything: All-in-One RAG Framework. arXiv:2510.12323, 2025.

[6] Wang X, et al. MinerU: An Open-Source Solution for Precise Document Content Extraction. 2024.

[7] Devlin J, Chang M W, Lee K, Toutanova K. BERT: Pre-training of Deep Bidirectional Transformers for Language Understanding. NAACL, 2019.

[8] Vaswani A, Shazeer N, Parmar N, et al. Attention Is All You Need. NeurIPS, 2017.

[9] OpenAI. GPT-4 Technical Report. arXiv preprint arXiv:2303.08774, 2023.

[10] Qwen Team. Qwen2.5 Technical Report. 2024.

[11] BAAI. BGE Embedding and Reranker Models. 2023.

[12] Neo4j Graph Data Platform Documentation. https://neo4j.com/docs/

[13] Qdrant Vector Database Documentation. https://qdrant.tech/documentation/

[14] MinIO Object Storage Documentation. https://min.io/docs/

[15] FastAPI Documentation. https://fastapi.tiangolo.com/

## 致谢

在本论文完成过程中，感谢指导教师在选题、系统设计和论文撰写方面给予的指导与建议。感谢同学和测试用户在系统体验过程中提出的反馈意见。本文系统从课程资料管理、智能问答到知识回流和学习支持，涉及多个模块和技术组件，开发与整理过程也使本人对大语言模型、检索增强生成、多模态解析和教育系统设计有了更深入理解。谨向所有给予帮助和支持的人表示诚挚感谢。

## 附录 A 核心流程示意

### A.1 课程资料入库流程

资料上传 -> 权限校验 -> 哈希去重 -> 原始文件存储 -> 解析任务创建 -> 文件类型分流 -> 文档/图片进入 RAG-Anything -> 音视频预处理后进入 RAG-Anything -> content item 生成 -> embedding -> 双图构建 -> 向量库/图库存储 -> 业务库同步 -> 教师端展示。

### A.2 用户问答流程

用户提问 -> 课程上下文校验 -> 多轮历史摘要 -> 问题分类 -> 查询改写 -> 临时附件解析 -> RAG-Anything 混合检索 -> reranker 重排序 -> 证据组织 -> 置信度计算 -> LLM 生成回答 -> 引用溯源 -> 反馈记录 -> 低置信审核。

### A.3 教师审核回流流程

低置信或负反馈 -> 审核项生成 -> 教师查看证据 -> 修正答案 -> 选择回流 -> FAQ/补充资料生成 -> 增量 embedding -> 图谱更新 -> 后续问答复用。
