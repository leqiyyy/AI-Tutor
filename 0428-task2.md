# 0428 Task 2: 多轮对话、学生画像、个性化推荐与学习路径实施计划

更新时间：2026-04-29

目标：在另一个开发线程继续修改 AI 助教核心主链路的同时，本线程并行补强多轮对话上下文处理、学生画像、个性化推荐、学习路径生成与实验评估能力，并尽量降低代码冲突。

现有 AI 助教主链路必须保持稳定：

```text
资料上传 -> 索引 -> 用户问题处理 -> 检索 -> 重排 -> 生成回答
-> 学生点赞/点踩/反馈 -> 教师审核 -> 教师修改答案 -> 保存并回流知识库
```

本计划不是要求能力完全隔离，而是要求采用“先独立实现、后小切口接入”的方式：先把可独立运行的服务、模型、算法和测试准备好，等核心主链路稳定后，再用少量 hook 接入 `chat_service` / RAG 调用链路。

---

## 1. 总体原则

1. 并行开发期间，优先新增文件，少改高冲突文件。
2. 暂不直接修改 `RAGAnythingAdapter`、`chat_service.py`、`kb_service.py` 等核心主链文件；如必须接入，单独做小 patch。
3. 不改 `RAGAnythingAdapter` 的主索引和主查询语义，只在进入 RAG 前提供更好的 query/context。
4. 不直接污染现有 `KnowledgeEntity` / `KnowledgeRelation` 知识图谱结构。
5. 学习路径使用单独的教学图谱投影层，消费原知识图谱结果，但不反向影响 RAG 检索图谱。
6. 学生画像、推荐、路径生成从现有行为日志、问答、任务、闪卡、审核数据中派生。
7. 推荐和画像更新不阻塞 `chat/query` 主响应。
8. 第一版优先使用可解释规则算法，后续再升级 BKT/DKT、Personalized PageRank、Beam Search 等算法。
9. 所有新增算法输出必须可解释、可评估、可写入实验报告。

---

## 1.1 并行开发低冲突策略

当前另一个线程正在修改 AI 助教核心主链路，因此本任务采用以下策略：

### 低冲突优先修改区

优先新增：

- `backend/app/services/conversation_context_service.py`
- `backend/app/services/student_profile_service.py`
- `backend/app/services/recommendation_service.py`
- `backend/app/services/learning_graph_service.py`
- `backend/app/services/learning_path_service.py`
- `backend/app/models/personalization.py`
- `backend/tests/test_conversation_context_service.py`
- `backend/tests/test_personalization_services.py`

这些文件与当前主链修改重叠较低，可先实现算法和数据结构。

### 延后接入区

以下文件可能被另一个线程频繁修改，暂缓大改：

- `backend/app/services/chat_service.py`
- `backend/app/integrations/rag/raganything_adapter.py`
- `backend/app/services/kb_service.py`
- `backend/app/api/v1/chat.py`
- `backend/app/api/v1/frontend_compat.py`

后续只做小切口接入：

```text
chat_service.send_message
-> build_conversation_context(...)
-> rag.query(question=standalone_question, ...)
-> record context meta
```

### 数据库迁移策略

模型可以先新增，但 Alembic migration 建议在核心主链改动合并稳定后统一生成，避免多个线程同时新增 revision 造成 head 冲突。

如果需要本地快速验证，可先依赖 `AUTO_CREATE_TABLES=true` 的开发模式；正式部署前再补迁移脚本。

### 接口策略

推荐、画像、学习路径可先以独立服务函数和后端单测验证。API 层可以后置，避免与前端联调线程冲突。

---

## 2. 阶段一：多轮对话上下文管理

### 2.1 当前问题

当前系统已经保存会话和消息，并在问答时取最近若干历史消息作为 `history` 传给 RAG。但这仍属于基础多轮能力，缺少：

- 长对话摘要。
- 当前追问的指代消解。
- 将省略式问题改写成独立问题。
- 历史上下文与本轮 RAG 证据的显式分层。
- token budget 管理。
- 多轮对话专项评测。

### 2.2 修改点

新增：

- `backend/app/services/conversation_context_service.py`

修改：

- `backend/app/services/chat_service.py`
- `backend/app/models/chat.py`
- Alembic migration

建议扩展 `ChatSession` 字段：

- `summary`
- `summary_updated_at`
- `context_version`

### 2.3 方法

在 `chat_service.send_message` 调用 RAG 前新增上下文构建步骤：

```text
用户原始问题
-> 读取 session 最近 6-10 轮原文
-> 读取 session 长期摘要
-> 判断当前问题意图
-> 生成 standalone_question
-> 用 standalone_question 执行 RAG 检索
-> 回答生成时保留原始问题、最近历史、摘要和检索证据
-> 回答完成后更新 session summary
```

建议核心函数：

```python
def build_conversation_context(db, session, current_question) -> dict:
    ...

def rewrite_standalone_question(history, summary, question) -> str:
    ...

def classify_context_intent(history, question) -> str:
    ...

def update_session_summary(db, session, recent_messages) -> None:
    ...
```

`context_intent` 建议枚举：

- `new_topic`
- `follow_up`
- `clarification`
- `review_request`
- `compare`
- `unknown`

### 2.4 接入方式

保留现有：

```python
result = await rag.query(
    question=content,
    class_id=class_id,
    history=history,
    attachments=prepared_attachments,
    role=role,
)
```

调整为：

```python
conversation_context = build_conversation_context(
    db=db,
    session=session,
    current_question=content,
)

result = await rag.query(
    question=conversation_context["standalone_question"],
    class_id=class_id,
    history=conversation_context["recent_turns"],
    attachments=prepared_attachments,
    role=role,
)
```

同时在 `rag_query_events.extra_data` 中记录：

- `original_question`
- `standalone_question`
- `context_intent`
- `history_turn_count`
- `summary_used`
- `conversation_context_version`

### 2.5 验收标准

- 用户问“那第二点呢？”能结合上一轮问题完成检索。
- 用户切换新主题时，不被旧历史污染。
- 多轮追问返回的 sources 仍来自课程知识库。
- `rag_query_events` 能记录上下文处理结果。
- 如果上下文改写失败，自动回退到原始问题，不影响主链路。

---

## 3. 阶段二：学生画像增强

### 3.1 当前问题

当前学生画像主要基于：

- 提问数。
- 点踩数。
- 任务完成率。
- 学习行为数量。
- 弱知识点关键词。
- 强知识点关键词。

这适合第一版可解释画像，但不足是：

- 弱知识点主要来自关键词统计。
- 没有概念级掌握度。
- 没有画像置信度。
- 没有风险信号。
- 没有偏好模型。

### 3.2 修改点

新增模型：

- `StudentConceptMastery`
- `StudentLearningPreference`
- `StudentRiskSignal`

修改：

- `backend/app/models/analytics.py`
- `backend/app/services/analytics_service.py`
- Alembic migration

### 3.3 建议数据结构

`StudentConceptMastery`：

```text
id
user_id
class_id
concept_id
concept_name
mastery_score
confidence
evidence_count
last_event_at
source_breakdown
extra_data
created_at
updated_at
```

`StudentLearningPreference`：

```text
id
user_id
class_id
preferred_resource_types
preferred_difficulty
preferred_study_time
interaction_summary
extra_data
created_at
updated_at
```

`StudentRiskSignal`：

```text
id
user_id
class_id
risk_type
risk_level
reason
evidence
status
created_at
updated_at
```

### 3.4 第一版画像算法

先使用规则版 mastery 估计：

```text
mastery_score =
  base_score
  + task_score_signal
  + flashcard_signal
  + teacher_verified_signal
  - low_confidence_signal
  - dislike_signal
  - repeated_question_signal
```

信号来源：

- `Task` / `Submission`
- `FlashcardRecord`
- `ChatMessage`
- `ReviewItem`
- `ReviewSyncRecord`
- `LearningRecord`
- `QuestionAnalytics`
- `KnowledgeEntity`

### 3.5 后续算法升级

第二版可接入 BKT：

```text
P(L_t | evidence)
P(learn)
P(guess)
P(slip)
P(transition)
```

第三版再考虑 DKT / SAKT / AKT，但前提是已有足够学生行为序列数据。

### 3.6 验收标准

- 学生画像接口能返回概念级掌握度。
- 每个掌握度分数能解释证据来源。
- 画像更新失败不影响聊天主流程。
- 能在实验报告中统计画像覆盖率、低掌握知识点数量、风险学生数量。

---

## 4. 阶段三：教学图谱投影层

### 4.1 是否影响原知识图谱

不建议直接改原来的知识图谱。

原因：

- 原知识图谱主要服务 RAG 检索、资料溯源、图谱展示。
- 学习路径需要教学语义，如先修关系、章节顺序、难度、学习目标、资源匹配。
- 两者职责不同，混在一起会增加 RAG 图谱污染风险。

因此新增一个教学图谱投影层：

```text
RAG 知识图谱 -> 教学图谱投影 -> 学习路径/推荐/画像
```

### 4.2 修改点

新增模型：

- `LearningConcept`
- `LearningConceptRelation`
- `LearningResourceLink`

新增服务：

- `backend/app/services/learning_graph_service.py`

新增接口可选：

- `GET /api/v1/recommendations/learning-graph`
- `POST /api/v1/admin/learning-graph/rebuild`

### 4.3 投影规则

从原图谱投影：

```text
KnowledgeEntity -> LearningConcept
KnowledgeRelation -> LearningConceptRelation
Material -> LearningResourceLink
Task -> LearningResourceLink
Flashcard -> LearningResourceLink
ReviewSyncRecord -> LearningResourceLink
```

关系类型建议：

- `prerequisite`
- `part_of`
- `related_to`
- `example_of`
- `remediates`
- `assesses`
- `reviewed_answer_for`

### 4.4 教学字段

`LearningConcept` 建议包含：

- `concept_name`
- `concept_type`
- `difficulty`
- `importance`
- `chapter`
- `source_entity_id`
- `confidence`
- `extra_data`

`LearningConceptRelation` 建议包含：

- `source_concept_id`
- `target_concept_id`
- `relation_type`
- `weight`
- `confidence`
- `source_relation_id`
- `extra_data`

### 4.5 验收标准

- 原知识图谱接口不变。
- 教学图谱投影失败不影响 RAG 问答。
- 能从教学图谱找到某个弱知识点的先修知识点。
- 能解释学习路径中的每一步来自哪条图谱关系。

---

## 5. 阶段四：个性化推荐升级

### 5.1 当前问题

当前推荐已经有可解释规则版，但仍比较粗：

- 弱知识点主要靠关键词。
- 资源与知识点关联不够结构化。
- 用户偏好反馈较弱。
- 推荐曝光、点击、完成、不感兴趣等事件未形成完整闭环。

### 5.2 修改点

新增服务：

- `backend/app/services/recommendation_service.py`

修改：

- `backend/app/api/v1/recommendations.py`
- `backend/app/services/analytics_service.py`
- `backend/app/models/analytics.py`

新增模型可选：

- `RecommendationEvent`
- `RecommendationSnapshot`

### 5.3 推荐评分公式

保留当前 `explainable_weighted_rules` 作为 baseline，升级为明确评分器：

```text
score =
  0.35 * knowledge_gap_score
+ 0.20 * content_relevance
+ 0.15 * urgency
+ 0.10 * difficulty_match
+ 0.10 * user_preference
+ 0.10 * freshness
```

各项含义：

- `knowledge_gap_score`：来自 `StudentConceptMastery`。
- `content_relevance`：资源与知识点的关联强度。
- `urgency`：任务截止、低掌握知识点频繁出现、教师审核频次。
- `difficulty_match`：资源难度与当前掌握度是否匹配。
- `user_preference`：点击、完成、收藏、不感兴趣等反馈。
- `freshness`：资料新鲜度。

### 5.4 推荐事件

建议记录：

- `impression`
- `click`
- `complete`
- `dismiss`
- `helpful`
- `not_helpful`
- `save`

### 5.5 推荐结果结构

推荐项应包含：

```json
{
  "target_id": "...",
  "target_type": "material",
  "title": "...",
  "score": 0.87,
  "reason": "...",
  "evidence_signals": {
    "knowledge_gap_score": 0.9,
    "content_relevance": 0.8,
    "urgency": 0.4,
    "difficulty_match": 0.7,
    "user_preference": 0.5,
    "freshness": 0.6
  }
}
```

### 5.6 验收标准

- 推荐结果可解释。
- 学生反馈会影响后续排序。
- 推荐接口保持兼容前端现有调用。
- 能在 benchmark 中比较推荐前后排序变化。

---

## 6. 阶段五：学习路径生成

### 6.1 修改点

新增模型：

- `LearningPath`
- `LearningPathStep`

新增服务：

- `backend/app/services/learning_path_service.py`

修改接口：

- `GET /api/v1/recommendations/learning-path`

### 6.2 第一版路径生成方法

第一版使用可解释图搜索与规则排序：

```text
1. 读取学生低掌握知识点。
2. 从教学图谱中补齐先修节点。
3. 按 prerequisite 关系做拓扑排序。
4. 每个知识点匹配资料、教师 FAQ、闪卡、任务。
5. 根据学生可用时间生成 3-7 天学习路径。
6. 返回每一步的推荐理由和证据。
```

### 6.3 后续算法升级

第二版：

- Personalized PageRank 计算候选知识点重要性。
- Beam Search 生成 top-k 路径。
- 加入路径长度、难度梯度、先修约束、资料可用性。

第三版：

- 基于 BKT mastery 动态重排。
- 比较 KG path search 与规则路径的效果。

### 6.4 路径步骤结构

`LearningPathStep` 建议包含：

```text
id
path_id
step_order
concept_id
concept_name
goal
recommended_resources
estimated_minutes
reason
status
completed_at
extra_data
```

### 6.5 验收标准

- 学生端能看到本周学习路径。
- 每个步骤能解释为什么学习该知识点。
- 路径中不违反明显先修关系。
- 完成路径节点后能更新 mastery。
- 路径生成失败不影响普通推荐和 AI 问答。

---

## 7. 阶段六：实验评估增强

### 7.1 修改点

修改：

- `backend/scripts/run_research_benchmark.py`

新增 fixture：

- `backend/tests/fixtures/benchmark/multiturn_spec.json`
- `backend/tests/fixtures/benchmark/recommendation_spec.json`
- `backend/tests/fixtures/benchmark/learning_path_spec.json`

### 7.2 多轮对话评估

新增指标：

- `standalone_rewrite_success_rate`
- `context_carry_over_accuracy`
- `multi_turn_answer_correctness`
- `history_noise_failure_count`

对比组：

- 原始 query。
- 最近历史直接拼接。
- standalone question rewrite。
- standalone question rewrite + session summary。

### 7.3 推荐评估

新增指标：

- `precision_at_k`
- `ndcg_at_k`
- `hit_rate_at_k`
- `ctr`
- `completion_rate`
- `dismiss_rate`

对比组：

- 当前规则推荐。
- mastery enhanced 推荐。
- KG path enhanced 推荐。

### 7.4 学习路径评估

新增指标：

- `path_coverage`
- `prerequisite_violation_count`
- `avg_difficulty_jump`
- `resource_coverage`
- `mastery_improvement`

### 7.5 RAG 原指标保持

继续保留：

- `Recall@K`
- `nDCG@K`
- `MRR`
- `fallback_rate`
- `main_chain_success_rate`
- `avg_confidence`
- `p95_latency_ms`

### 7.6 验收标准

- benchmark 输出 JSON 和 Markdown。
- 能比较多轮上下文策略。
- 能比较推荐算法策略。
- 能比较学习路径生成策略。
- 实验结果可直接进入论文实验章节。

---

## 8. 推荐实施顺序

### 当前执行进展（2026-04-29）

已按“低冲突优先”开始执行，当前没有修改 `chat_service.py`、`RAGAnythingAdapter`、`kb_service.py` 等 AI 助教核心主链文件。

本轮已新增：

- `backend/app/models/personalization.py`
- `backend/app/services/conversation_context_service.py`
- `backend/app/services/student_profile_service.py`
- `backend/app/services/recommendation_service.py`
- `backend/app/services/learning_graph_service.py`
- `backend/app/services/learning_path_service.py`
- `backend/tests/test_personalization_foundations.py`

本轮极小修改：

- `backend/app/models/__init__.py`：仅注册新增 personalization models，保证 SQLAlchemy metadata 可发现。
- `backend/app/api/v1/recommendations.py`：保留旧接口不变，新增低冲突 v2 接口。

本轮新增接口：

- `GET /api/v1/recommendations/materials-v2`
- `GET /api/v1/recommendations/learning-path-v2`
- `POST /api/v1/recommendations/events`
- `POST /api/v1/recommendations/learning-graph/rebuild`

暂未做：

- 暂未修改 `chat_service.py` 接入 `standalone_question`。
- 暂未新增 Alembic migration，避免和另一个线程同时生成 migration head 造成冲突。
- 暂未改前端接口。
- 新增 v2 接口依赖 `personalization.py` 中的新表；正式联调前需要补 Alembic migration，或在本地开发环境使用 `AUTO_CREATE_TABLES=true` 建表。

验证情况：

- `python3 -m compileall` 已通过新增 Python 文件语法检查。
- 当前环境缺少 `pytest`，所以 `python3 -m pytest backend/tests/test_personalization_foundations.py -q` 暂不能运行。
- 已用 `python3 -c` 验证推荐排序和学习路径先修排序核心函数可执行。
- 已用 `python3 -m compileall` 验证新增 v2 recommendations API 文件语法通过。

### P0：不影响主链的基础增强

1. 新增 `conversation_context_service.py`。
2. 在 `chat_service.send_message` 中接入 `standalone_question`，失败则回退原始问题。
3. 在 `rag_query_events.extra_data` 记录上下文处理字段。
4. 增加多轮对话单测。

### P1：画像与推荐基础闭环

1. 新增 `StudentConceptMastery`。
2. 实现规则版 mastery 更新。
3. 升级推荐评分器，但保留旧响应结构。
4. 记录推荐反馈事件。

### P2：教学图谱投影与学习路径

1. 新增教学图谱投影模型。
2. 实现从 `KnowledgeEntity` / `KnowledgeRelation` 的投影。
3. 实现拓扑排序版学习路径。
4. 将学习路径接入 `/recommendations/learning-path`。

### P3：实验与部署验证

1. 扩展 benchmark。
2. 新增多轮对话 spec。
3. 新增推荐 spec。
4. 新增学习路径 spec。
5. 输出实验报告。

---

## 9. 风险与规避

### 风险 1：上下文改写影响 RAG 检索

规避：

- 保存 `original_question`。
- 改写失败或低置信时使用原始问题。
- benchmark 比较 rewrite 前后 Recall@K。

### 风险 2：学习路径污染原知识图谱

规避：

- 使用单独教学图谱投影表。
- 原知识图谱只读消费。
- 投影可重建、可删除。

### 风险 3：画像更新拖慢聊天响应

规避：

- 聊天主流程只记录轻量 `LearningRecord`。
- mastery 更新可在回答后执行，或后续放入 Celery。

### 风险 4：推荐算法不可解释

规避：

- 第一版使用固定权重公式。
- 每个推荐项返回 `evidence_signals`。
- 实验报告中保留权重和排序依据。

### 风险 5：数据不足导致复杂模型不稳定

规避：

- 先规则版。
- 再 BKT。
- 最后 DKT/SAKT/AKT。

---

## 10. 最小可交付版本

最小版本只做以下内容即可形成闭环：

1. 多轮对话：
   - 最近历史 + standalone question rewrite。
   - session summary 可先简化。

2. 学生画像：
   - `StudentConceptMastery`。
   - 规则版 mastery score。

3. 推荐：
   - 固定权重评分器。
   - 返回推荐理由和证据信号。

4. 学习路径：
   - 教学图谱投影。
   - 低掌握知识点 + 先修拓扑排序。

5. 实验：
   - 多轮 query 对比。
   - 推荐 Precision@K / nDCG@K。
   - 学习路径 prerequisite violation count。

---

## 11. 后续算法候选

### 多轮对话

- Prompt-based standalone question rewriting。
- Conversational Query Rewriting。
- CONQRR-style retrieval reward query rewriting。
- Session summary + semantic memory retrieval。

### 学生画像

- Rule-based mastery estimation。
- Bayesian Knowledge Tracing。
- Deep Knowledge Tracing。
- Self-attentive Knowledge Tracing。

### 推荐

- Explainable weighted rules。
- Knowledge graph path-based recommendation。
- Personalized PageRank。
- Learning-to-rank。

### 学习路径

- Topological sorting with constraints。
- Beam Search。
- Personalized PageRank + path search。
- Ant Colony Optimization / Particle Swarm Optimization，作为论文对比算法可选，不建议第一版落地。
