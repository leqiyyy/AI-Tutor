# 基于大语言模型和检索增强生成的助教平台设计

## 第一章 绪论

### 1.1 研究背景与问题提出

随着高校信息化教学不断推进，课程资料的数字化积累速度显著加快。围绕一门课程，教师通常会持续产生和维护讲义、课件、实验指导书、课堂通知、作业要求、讨论记录、历年答疑材料等多种类型的教学资源。对学生而言，这些资源本身具有较高的学习价值，但在实际使用过程中往往呈现出来源分散、格式异构、组织粒度不统一等特点：部分资料以 PDF、Word 或 PPT 形式保存，部分资料以图片、表格或网页文本形式存在，部分课程经验又沉淀在教师的临时答疑和群组交流之中。资源规模的增长并不天然等同于资源利用效率的提升，学生在复习或完成作业时，依然需要花费较多时间在多份资料之间检索、比对和定位知识点，导致课程知识服务能力与资源积累规模之间存在明显落差。对此，可在正文中配合 `图1-1 课程教学资源分散问题示意图` 展示课程资料在不同载体上的离散状态。

除资源利用效率不足之外，传统课程答疑方式还普遍面临教师重复劳动较重、个性化支持成本较高等现实问题。在课程教学过程中，大量问题具有明显的重复性，例如概念辨析、公式使用条件、实验步骤理解、作业要求确认等，教师需要反复作答。与此同时，不同学生在先修基础、理解节奏、关注重点和学习习惯方面存在差异，统一的课堂讲解难以覆盖全部个体需求，而纯粹依靠教师进行一对一补充辅导又会带来较高的时间成本与组织压力。尤其在课程规模较大、教学周期较长的情况下，传统答疑机制容易出现响应不及时、知识沉淀不足、优质答复难以复用等问题。围绕这一现象，可在后续排版时设置 `表1-1 传统课程答疑方式存在的问题`，从响应效率、知识复用、个性化支持和持续优化能力等方面进行归纳。

近年来，以大语言模型为代表的生成式人工智能技术在自然语言理解、对话交互、知识总结和内容生成等方面表现出较强能力，为构建智能教学辅助系统提供了新的技术路径。大语言模型能够以自然语言形式与学生交互，支持问题理解、内容归纳、学习建议生成和多轮上下文跟踪，从而在教学支持场景中展现出良好的应用前景。然而，通用大语言模型若直接用于具体课程问答，也存在较为突出的局限性。首先，模型回答往往依赖预训练阶段的通用知识，难以自动与具体课程资料严格对齐，容易出现脱离教学边界的泛化回答。其次，模型给出的答案缺少清晰可追溯的课程证据支持，在教学场景中不利于建立可信性。再次，课程中常见的术语表述、知识组织方式和教师个性化要求具有明显领域特征，通用模型未必能够稳定适配。最后，当回答质量不足或出现偏差时，如果系统缺少教师审核、纠错和知识回流机制，就难以形成可持续优化的闭环。因此，本文的研究目标并非构建一个泛化的对话机器人，而是面向具体课程知识边界，设计一个具备资料约束、证据支撑、反馈纠偏和持续更新能力的智能助教平台。

在此背景下，检索增强生成技术为课程场景中的大模型落地提供了重要思路。RAG 通过将外部知识检索与生成过程结合，使模型能够基于课程资料、结构化知识和历史审核结果来组织答案，从而缓解纯生成模式下的幻觉问题，增强回答的针对性与可解释性。与此同时，现代课程资源已经不再局限于纯文本，课件截图、公式、图表、实验图片、扫描文档等多模态内容在教学中占据重要比例，单一文本检索机制难以充分覆盖真实教学资料。香港大学提出的 RAG-Anything 在 LightRAG 基础上扩展了多模态解析与跨模态知识组织能力，为课程资源的统一理解与检索提供了更具拓展性的技术框架。基于上述背景，本文围绕“课程知识服务”这一核心任务，设计并实现一个基于大语言模型和检索增强生成的 AI 助教平台，以期提升课程资料利用效率、减轻教师重复答疑压力，并为学生提供更具针对性的学习支持。

### 1.2 国内外研究现状

#### 1.2.1 大语言模型在教育场景中的应用研究

大语言模型的发展显著推动了智能教育应用研究的深化。当前相关研究主要集中在智能问答、教学内容生成、辅助写作、学习陪伴和教师教学支持等方向。一方面，大语言模型能够根据学生输入的自然语言问题生成较为流畅的解释性回答，用于承担基础问答、知识点释义和学习建议提供等任务；另一方面，模型在文本生成方面的能力也使其能够参与习题解析、课程总结、学习提纲生成、教案辅助编写等环节。在教育应用中，研究重点已经由“模型能否回答”逐步转向“模型能否在特定教学边界内回答得更准确、更可信、更可用”。

从已有成果来看，大语言模型在教育场景中具有交互自然、任务适应性强、输出形式灵活等优势，尤其适合承担高频、碎片化和语言密集型教学支持任务。但同时，教育场景对回答准确性、课程适配性和内容可核验性具有更高要求，单纯依赖模型参数记忆往往难以满足课堂教学的严谨性需求。由此可见，大语言模型在教育领域的应用价值已经得到广泛关注，但其真正进入具体课程教学支持环节，仍然需要与课程知识约束、教师审核机制以及教学数据闭环进一步结合。

#### 1.2.2 基于RAG的课程问答与知识服务研究

检索增强生成技术通过“外部知识检索 + 证据拼接 + 生成回答”的处理链路，有效降低了大语言模型在专业场景中出现事实偏移的概率，因此已成为当前知识问答系统的重要技术方向。在课程场景中，RAG 的意义尤其突出。课程问答面对的知识来源通常具有明确边界，例如教材内容、课件资料、实验指导、教师公告和课程术语规范等，这些内容并不适合完全依赖模型内部参数进行泛化推断，而应通过外部知识检索进行约束。

国内外已有研究普遍表明，RAG 技术能够在专业问答、企业知识助手、教育资源服务等场景中提升回答的相关性与可解释性。一些研究开始将课程资料切分为知识片段，通过向量化检索、关键词匹配或混合检索方式为回答提供证据基础。但从课程场景的实际需求来看，现有研究仍存在若干不足：一是许多系统停留在文本片段级检索，缺少对课程概念关联、上下位关系和知识脉络的组织；二是对于教师答疑纠错和知识补充的利用不足，回答质量难以通过教学反馈持续提升；三是面对图表、公式和扫描课件等复杂资料时，单纯文本 RAG 的适配能力有限。因此，如何在课程资料边界下构建更稳定、更可信且可持续更新的 RAG 问答链路，仍是值得深入研究的问题。

#### 1.2.3 多模态教学资源解析与知识组织研究

随着数字课程资源形态的不断丰富，教学资料的处理对象已从传统文本扩展到图片、表格、公式、录音、视频和扫描文档等多种模态。围绕多模态教学资源处理，已有研究常从 OCR、版面分析、语音识别、关键帧抽取、结构化标注和跨模态表示学习等方向展开。对于课件和讲义类资料，研究重点通常放在文本提取、表格识别、公式检测和图片区域分析等方面；对于音视频类资料，则更强调语音转写、时间片段切分和内容摘要组织。

然而，从教学系统落地角度看，多模态解析并不只是将非文本内容“转成文本”这么简单，更关键的问题在于如何让这些内容进入后续问答与知识服务流程。若图像中的流程图、表格中的参数信息、课件中的公式表达无法被结构化保存并参与检索，那么多模态解析对教学问答的帮助仍然有限。当前不少系统已具备基础的多模态处理能力，但在跨模态知识融合、统一检索和答案证据组织方面仍处于持续演进阶段。因而，多模态教学资源的统一解析与知识化组织，已成为构建高质量 AI 助教平台的重要研究方向。

#### 1.2.4 学习分析与个性化支持研究

学习分析研究关注如何通过记录和分析学习行为数据，辅助理解学生的学习状态，并进一步支持教学决策与个性化服务。围绕学习分析，已有研究普遍涉及学习日志记录、学习画像构建、学习进度跟踪、薄弱知识识别、资源推荐、错题管理与复习支持等内容。随着在线教学平台与智能问答系统的发展，学生在提问、作业提交、错题订正、复习回顾等环节会持续产生可分析的数据，为构建更细粒度的学习支持机制创造了条件。

从系统实现角度看，学习分析并不是独立于问答系统之外的附属功能，而应与问答日志、错误反馈、知识点追踪和学习资源供给形成联动关系。若系统能够基于学生的提问主题、答疑反馈、错题记录和复习行为形成学生画像，就有可能进一步提供更具针对性的学习建议、复习闪卡和资料推荐。这也意味着，AI 助教平台不应只停留在“回答问题”，还应延伸至“支持学习过程”的层面。现有研究虽已在学习分析和个性化推荐方面取得一定进展，但在与课程问答、教师审核和课程知识库的深度融合方面仍有提升空间。

#### 1.2.5 智能对话系统与AI助教研究

智能对话系统的发展为 AI 助教研究提供了直接技术基础。传统对话系统多依赖规则、模板或有限领域知识库，在灵活性和扩展性方面存在不足；而结合大语言模型后，AI 助教在语义理解、多轮交互和自然语言表达上有了明显提升。目前，智能助教研究通常围绕课程答疑、作业辅导、学习提醒、教学资源推荐和教师辅助支持等目标展开，期望构建能够长期陪伴学习过程的智能教学支持主体。

尽管如此，当前 AI 助教系统普遍仍面临三方面挑战。其一，系统虽具备自然对话能力，但课程边界控制不足，容易输出“看似合理但缺乏课程依据”的答案。其二，教师在答疑纠偏中的作用尚未被充分制度化，很多系统缺少将教师修正答案回流到知识库的机制。其三，问答、资源管理、学习分析和管理端配置常被拆分为孤立功能，缺少围绕具体课程建立的一体化平台设计。因此，如何构建兼顾对话能力、知识约束、教师协同和学习支持的 AI 助教平台，是当前研究进一步深化的重要方向。

#### 1.2.6 现有研究的不足与本文切入点

综合以上研究可以看出，国内外在大语言模型教育应用、RAG 问答、多模态资料处理、学习分析和智能助教等方面均已积累较为丰富的成果，但在面向课程真实教学边界的系统化融合上仍存在明显不足。第一，现有系统中不少问答能力主要建立在通用语义检索或大模型生成能力之上，对具体课程知识边界约束不够，难以保证回答稳定贴合课程资料。第二，教师审核、纠偏和知识回流机制在多数方案中仍然不足，系统缺少借助教学反馈实现持续优化的闭环。第三，多模态资料虽然受到重视，但真正能够将图片、表格、公式等内容与文本知识统一组织并参与问答检索的系统仍相对有限。第四，问答、学习分析、复习支持与平台管理常常被分散实现，缺少围绕学生、教师和管理员三类角色协同工作的整体性平台设计。

基于上述不足，本文选择以具体课程知识服务为切入点，围绕“课程资料入库、RAG 问答、教师审核回流、学习分析支持”构建一体化 AI 助教平台。系统在知识引擎层面采用“增强型 RAG-Anything 路径 + 本地回退引擎”的双路径设计：一方面引入基于 LightRAG 扩展的 RAG-Anything 作为多模态增强知识引擎，以提升复杂资料场景下的知识组织与检索能力；另一方面保留本地回退引擎，确保在依赖环境或增强路径不稳定时仍能维持核心问答服务。与此同时，系统还引入教师审核与知识回流机制，使低置信度或被用户否定的回答能够进入人工修正和知识补充流程，从而形成持续迭代优化的教学辅助闭环。可在此处设置 `图1-2 现有研究不足与本文切入点示意图` 以及 `表1-2 国内外研究现状对比表`，直观展示本文的创新关注点。

### 1.3 研究目标与主要内容

#### 1.3.1 研究目标

本文面向高校课程教学辅助场景，旨在设计并实现一个基于大语言模型和检索增强生成的 AI 助教平台，使其能够在真实课程边界下为学生和教师提供更加高效、可信和可持续优化的教学支持服务。具体而言，本文的研究目标主要体现在以下几个方面。

第一，围绕课程资料构建统一的课程知识库。系统需要支持多种课程资料的上传、存储、解析和组织，使分散的教学资源能够转化为可检索、可追踪、可更新的知识基础。第二，构建基于课程资料约束的问答能力，使大语言模型生成的回答能够尽量建立在课程文档、知识片段和相关证据之上，提升回答的相关性与可信度。第三，建立教师审核与知识回流机制，使系统能够将低置信度答案、用户差评反馈和教师修正内容纳入统一闭环，为后续回答质量提升提供依据。第四，结合学习行为记录与分析，提供错题管理、闪卡复习、学习画像和资料推荐等支持功能，推动平台由“问答工具”进一步发展为“学习支持平台”。第五，通过原型系统开发、功能测试和运行验证，评估所设计方案在课程场景中的可行性和实用价值。

#### 1.3.2 主要研究内容

围绕上述目标，本文主要开展以下几个方面的研究与实现工作。

其一，对高校课程场景下 AI 助教平台的用户角色、业务流程和核心需求进行分析，明确学生、教师和管理员三类角色在课程学习、资源管理、教学审核和系统配置中的功能边界。其二，设计平台总体架构，构建前后端分离的系统原型，并形成涵盖课程管理、资料管理、智能问答、审核回流和学习分析等模块的整体方案。其三，围绕课程资料上传与知识库构建过程，研究资料解析、结构化组织、检索索引构建和课程知识关联组织方法，为后续问答提供知识基础。其四，设计基于 RAG 的课程问答链路，研究问题输入、上下文组织、知识检索、候选证据生成、重排序、答案生成和来源引用等关键环节，并探索将 RAG-Anything 作为增强型知识引擎接入系统。其五，设计教师审核与知识回流机制，使教师能够对系统回答进行修正，并将优质修正内容补充回知识库，支持后续问答的动态改进。其六，设计学习分析与个性化支持功能，结合问答日志、错题记录和复习行为，为学生提供学习报告、复习支持和推荐辅助。其七，通过系统测试和典型案例分析验证平台主流程的可运行性，并结合后续用户评估设计分析系统的实用潜力。

为增强章节结构的可视化表达，可在本节后续排版中加入 `图1-3 本文研究目标与研究内容关系图`，展示研究目标与各模块之间的对应关系。

### 1.4 研究方法与技术路线

#### 1.4.1 研究方法

为保证本文研究既具备理论依据，又能够与系统实现紧密结合，本文综合采用文献研究法、系统分析法、原型实现法和实验测试法开展研究。

文献研究法主要用于梳理大语言模型、RAG、知识图谱、多模态解析、学习分析和 AI 助教等方向的研究进展，在此基础上明确课程场景中智能助教平台设计的现实需求与技术切入点。系统分析法主要用于从真实教学业务出发，分析用户角色、功能模块、数据流和处理链路之间的关系，从而形成能够支撑课程场景的总体架构设计。原型实现法主要体现在系统开发过程中，通过构建前后端分离的原型系统，将课程资料管理、知识库构建、智能问答、教师审核回流和学习分析等模块逐步落地，并据此验证技术方案的可行性。实验测试法主要用于通过功能测试、典型案例分析和运行结果观察，对系统主流程的稳定性、闭环性和应用效果进行验证，为后续性能分析与实用性评估提供依据。

#### 1.4.2 技术路线

本文的技术路线围绕“需求分析 - 架构设计 - 知识构建 - 问答实现 - 审核回流 - 学习支持 - 测试验证”的主线展开。

首先，通过课程教学场景调研与功能需求分析，明确系统服务对象、核心业务和非功能需求，形成平台总体设计目标。其次，在系统架构层面，采用前后端分离设计，前端面向学生、教师和管理员提供不同界面入口，后端基于模块化服务组织课程管理、知识库、对话、审核、分析和管理配置等核心能力。再次，在知识处理层面，系统通过课程资料上传、文件存储、解析任务创建、内容结构化组织和索引构建形成课程知识基础，并在此过程中引入课程知识关联结构，为检索与证据组织提供辅助支撑。

在智能问答实现方面，系统以大语言模型为生成核心，以课程资料检索为知识约束基础，构建包含查询理解、候选证据召回、重排序、上下文组织和答案生成的 RAG 问答链路。为提升复杂资料场景的适应能力，系统预留并集成基于 LightRAG 扩展的 RAG-Anything 增强路径，同时保留本地回退引擎以保证核心服务稳定运行。在回答质量优化方面，系统对低置信度回答和用户负反馈进行记录，触发教师审核流程，并将教师修正结果回流到知识库，形成持续更新机制。进一步地，系统将问答日志、错题、复习行为和课程活动数据纳入学习分析过程，为学生画像构建、学习报告生成和规则驱动推荐提供基础。最后，通过系统测试与案例分析对平台关键流程进行验证。上述技术路线可在后续章节中通过 `图1-4 技术路线图` 进行集中展示。

### 1.5 论文组织结构

本文围绕基于大语言模型和检索增强生成的 AI 助教平台设计与实现展开，共分为六章，各章内容安排如下。

第一章为绪论，主要阐述本文的研究背景、国内外研究现状、研究目标与主要内容、研究方法与技术路线，以及全文的组织结构。第二章介绍本文涉及的相关理论与关键技术，重点说明大语言模型、RAG、基于 LightRAG 扩展的 RAG-Anything、多模态资料解析、课程知识图谱原型、学习分析以及前后端分离架构等技术在本系统中的作用。第三章从需求分析与总体设计角度出发，分析用户角色、功能需求、非功能需求、总体架构、核心业务流程以及数据库与接口设计。第四章围绕系统关键模块的设计与实现展开，重点论述课程资源管理与知识库构建、检索增强问答、教师审核与知识回流、学习分析与个性化支持、前后端交互与可视化以及多模态扩展能力等核心内容。第五章对系统测试与结果进行分析，包括实验环境、功能测试、典型案例、主流程运行情况、用户评估设计和系统局限性分析。第六章对全文工作进行总结，并从多模态能力深化、知识图谱完善、检索与重排序增强、离线评测与用户研究、模型性能优化等角度展望未来工作。

为便于论文定稿阶段统一图表编号和章节映射，可在本节中补充 `图1-5 论文组织结构图` 和 `表1-3 论文各章节内容安排表`。

## 第二章 相关理论与关键技术

### 2.1 大语言模型与提示式问答技术

#### 2.1.1 大语言模型的基本原理与能力特点

大语言模型是当前生成式人工智能体系中的核心基础技术，其本质是在大规模语料上通过自监督学习获得语言表示能力，并在此基础上完成自然语言理解、生成、归纳、改写、问答和推理等任务。当前主流大语言模型大多建立在 Transformer 架构之上。Transformer 通过自注意力机制建模序列内部各位置之间的依赖关系，相比传统循环神经网络能够更高效地处理长距离上下文，并具备更好的并行计算能力。对于问答系统而言，这种结构使模型能够同时关注用户问题、系统提示词、检索到的课程证据以及历史对话上下文，从而形成更连贯的响应结果。

从训练机制看，大语言模型通常先经过大规模预训练，使其学习语言统计规律、常见知识表达方式与基本语义结构；随后通过指令微调、偏好对齐等方式增强任务服从能力和对话可用性。在教育问答场景中，模型的价值并不只体现在“知道很多”，更重要的是能够理解问题意图、识别知识点范围、根据约束生成具有解释性的回答，并适应不同任务形式，例如知识点查询、概念解释、步骤说明、总结提炼、学习建议生成和复习材料组织等。

与此同时，大语言模型具有若干对教育场景尤为关键的能力特点。第一，模型具备较强的自然语言交互能力，能够以接近教师答疑的方式进行解释与引导。第二，模型能够处理非结构化输入，将零散问题重组为较清晰的语义表达。第三，模型具备较强的生成灵活性，可以根据提示词要求输出简答、分步解析、提纲、表格或结构化结果。第四，随着上下文窗口能力增强，大语言模型能够在一次生成过程中同时吸收较多课程证据，适合与检索系统结合使用。也正因如此，大语言模型成为构建 AI 助教平台的生成核心。

#### 2.1.2 提示工程与任务型问答机制

大语言模型虽然具备通用语言能力，但具体输出质量在很大程度上依赖提示工程设计。提示工程的核心任务，是通过系统提示、任务描述、角色设定、输出约束和上下文组织等方式，引导模型在给定场景下生成更符合目标的结果。在课程问答系统中，提示词通常不再只是一个简单问题，而是由多部分共同构成的任务输入，包括系统角色约束、课程场景要求、用户问题、历史对话、外部检索证据以及回答格式说明等。

从处理机制上看，提示式问答本质上是将具体任务“编译”为模型可理解的输入上下文。例如，在课程答疑场景中，系统会要求模型优先依据课程资料作答；在知识点查询场景中，系统会要求模型给出定义、适用条件和关键区别；在学习支持场景中，系统又可能要求模型输出复习建议、错题总结或闪卡内容。不同提示结构会直接影响模型的回答风格、证据依赖程度、信息组织方式和语言严谨性。因此，提示词设计是连接“模型能力”和“教学需求”的关键桥梁。

在工程实现层面，大语言模型的调用方式主要包括本地部署与远程 API 调用两类。本地部署模式通常将模型推理服务部署在本地服务器或专用算力环境中，再通过统一接口向业务系统提供服务，其优势在于可控性较强、数据边界更清晰、可针对特定场景进行资源调优；不足在于部署和维护成本较高，对硬件资源要求也更强。远程 API 调用模式则通过云端或第三方模型服务提供商访问模型能力，优势在于接入灵活、模型更新快、工程门槛相对较低，但也会受到网络延迟、成本和外部服务稳定性的影响。本文系统在模型接入层采用可切换设计，既支持基于远程 API 的调用方式，也保留面向本地 OpenAI 兼容接口的接入能力，从而兼顾原型验证的便利性与后续独立部署的可扩展性。

此外，当前主流模型服务已不局限于单一调用协议，一些模型采用传统的 Chat Completions 风格接口，另一些则采用更适合多轮输入与工具扩展的 Responses 风格接口。对系统而言，这意味着业务层不应将某一种固定 API 形式写死，而应通过抽象适配的方式统一封装不同协议。本文系统在模型适配层引入了这一思想，将生成模型、嵌入模型和视觉模型的调用统一封装在可切换的接口之下，为后续多模型协同和不同部署模式的切换提供了实现基础。

#### 2.1.3 大语言模型在教育问答中的优势与局限

将大语言模型应用于教育问答，具有明显的现实优势。首先，模型能够以自然语言直接与学生互动，降低了传统检索系统“会查但不会讲”的使用门槛。其次，模型适合生成解释性内容，能够将课程知识转化为更易理解的语言表达，这对概念辨析、步骤解析和学习引导尤为重要。再次，模型能够承担多种教学辅助任务，不仅可以回答问题，还能生成提纲、总结、复习卡片和学习建议，从而提升系统的综合服务能力。

但从教学严谨性出发，大语言模型的局限同样不容忽视。其一，模型存在幻觉风险，即可能生成语法流畅却事实错误或脱离课程边界的内容。其二，模型内部知识难以与具体课程资料严格绑定，因而在课程专业术语、教师特定要求和最新课程安排等方面容易出现偏差。其三，若没有证据引用与审核机制，模型回答在教学场景中的可信性较难保证。其四，模型虽然强于语言生成，但对图表、公式、扫描版面等复杂信息的理解往往还需要视觉语言模型和文档解析系统的协同支持。

因此，在教育场景下，大语言模型更适合作为生成中枢，而不是孤立运行的“万能教师”。只有将其与课程知识检索、提示约束、教师审核和学习反馈机制结合，才能发挥其优势并控制其风险。本文系统正是在这一思路下，将大语言模型置于“检索增强 + 证据约束 + 人工纠偏”的整体框架之中。

### 2.2 检索增强生成技术

#### 2.2.1 RAG基本原理

检索增强生成（Retrieval-Augmented Generation, RAG）是一种将外部知识检索与大语言模型生成过程结合的技术范式。其基本思想是：当用户提出问题时，系统不直接完全依赖模型参数中的隐式知识进行回答，而是先从知识库中检索与问题相关的证据，再将这些证据与问题共同输入大语言模型，由模型基于外部知识生成回答。由此，模型回答从“只凭记忆作答”转变为“基于证据作答”，从而提高结果的相关性、可解释性和可更新性。

从理论上看，RAG 的核心价值在于将知识存储与语言生成解耦。传统大语言模型虽然拥有强大的语言表达能力，但其知识更新依赖再训练或参数调整，更新成本较高，且难以精确控制知识边界。RAG 则通过引入外部知识库，使系统可以通过新增资料、更新索引或调整检索策略来实现知识更新，而无需频繁改动生成模型本身。这种“模型负责表达，知识库负责供证”的分工方式，尤其适合教学、医疗、法律、企业知识服务等对知识边界和可信性要求较高的场景。

#### 2.2.2 知识检索、证据拼接与生成流程

RAG 的处理流程通常包含知识准备、问题表示、候选召回、结果重排序、上下文组装和答案生成等关键阶段。首先，在知识准备阶段，系统需要对原始文档进行清洗、切分、索引和元数据组织，使课程资料能够转化为可检索对象。常见处理方式包括文本分块、向量嵌入、关键词抽取、结构化标签标注以及文档来源标记等。其次，在问题输入阶段，系统会对用户问题进行语义表示，有时还会进一步进行查询改写，以提高检索覆盖率和召回质量。

在候选召回阶段，系统可采用关键词检索、向量检索或混合检索等方式。关键词检索擅长保留原词匹配，对术语、公式符号和专有名词敏感；向量检索则更适合发现语义相近但表面措辞不同的内容；混合检索则试图综合两者优点。召回得到的候选证据往往仍包含噪声，因此还需要经过重排序环节，以提高最终进入生成上下文的证据质量。重排序可以基于规则、传统排序模型或神经重排序模型完成，其目标是让最能支撑回答的问题相关片段优先进入生成阶段。

在证据拼接与生成阶段，系统会将问题、候选证据、来源信息和任务约束一起组织为模型输入上下文，要求大语言模型依据证据生成答案。高质量的 RAG 系统不仅要输出答案本身，还应尽量保留来源引用、页码、文档名或知识条目等可追溯信息，以便教师和学生核验。对于课程场景而言，证据链的存在比“回答看起来很像对的”更重要，因为教学系统的核心不只是生成内容，更是生成可核验、可追踪、可用于教学反馈的内容。

#### 2.2.3 RAG在课程问答场景中的适用性

课程问答天然适合采用 RAG 技术。首先，课程知识来源相对明确，包括教材、课件、实验指导、教师公告、历史答疑和课程术语规范等，这些内容具有清晰的边界，便于构建课程知识库。其次，学生问题具有高频和重复特点，很多问题都可以在已有课程资料中找到相应依据，适合通过检索进行支持。再次，课程内容会随着教学进度、作业安排和教师补充说明不断变化，RAG 的外部知识更新方式比单纯依赖模型参数更灵活。

此外，课程场景对可解释性要求较高。学生往往不仅需要“答案是什么”，还希望知道“为什么”“依据在哪”“和课堂讲法是否一致”。RAG 通过证据引用和来源追踪，使系统回答更容易被教师接受，也更利于后续教学纠偏。对于本文系统而言，RAG 并不是一个附加功能，而是保证课程问答可信性与可更新性的核心机制。系统在实现中进一步结合查询改写、图辅助检索、重排序和教师回流，形成了更适合课程场景的增强型问答链路。

### 2.3 基于LightRAG扩展的RAG-Anything多模态知识引擎

#### 2.3.1 LightRAG的基本架构与特点

LightRAG 是一种强调轻量化图增强检索思想的 RAG 框架，其核心思路并非只把文档看作孤立文本块，而是进一步关注文本片段之间的实体、主题和语义关联。与单纯的向量召回相比，LightRAG 更强调利用知识关联结构辅助检索，使系统在面对复杂问题时不只是寻找“最相似的几段文本”，还能够在概念关联、上下位关系和多跳语义连接中组织证据。对于课程问答而言，这种思路有助于改善纯文本检索容易出现的“局部命中但整体脉络不足”问题。

从检索组织方式看，LightRAG 提供了更细粒度的查询视角，可以从局部知识点与全局主题两个层面组织问题理解与证据召回。换言之，系统既关注具体术语、局部事实和细粒度证据，也关注更高层次的章节主题、概念网络和问题背景，从而在复杂问答中实现由点到面的知识组织。这种双层视角对于课程知识服务尤为重要，因为学生问题既可能是“某个公式的含义是什么”，也可能是“某一章的核心知识脉络如何理解”。此外，LightRAG 还支持多种查询模式，例如面向局部的检索、面向全局的检索以及混合模式，为不同问题类型匹配不同检索策略提供了基础。

#### 2.3.2 RAG-Anything的多模态扩展能力

RAG-Anything 在 LightRAG 的基础上进一步面向复杂文档和多模态知识场景进行了扩展，其目标不再局限于纯文本问答，而是尝试让图像、表格、公式、版面结构等内容也能够进入统一的知识处理与检索过程。与传统 RAG 将文档先粗略转为纯文本再检索的做法相比，RAG-Anything 更强调“文档内容保真解析 + 跨模态统一组织 + 混合检索”。这意味着系统不只是提取文字，还会尽可能保留文档中的结构性线索，例如表格关系、公式区域、图像说明和版面布局。

从理论上理解，RAG-Anything 的增强价值主要体现在两个方面。第一，它强化了复杂文档解析能力，通过文档解析器和多模态表示方式将原始资料中的文本、表格、公式和图像区域转化为可组织、可索引的知识对象。以 MinerU 一类复杂文档解析工具为例，其重点并不只是 OCR 识别文字，还包括版面分析、阅读顺序恢复、表格抽取、公式识别以及结构化输出，这为后续知识检索保留了更丰富的原始线索。第二，它强化了跨模态检索能力，使系统在需要时能够结合视觉语言模型理解图片内容，或者将图像中的文字与图示语义共同纳入问答证据链。

从框架设计角度看，RAG-Anything 可以理解为在图增强检索基础上进一步引入跨模态关联组织机制。若将其面向课程文档的处理链路抽象化，可将其视为同时维护“文本语义与知识关联”以及“多模态内容之间的对应关系”两类组织结构：前者负责承载文本实体、概念和上下文关系，后者负责连接图像、表格、公式等非纯文本内容与相应语义线索。这种双重组织思想，使多模态资料不再只是附属附件，而能够作为检索与生成的有效证据来源。

视觉语言模型（Vision-Language Model, VLM）在这一过程中承担重要作用。VLM 能够联合处理图像与文本输入，将图像中的视觉元素映射为可参与问答的语义表示。在教学场景中，这意味着系统有机会理解截图中的题目、流程图中的步骤、图表中的趋势信息或实验图片中的关键对象。对于依赖图文混合资料的课程而言，VLM 是多模态 RAG 链路中连接“视觉内容”和“语言回答”的关键组件。

#### 2.3.3 RAG-Anything在本系统中的集成方式

本文系统采用“增强型多模态知识引擎 + 本地回退引擎”的分层集成思路，将 RAG-Anything 作为增强路径接入，并保留本地简化 RAG 引擎以保障核心功能稳定。这样设计的原因在于：RAG-Anything 具备更强的复杂文档解析与多模态检索潜力，适合承担高质量增强检索任务；但其运行依赖文档解析器、嵌入模型、生成模型和视觉模型等多种组件，工程化部署门槛更高，因此在原型系统阶段仍需要稳定的回退路径保证业务可用性。

在本系统实现中，增强路径支持配置解析器类型、解析方式和查询模式，并允许在生成模型、嵌入模型和视觉模型三个维度分别接入不同后端。也就是说，系统并不强制生成、嵌入和视觉理解由同一模型提供，而是通过统一的模型路由机制完成多模型协同。对于增强路径无法正常工作、依赖未就绪或返回结果为空的情况，系统将自动切换到本地回退检索链路，从而避免问答主流程完全失效。由此可见，RAG-Anything 在本文系统中既是理论上的多模态增强方向，也是工程上的可插拔知识引擎路径。

### 2.4 多模态文档解析与结构化存储技术

#### 2.4.1 多模态教学资料的处理需求

课程资源的真实形态决定了 AI 助教平台不能只围绕纯文本设计。高校课程资料除了教材摘要和文字讲义之外，还广泛包含课件截图、流程图、实验图片、表格数据、公式推导页、扫描版讲义，甚至还会扩展到录音、录播视频和演示录像等内容。如果系统只能处理文本段落，那么大量关键信息实际上会被遗漏。例如，一张实验流程图中的箭头关系、一个表格中的参数对比、一页课件上的公式结构或一张扫描图片中的题目条件，都可能是学生提问时所需的重要依据。

因此，多模态处理并非“功能堆叠”，而是课程知识服务的现实需求。一个面向教学场景的智能助教平台，至少应具备识别复杂文档结构、保留关键版面信息、抽取图表与公式内容，并将这些内容转化为后续检索可用对象的能力。只有如此，系统才可能真正覆盖课程资料的主流形态，而不是仅服务于少量纯文本材料。

#### 2.4.2 文本、音频、视频资料的抽取与组织方法

多模态资料处理的基础在于“抽取”，但真正有效的关键在于“组织”。对于文本型文档，系统通常需要进行编码识别、文本清洗、段落切分、关键词抽取与元数据标注。对于扫描件、图片和版面复杂的 PDF，常用技术包括 OCR、版面分析、阅读顺序恢复、表格识别与公式抽取。其中，OCR 的作用是将图像中的字符转化为机器可处理文本；版面分析则用于识别标题、正文、表格、图片区域和阅读顺序；公式与表格抽取进一步保证文档中的结构化知识不会在纯文本化过程中被丢失。

在复杂文档解析工具中，MinerU 具有较强代表性。该类工具强调从 PDF、扫描件和课件中恢复文档结构，不仅提取文字，还会尽可能输出版面层次、表格、公式和结构化内容，以 Markdown 或 JSON 等形式为后续处理提供统一输入。这种解析思路非常适合课程资料场景，因为教学文档中常见的表格、列表、公式与图文混排信息，若缺少结构恢复，很容易在后续检索和生成中失真。

对于图像资料，除 OCR 之外，还需要借助视觉语言模型对图像整体语义进行描述，从而弥补“图中有意义但未必有可识别文本”的问题。例如，流程图、函数曲线图、结构图和实验示意图中，大量有效信息并非简单字符，而是图形关系与视觉布局。VLM 的作用正是在这一环节将视觉线索转换为可参与检索和生成的语义信息。

对于音频和视频资料，常用处理路径包括自动语音识别、时间片段切分、关键帧抽取和片段级摘要组织。音频内容可以通过 ASR 转写为文本，再结合时间戳建立可回溯索引；视频内容则通常需要联合语音文本、关键帧图像与片段描述进行组织。需要指出的是，本文系统当前已经实现了文档和图片场景下的基础多模态支持，而音视频的深度理解链路更多作为扩展方向保留在系统设计之中。因此，在论文表达上应强调“多模态扩展能力和处理思路已明确，当前重点实现集中在文档与图像场景”。

#### 2.4.3 结构化存储对后续问答与检索的支撑作用

多模态内容若仅停留在“解析成功”层面，其价值仍然有限；只有进入结构化存储后，才能真正支撑后续问答、检索与证据输出。结构化存储的核心目标，是将解析结果按统一模式组织为文本片段、内容项、来源信息、页码信息、模态类型、位置信息和质量标签等数据，使系统能够按需检索和回溯。对于课程问答系统而言，结构化存储不仅服务于“找到内容”，更服务于“知道内容来自哪里、属于什么类型、能否作为证据使用”。

在本文系统中，结构化存储思想体现在两个层面。一方面，原始资料可以保存在本地文件系统或对象存储中，保证资料文件本身的留存与管理；另一方面，解析结果会以数据库记录和结构化字段形式保存，为知识库构建、问答检索、图谱组织和质量诊断提供支持。特别是当文档解析结果中包含表格、公式、图片说明和质量标记时，结构化存储能够为多模态检索与结果引用提供统一数据基础。因此，结构化存储是连接“资料解析”和“智能问答”的中间枢纽，也是本系统后续知识处理链路得以成立的重要前提。可在本节后续配合 `图2-4 多模态资料结构化处理示意图` 展示该过程。

### 2.5 知识图谱与知识关联组织技术

#### 2.5.1 知识图谱基本概念

知识图谱是一种以图结构组织知识的表示方式，通常以实体作为节点、以关系作为边，将概念、属性与实体间关系组织为可计算的知识网络。与传统文档集合相比，知识图谱更强调知识点之间的显式关联，能够支持关系查询、语义扩展、路径发现和结构化推理。对于课程场景而言，知识图谱并不一定要追求百科级规模，更重要的是围绕课程知识边界建立概念之间的关联结构，使系统能够识别“某知识点与哪些内容相关”“某问题涉及哪些先修概念”以及“不同资料中的同类知识如何连接”等问题。

#### 2.5.2 课程知识点实体与关系建模

在课程场景中，知识图谱的建模对象通常包括课程章节、知识点、术语、公式、实验步骤、作业主题和教学资源等。实体之间的关系则可能表现为包含、前置依赖、相关、举例说明、共同出现或来源于同一教学资料等。理想状态下，课程知识图谱应能够在概念层面呈现教学内容结构，并支持由文档片段反向定位知识点、由知识点回溯资料来源。

但从原型系统实现角度看，课程知识图谱并不一定一开始就以高度复杂的语义关系形式出现。在工程实践中，更可行的路径往往是先构建“课程知识关联结构”或“课程知识图谱原型”，逐步将文档关键词、核心实体和共现关系组织为图结构，再随着抽取能力增强逐步细化关系类型。本文系统即遵循这一思路，在知识库构建阶段提取课程相关关键词和知识项，形成以实体与关联边为核心的原型图结构，并在后续检索与可视化中发挥辅助作用。这种渐进式知识组织方式有利于在原型阶段兼顾实现可行性与后续演进空间。

#### 2.5.3 图谱辅助检索与证据组织机制

知识图谱在课程问答中的价值，不仅在于可视化展示课程知识结构，更在于为检索与证据组织提供辅助能力。首先，图结构能够帮助系统从单个术语扩展到相关知识点，从而缓解学生问题表达不完整、用词不一致带来的检索偏差。其次，图谱可用于组织证据脉络，使系统在回答概念关系、知识比较和章节总结类问题时，不只返回零散文本片段，而是结合相关节点与关系给出更完整的解释。再次，图谱还可以为后续推荐、复习支持和学习路径分析提供基础，例如识别学生反复提问的薄弱知识点及其关联内容。

需要强调的是，本文系统当前实现的是面向课程场景的知识图谱原型与知识关联结构，而非完全成熟的自动知识图谱构建系统。因此，在论文中宜将其定位为“图辅助组织与检索能力”，强调其在增强问答和知识服务中的实际作用，而不是夸大为完备的自动推理系统。可在本节配合 `图2-5 知识图谱辅助检索示意图` 展示图结构在证据组织中的辅助价值。

### 2.6 学习分析与个性化支持技术

#### 2.6.1 学习行为记录与学习画像构建

学习分析的基础是对学习过程数据的持续采集与组织。对于 AI 助教平台而言，学生的提问记录、问题主题、反馈评价、课程访问行为、作业相关互动以及复习操作等，都是反映学习状态的重要信号。通过对这些行为进行结构化记录，系统可以逐步形成对学生学习关注点、活跃程度、常见困难和偏好课程的整体刻画。

学习画像则是在行为数据基础上构建的学生抽象表示。它通常并不追求对学生能力的绝对评判，而更强调从过程数据中总结“当前学习特征”。例如，某学生在哪些课程中提问更频繁、哪些知识主题反复出现负反馈、哪些内容在复习中被多次回顾，这些都可以成为学习画像的重要组成部分。对于本文系统而言，学习画像为学情分析、错题整理、复习提醒和推荐支持提供了必要基础。

#### 2.6.2 错题本与闪卡复习机制

错题管理与闪卡复习是学习支持系统中较为成熟且有效的两类机制。错题本强调对学习中出现错误、模糊或高频困惑内容的持续沉淀，使学生能够围绕薄弱知识点进行针对性回顾；闪卡则通过“问题 - 答案”或“概念 - 解释”的短小结构强化主动回忆，适合知识点记忆和间隔复习。将这两类机制与 AI 助教平台结合，有助于把单次问答延伸为持续学习支持过程。

在课程问答场景下，系统可以根据学生提问、负反馈、审核记录或教师归纳结果提取出值得重复复习的知识点，并进一步生成复习卡片、总结条目或错题标签。这样一来，问答日志不再只是即时对话记录，而是能够沉淀为后续复习资源。对于本文系统而言，闪卡和错题机制正是连接“即时答疑”和“长期学习支持”的关键桥梁。

#### 2.6.3 资料推荐与学习支持思路

个性化支持并不一定意味着复杂的推荐模型，在原型系统阶段，更重要的是建立明确的推荐逻辑和可解释的支持机制。面向课程场景，推荐的依据可以来自学生近期问题主题、薄弱知识点、课程活跃度、反馈记录以及教师提供的重点资料。系统据此可以主动呈现更相关的学习材料、复习建议或课程提醒，帮助学生减少在大量资料中无目标浏览的成本。

从方法上看，推荐机制可以分为规则驱动和模型驱动两类。规则驱动方法实现简单、可解释性强，适合原型系统快速落地；模型驱动方法则有望在用户数据积累后进一步提升推荐精度。本文系统当前采用学习行为记录与规则驱动推荐相结合的方式，为学生提供个性化支持。该设计体现出一个重要思想：AI 助教平台不仅要回答学生“现在问的是什么”，还应逐步理解学生“接下来更需要什么”。

### 2.7 前后端分离与可扩展系统架构技术

#### 2.7.1 前后端分离架构

前后端分离是一种将用户界面层与业务服务层解耦的系统架构方式。其核心优势在于：前端可以专注于页面交互、状态展示和可视化体验，后端则集中处理业务逻辑、数据持久化、鉴权控制和 AI 能力编排。对于具备多角色、多页面和多业务链路的 AI 助教平台而言，前后端分离能够显著提高系统的模块清晰度和后续维护效率。

本文系统前端采用 React、Vite、TypeScript 和 Tailwind CSS 等技术构建交互界面，便于快速实现学生端、教师端和管理端的页面组织与组件复用；后端采用 FastAPI 构建服务接口，并配合 SQLAlchemy 完成数据持久化访问。这种组合具有开发效率较高、接口组织清晰、适合原型迭代和后续扩展等优点。对于数据库层，系统在开发环境下支持 SQLite 以便快速验证，在部署或扩展场景下可切换至 PostgreSQL 等更适合生产环境的数据库方案。对于异步任务处理，系统则通过 Redis 与 Celery 形成任务队列基础，用于支持资料解析与知识库构建等耗时流程。

#### 2.7.2 JWT鉴权与角色权限控制

在多角色教学平台中，鉴权与权限控制是保证系统安全性和业务边界的重要基础。JWT（JSON Web Token）是一种广泛应用于前后端分离系统中的无状态认证方案。其基本做法是：用户登录后，服务端根据用户身份生成包含主题、角色和过期时间等信息的令牌，前端在后续请求中通过 `Bearer Token` 方式携带该令牌，后端对令牌进行校验后识别用户身份。与传统服务端 Session 相比，JWT 更适合分离式架构下的接口调用与跨服务扩展。

本文系统在认证层采用基于 JWT 的访问控制方式，结合口令加密与角色校验实现用户访问保护。教师、学生和管理员在系统中的功能边界并不相同，因此平台还需要在认证成功之后进一步执行角色权限控制。例如，学生主要面向课程学习与问答，教师拥有资料上传、审核回流和课程管理能力，管理员则负责系统配置和全局管理。基于角色的权限控制能够保证接口只对合适的用户开放，是教学平台稳定运行的重要安全基础。

#### 2.7.3 模块化后端与可切换AI适配架构

AI 助教平台的复杂性不仅来自业务流程，也来自模型与基础设施的多样性。生成模型、嵌入模型、视觉模型、重排序模型、知识引擎、文档解析器和存储后端往往来源不同、调用方式不同、部署约束也不同。若在系统设计时将这些组件直接耦合到业务逻辑中，后续切换模型、替换知识引擎或升级存储方案都会变得困难。

为此，本文系统采用模块化后端与可切换 AI 适配架构。一方面，在知识引擎层保留不同 RAG 路径的切换能力，使系统既能运行本地回退引擎，也能接入 RAG-Anything 增强路径；另一方面，在模型接入层对生成、嵌入、视觉和重排序能力分别进行路由抽象，使系统可根据配置在 `mock`、`api` 和 `local` 等后端之间切换。类似地，在文件存储层也支持本地文件系统与 MinIO 对象存储两种方案。这样的设计具有三方面价值：其一，便于系统在原型验证阶段快速落地；其二，便于根据实际算力和成本条件切换模型来源；其三，便于后续进行对比实验、性能优化与工程扩展。可在本节最后通过 `表2-3 关键技术及其在本系统中的作用` 对上述技术进行统一归纳。

### 2.8 本章小结

本章围绕本文系统所依赖的相关理论与关键技术进行了阐述。首先，分析了大语言模型的基本原理、提示工程机制以及其在教育问答中的优势与局限，明确了大语言模型在本系统中作为生成核心而非独立知识来源的定位。其次，介绍了 RAG 技术的基本思想与处理流程，说明了其在课程问答中实现知识约束、证据支撑与动态更新的必要性。在此基础上，进一步讨论了 LightRAG 的图增强检索思想与 RAG-Anything 的多模态扩展能力，阐明了本文系统采用增强路径与本地回退路径并存的技术原因。

随后，本章对多模态文档解析、OCR、视觉语言模型、MinerU 复杂文档解析思路以及结构化存储进行了说明，强调了复杂课程资料进入知识库和问答系统所需的中间技术基础。同时，围绕课程知识图谱原型、学习分析与个性化支持、前后端分离、JWT 鉴权以及可切换 AI 适配架构，说明了这些技术如何共同支撑 AI 助教平台的整体实现。以上内容为第三章的系统需求分析与总体设计，以及第四章的关键模块设计与实现提供了必要理论依据。

## 第三章 系统需求分析与总体设计

### 3.1 用户角色与应用场景分析

#### 3.1.1 学生用户场景分析

学生是本系统最核心的服务对象，其主要需求集中在课程学习支持和问题求解支持两个方面。在传统教学场景中，学生虽然可以获得讲义、课件、实验指导和作业资料，但这些资源往往分散在不同载体中，查找成本较高；当学生遇到具体问题时，又需要等待教师答疑或在群聊记录中自行翻找历史回答，学习效率容易受到影响。因此，系统需要从学生视角出发，提供围绕课程知识获取、问答交互、学习记录和复习支持的一体化入口。

结合系统设计，学生用户的主要应用场景包括以下几个方面。第一，学生可以进入具体课程页面查看课程资料、课程公告和相关学习信息，完成日常学习内容浏览。第二，学生可以在课程上下文中向 AI 助教发起问题，系统结合课程知识库、历史资料与问答上下文生成回答，并在可能的情况下返回引用来源。第三，当学生对回答不满意时，可以通过反馈机制表达评价，推动系统进入后续审核与优化流程。第四，学生可以在学习过程中积累错题、闪卡和学习行为记录，并基于这些数据获得复习支持和学习建议。第五，学生还可以在个人学习仪表盘中查看学习画像、学习活跃情况和课程相关分析结果，从而形成更清晰的自我学习认知。

从本质上看，学生端的设计目标不是简单提供一个聊天窗口，而是围绕课程学习全过程构建智能支持入口，使其能够同时承担“快速问答工具”“资料索引入口”和“学习辅助载体”三种角色。后续可配合 `表3-1 用户角色与权限说明表` 对学生角色的主要操作权限进行归纳。

#### 3.1.2 教师用户场景分析

教师是课程知识内容的维护者、教学规范的制定者和系统回答质量的重要监督者，因此在本系统中承担着比普通内容管理员更丰富的职责。与学生相比，教师用户更关注课程资料管理、知识库构建、问答质量把控和课程学情洞察等功能。

具体而言，教师的主要应用场景包括：其一，创建课程与班级、维护课程基本信息，并管理学生加入班级后的教学组织关系。其二，上传课程资料，包括讲义、课件、实验指导等教学文档，并触发知识库构建流程，使课程资源能够转化为系统可检索知识。其三，查看 AI 助教回答中被系统自动标记的低置信度结果或被学生点踩的问题，对其进行审核、修正和回流补充，以提升后续问答质量。其四，查看课程相关学习分析结果，例如高频问题主题、学生整体提问趋势和个体学习画像，辅助教师理解课程痛点。其五，在需要时利用教师端 AI 助手组件进行课程内容查看、答疑管理和教学支持。

教师角色的引入，使本系统与纯自动化问答平台形成明显区别。系统并不试图完全替代教师，而是通过“AI 先答 - 教师纠偏 - 知识回流”的方式，将教师经验沉淀为课程知识服务能力。换言之，教师不仅是平台使用者，更是平台持续优化过程中的关键协同主体。

#### 3.1.3 管理员用户场景分析

管理员主要负责系统的全局运行配置与平台级管理，其职能侧重于资源治理、用户治理和系统可观测性管理。相较于教师和学生，管理员不直接参与具体课程学习过程，但对平台稳定运行与策略配置具有重要影响。

在本系统中，管理员的主要应用场景包括：管理用户账户与用户状态，维护课程与班级基础信息，查看系统通知与异常情况，配置模型接入参数与 AI 路由策略，观察知识库索引任务状态、问答性能指标与实验结果等。尤其在本系统引入可切换模型后端、RAG 引擎和异步索引机制之后，管理员端需要具备较强的配置与监控能力，以便对模型来源、索引任务和系统运行健康度进行统一治理。

因此，管理员角色的存在，不仅是一般意义上的后台管理需要，也与 AI 系统的工程化运维密切相关。其设计目标在于为系统提供统一配置入口、任务观测入口和故障诊断入口，从而支撑平台的长期运行与扩展。

### 3.2 功能需求分析

#### 3.2.1 课程与班级管理需求

课程与班级管理是本系统其他能力得以展开的基础。课程问答、资料管理、知识库构建和学习分析都必须依附于明确的课程上下文和班级组织关系，因此系统首先需要支持课程与班级的基本管理能力。具体而言，系统应允许教师创建课程、创建班级、维护课程与班级信息，并建立课程、教师、学生之间的关系映射。同时，学生应能够基于邀请码或课程入口加入对应班级，确保其访问范围与学习行为被绑定到正确的教学场景中。

从数据组织角度看，课程与班级管理不仅是前端页面需求，更决定了知识库和问答服务的边界。只有将问答会话、资料文件、学习记录和图谱信息绑定到具体课程或班级，系统才能避免跨课程混淆，并保证检索结果与教学语境一致。因此，课程与班级管理需求既具有业务属性，也具有知识隔离属性。

#### 3.2.2 课程资料管理与知识库构建需求

本系统的核心价值之一在于将分散课程资料转化为可检索、可追踪、可更新的知识基础，因此必须具备完整的课程资料管理与知识库构建能力。首先，系统应支持教师上传多种类型的课程资料，并记录文件名、类型、大小、上传者和课程归属等信息。其次，系统应在上传完成后触发解析与索引任务，对资料进行文本提取、关键词抽取、内容项组织和状态管理。再次，系统应支持查看资料的索引状态、构建结果和失败原因，以便教师和管理员判断知识库构建是否成功。

此外，课程资料管理需求还应覆盖重复文件识别、任务重试、索引异常恢复和动态更新等能力。由于教学资料在课程过程中可能被增补、替换或修正，系统需要支持知识库内容的持续更新，而不是一次性构建后静态不变。由此可见，资料管理模块不仅承担文件上传功能，更承担课程知识持续积累的入口职责。

#### 3.2.3 AI助教问答需求

作为 AI 助教平台的核心服务，问答模块需要同时满足基础可用性和教学可信性两方面要求。从功能层面看，系统应支持学生和教师在课程上下文中发起问题，并根据课程资料、知识库内容和对话上下文生成回答。回答结果应尽量包含来源引用、置信度信息和相关建议，以便用户理解回答依据并开展后续追问。

从场景上看，问答模块需要支持知识点查询、概念解释、作业辅助理解、资料定位、课程规则确认和任务型学习支持等多类问题。与此同时，系统还应支持多轮对话，能够在已有会话基础上理解后续提问，形成连续交互体验。由于教学场景对答案质量要求较高，问答模块还需要与知识检索、重排序、反馈记录和教师审核流程结合，构成完整的可信问答链路。

#### 3.2.4 教师审核与知识回流需求

单纯依赖自动问答并不足以支撑高可信度课程服务，因此系统必须具备教师审核与知识回流能力。具体来说，当回答置信度较低、用户给出差评或教师主动干预时，系统应能自动生成审核条目，记录问题内容、原始 AI 回答、触发原因和处理状态。教师随后可对回答进行修正、确认或驳回，并将修正后的高质量答案回流到知识库中，供后续类似问题复用。

这一需求的重要性在于，它将教师经验从“单次人工纠错”转化为“系统长期知识资产”。通过审核与回流机制，系统能够逐步积累符合课程要求的高质量问答对，缓解纯自动回答的误差风险，并为后续知识更新和问答优化提供持续依据。因此，审核与回流模块是连接“智能生成”和“人工把关”的关键枢纽。

#### 3.2.5 学习分析与个性化支持需求

如果系统仅停留在资料管理与即时问答层面，其作用仍然偏向工具化。为了真正形成教学辅助平台，系统还需要支持学习行为记录、学习画像构建、错题管理、闪卡复习和个性化资料推荐等功能。具体需求包括：记录学生在课程中的提问、浏览、复习和反馈行为；统计高频问题主题与学习活跃情况；根据问答与复习数据识别学生的强项和薄弱点；围绕错题和知识点生成可重复复习的资源；结合课程进展和学生画像提供针对性的学习支持。

该需求的提出，意味着系统从“回答一个问题”延伸到了“支持一个学习过程”。它不仅增强了平台的持续使用价值，也为教师理解学生学习状态提供了数据基础。

#### 3.2.6 多模态资料处理需求

由于课程资料天然存在图文混排和多模态特点，系统在需求层面还需要具备对复杂资料的适应能力。具体而言，系统应至少能够处理文本、表格、公式、图片和复杂 PDF 等常见课程资料形态，并尽可能保留其结构化信息，以便进入后续检索与问答流程。对于扩展方向上的音频与视频资料，系统也应在架构设计上预留可接入能力，以支持后续通过 ASR、关键帧抽取和多模态理解进一步增强资料覆盖范围。

因此，多模态处理需求不是附属功能，而是课程资料真实形态决定的基础要求。后续可通过 `表3-2 系统功能需求汇总表` 对上述功能需求进行集中整理。

### 3.3 非功能需求分析

#### 3.3.1 可用性需求

系统应具备良好的可用性，能够让学生、教师和管理员以较低学习成本完成各自操作。对于学生而言，问答入口应清晰，课程页面和学习支持功能应易于理解；对于教师而言，资料上传、索引状态查看和审核流程应尽量简洁；对于管理员而言，配置界面和监控指标应具有较强可读性。由于本系统面向教学支持场景，因此界面组织和操作路径不应过于复杂，否则会削弱系统在真实教学中的应用价值。

#### 3.3.2 可维护性需求

AI 助教平台涉及前端页面、后端业务、数据库、模型适配、检索引擎和异步任务等多个层次，若缺少良好的模块划分，将导致维护成本快速上升。因此，系统需要具备较强的可维护性，包括清晰的模块边界、相对独立的服务职责、统一的数据模型管理方式以及便于替换的模型与存储接口。只有在架构上保持良好分层，后续章节中涉及的模型切换、知识引擎升级和功能增强才具备现实可行性。

#### 3.3.3 可扩展性需求

本系统研究目标不仅是完成单一问答功能，而是构建可持续扩展的教学辅助平台。因此，系统在设计阶段应充分考虑多模型接入、不同 RAG 引擎切换、多种存储方式兼容以及多模态能力拓展的需要。例如，生成模型、嵌入模型、视觉模型和重排序模型都可能在后续实验中发生调整；知识引擎也可能在本地回退引擎与增强型引擎之间切换；文件存储则可能在本地磁盘与对象存储之间迁移。可扩展性需求决定了系统必须采用抽象接口与配置化机制，而不能将实现细节写死在业务流程中。

#### 3.3.4 安全性需求

由于系统涉及用户身份、课程资料、学习记录和教师审核结果等敏感信息，因此安全性需求不可忽视。系统需要具备用户认证能力、角色权限控制能力和基础的数据访问隔离能力，确保不同角色只能访问各自授权范围内的数据。此外，课程资料与问答内容也应避免跨课程泄露，知识检索与学习分析都需要严格围绕课程边界展开。对于 AI 模型调用环节，系统还需要通过统一配置管理 API 密钥和模型访问方式，降低敏感配置暴露风险。

#### 3.3.5 响应性与稳定性需求

作为面向教学场景的交互系统，平台需要在主要业务流程上保持较好的响应性和稳定性。即时问答请求应能够在合理时间内返回结果，资料上传后的解析任务应支持异步处理，避免阻塞前端交互；对于解析失败、模型依赖不可用等情况，系统应能够提供明确状态反馈和回退路径，减少整条业务链中断的风险。尤其在引入增强型多模态 RAG 引擎后，稳定的回退机制和任务重试机制成为保障整体可用性的关键要求。

### 3.4 系统总体架构设计

#### 3.4.1 总体架构设计思路

结合前述需求，本文系统总体采用“前端交互层 + 后端业务层 + 数据与存储层 + AI 与知识处理层”的分层架构。其核心设计思路在于：以课程和班级作为业务边界，以知识库和问答链路作为智能核心，以审核回流和学习分析作为持续优化支撑，从而构建一个既能服务即时教学需求，又具备后续扩展能力的 AI 助教平台。

在总体策略上，系统坚持两个原则。第一，课程边界优先原则。无论是资料管理、问答检索、学习分析还是图谱组织，都必须围绕课程或班级上下文展开，以保证教学内容一致性。第二，稳定性优先原则。系统在引入增强型多模态知识引擎的同时，保留本地回退问答路径，避免因模型配置、外部依赖或复杂解析链路异常导致主功能不可用。因此，本系统并非简单叠加前沿技术，而是在“能力增强”与“工程可用”之间进行平衡设计。

#### 3.4.2 前端架构设计

前端部分采用基于 React 的单页应用架构，并结合 Vite、TypeScript、Tailwind CSS、React Router 等技术完成页面构建与路由组织。该方案具有开发效率较高、组件复用性较强、适合多角色界面快速迭代等特点。当前系统前端围绕学生、教师和管理员三类角色构建了相应页面入口，主要包括登录注册页、学生仪表盘、学生课程页、教师仪表盘、教师课程页和管理员仪表盘等。

从交互组织方式看，前端重点承担页面展示、用户输入收集、学习状态呈现和角色化可视交互等职责。由于论文研究重点更偏向知识处理与问答机制，因此前端在系统中主要承担“多角色交互载体”的角色。需要说明的是，当前项目中的部分前端页面已达到较高原型展示水平，但并非所有页面都与后端所有接口形成完整实时联动，因此在论文中更适合将其表述为“已完成多角色系统原型前端设计，并围绕核心场景提供交互页面支撑”。

#### 3.4.3 后端架构设计

后端部分采用 FastAPI 构建 REST 风格服务接口，并以模块化单体结构组织业务能力。该设计既避免了微服务拆分带来的额外复杂度，又能通过明确模块划分保证功能边界清晰。系统当前已经形成认证、用户、课程、班级、知识库、任务、闪卡、对话、审核、学习分析、学生画像、通知和管理员配置等多个接口模块，能够较完整地支撑平台的业务主流程。

从职责划分角度看，后端主要承担四类任务：其一，管理课程、班级、资料、会话与学习记录等业务数据；其二，协调资料解析、知识库构建、问答生成和审核回流等 AI 相关流程；其三，通过 JWT 认证与角色校验控制不同用户权限；其四，通过管理接口与指标记录提供系统运行观测能力。采用模块化单体架构的优势在于，业务逻辑、数据库模型和 AI 能力编排仍保留在统一代码仓库中，便于原型系统快速迭代和论文实现部分的整体论述。

#### 3.4.4 数据与存储层设计

数据与存储层主要负责持久化保存课程业务数据、知识处理结果和运行过程记录。数据库方面，系统采用关系型数据库管理用户、课程、班级、资料、会话、审核、学习分析等结构化数据；在开发阶段支持 SQLite 快速运行，在部署与扩展场景下可切换为 PostgreSQL。关系型数据库之所以适合本系统，是因为平台存在大量具有明确实体关系的数据，如课程与班级、班级与成员、会话与消息、审核项与回流记录等，使用关系模型更易于表达其组织关系。

在文件存储方面，系统支持本地文件系统和 MinIO 对象存储两种模式。前者便于本地开发和原型验证，后者则适合在资料规模扩大后提供更稳定的对象化管理能力。通过将文件原始内容与解析结果分层管理，系统既能保留原始资料，又能将后续知识库构建所需的结构化信息保存到数据库中。这样一种“文件留存 + 结构化持久化”的设计，适合课程资料管理场景下对来源追溯与知识重建的双重要求。

#### 3.4.5 AI与知识处理层设计

AI 与知识处理层是本系统区别于一般教学平台的核心部分，主要由文档解析、知识库构建、课程知识关联组织、RAG 问答、模型路由、多模态增强和审核回流等能力共同构成。在知识引擎层面，系统采用“本地回退引擎 + RAG-Anything 增强路径”的双路径设计：本地回退引擎负责在一般情况下稳定完成文本检索、图辅助扩展和基础回答；增强路径负责在模型与解析环境就绪时提供更强的复杂文档和多模态处理能力。

在模型协同层面，系统对生成模型、嵌入模型、视觉模型和重排序模型分别设置路由抽象，使其可以依据配置选择本地或远程后端。这种设计使系统不再依赖单一模型供应方案，而具备更强的实验与部署弹性。对于知识更新层，系统还通过资料再索引、审核回流和任务重试等机制保证知识处理链路具备动态更新能力。后续可在 `图3-1 系统总体架构图` 中对上述分层结构与模块关系进行统一展示。

### 3.5 核心业务流程设计

#### 3.5.1 课程资料上传入库流程

课程资料上传入库流程是系统知识库构建的起点。整体流程可概括为：教师在课程或班级上下文中上传资料，系统首先完成文件接收、元数据记录和存储保存；随后创建对应的资料记录、知识空间关联和解析任务记录；接着根据配置进入同步或异步解析流程，对资料执行文本提取、关键词抽取、内容项整理和知识索引构建；最后，将任务状态、摘要结果、切分片段和扩展元数据保存下来，并更新资料与知识空间的状态信息。

这一流程之所以采用“上传记录”和“解析构建”分离的设计，是因为资料接收本身较快，而知识处理可能较耗时，尤其在引入复杂文档解析与多模态增强时更是如此。通过将二者拆分，系统可以一方面保证前端交互及时响应，另一方面为后续任务重试、队列调度和异常恢复提供基础。后续可使用 `图3-2 资料上传入库流程图` 展示该流程。

#### 3.5.2 基于RAG的课程问答流程

课程问答流程围绕“问题输入 - 课程知识检索 - 证据组织 - 模型生成 - 结果记录”的主线展开。当学生或教师在课程会话中提出问题后，系统首先记录对话上下文并识别对应课程边界，然后根据当前模型配置和知识引擎配置选择问答路径。随后，系统在课程知识库中进行候选证据召回，并结合检索策略、图辅助扩展和重排序机制筛选出更适合支撑回答的内容。之后，大语言模型在课程证据、提示词约束和对话历史基础上生成回答，并附带来源、建议和置信度信息。最终，系统将本轮问答结果和引用信息写入会话记录，并根据置信度或用户反馈决定是否触发审核流程。

这一流程体现了系统的核心设计理念，即回答并非直接由模型凭空生成，而是尽量建立在课程资料和知识处理结果之上。后续可用 `图3-3 问答—审核—回流闭环流程图` 展示这一主链路。

#### 3.5.3 教师审核与知识回流流程

当系统检测到低置信度回答，或者学生对回答给出负向反馈时，系统将自动创建审核条目，记录问题内容、原始回答和触发原因。教师进入审核界面后，可查看待处理条目，对问题进行人工修正、确认或驳回。若教师认为修正后的答案具有长期复用价值，则系统进一步创建回流记录，并将修正问答对同步至知识引擎和本地知识存储中，用作后续类似问题的辅助知识。

从流程设计上看，审核与回流机制的关键不在于“人工参与一次”，而在于“人工参与能否改变后续系统表现”。因此，本系统特别强调审核结果的可追踪性和同步状态管理，使教师修正不仅停留在当前页面，而能真正进入后续知识服务链路。

#### 3.5.4 学习分析与推荐流程

学习分析与推荐流程建立在学生学习行为持续采集的基础上。系统在资料浏览、问题提问、回答反馈、错题整理和闪卡复习等过程中记录学习行为数据，并定期或按需更新学生画像与课程分析结果。在此基础上，系统能够生成周报、月报、问题主题聚合结果、学习活跃度指标和规则驱动的推荐结果，用于帮助学生进行针对性复习，也用于支持教师观察课程共性问题。

该流程的设计强调“分析服务于支持”，即学习分析不是孤立统计，而是要进一步反哺个性化学习帮助。后续可用 `图3-4 学习分析与推荐流程图` 展示其数据流。

### 3.6 数据库与接口设计

#### 3.6.1 核心数据库实体设计

结合系统业务与知识处理需求，本文系统的数据库实体可划分为用户与课程组织、课程资料与知识库、对话与审核、学习分析与复习支持四个层次。用户与课程组织层包含用户、课程、班级和班级成员等实体，用于表达教学关系与权限边界；课程资料与知识库层包含资料、知识空间、解析任务、知识实体和知识关系等实体，用于管理课程资料与知识组织结果；对话与审核层包含会话、消息、引用、审核项和回流记录等实体，用于承载问答主流程与人工纠偏闭环；学习分析与复习支持层则包括学习记录、问题统计、学生画像、错题和闪卡等实体，用于支持学习行为分析和个性化功能。

这种实体划分方式具有较强的层次性，既便于数据库建模，也与系统模块划分保持一致。后续可在 `图3-5 数据库E-R图` 中展示主要实体关系，并在 `表3-3 核心数据库实体说明表` 中进一步说明字段含义。

#### 3.6.2 课程与知识库相关数据模型设计

课程与知识库相关模型主要围绕 `Course`、`Class`、`ClassMember`、`Material`、`KBSpace`、`FileParseTask`、`KnowledgeEntity` 和 `KnowledgeRelation` 等实体展开。其中，课程和班级实体负责定义教学上下文；资料实体用于记录文件基本信息和知识库状态；知识空间实体用于描述课程知识库整体状态；解析任务实体用于承载具体资料的索引构建过程；知识实体与知识关系则共同构成课程知识图谱原型或知识关联结构。

这种设计反映出一个明确思路：系统并不将“文件上传成功”视为知识构建完成，而是通过知识空间和解析任务这两个中间实体，将资料管理与知识处理过程显式建模。这样既便于记录索引状态和失败原因，也有利于支持重试、质量诊断和动态更新。同时，知识实体和关系被单独建模，而不是直接嵌入资料记录中，体现出系统对课程知识组织和图辅助检索的重视。

#### 3.6.3 聊天、审核与学习分析相关数据模型设计

问答与学习支持相关模型主要包括 `ChatSession`、`ChatMessage`、`ChatCitation`、`ReviewItem`、`ReviewSyncRecord`、`LearningRecord`、`QuestionAnalytics`、`StudentProfile`、`StudyMistake`、`Flashcard`、`FlashcardRecord` 和 `RAGQueryEvent` 等实体。会话与消息实体用于保存多轮交互过程；引用实体用于保存来源信息，增强回答可追溯性；审核项与回流记录用于支撑教师纠偏闭环；学习记录、主题统计和学生画像实体用于沉淀学习分析结果；错题与闪卡实体则用于支持后续复习机制；RAG 查询事件实体则用于记录检索与生成过程中的关键运行指标，为系统性能分析和管理端观察提供基础。

这种数据模型设计说明，本系统并未将问答视作一次性请求，而是将其纳入长期数据积累体系之中，使问答结果、人工反馈和学习支持可以共享同一套数据基础。这种设计将直接支撑后续第四章和第五章中的实现分析与实验评估。

#### 3.6.4 核心接口设计

在接口层面，系统围绕主要业务模块组织了一组 REST 风格接口，涵盖认证、用户、课程、班级、知识库、任务、闪卡、聊天、审核、分析、通知和管理员配置等功能。认证接口负责用户登录与身份认证；课程与班级接口负责课程组织管理；知识库接口负责资料上传、索引状态查看和知识处理结果查询；聊天接口负责课程问答与对话管理；审核接口负责待审核条目与教师回流操作；分析接口负责学习画像、报表和统计结果输出；管理员接口则用于模型配置、索引任务管理、RAG 性能观测和实验结果查看。

从设计原则上看，这些接口并非杂乱堆叠，而是与系统角色和核心流程保持对应关系：学生主要使用课程、聊天、学习分析等接口；教师主要使用课程、知识库、审核和分析接口；管理员则侧重配置、监控和任务治理接口。接口分层与角色权限控制相结合，使系统在功能组织上更具清晰性。后续可通过 `表3-4 核心接口说明表` 对关键接口及其功能进行归纳。

### 3.7 本章小结

本章从系统需求分析与总体设计两个层面，对本文 AI 助教平台的整体方案进行了阐述。首先，围绕学生、教师和管理员三类角色分析了平台的主要应用场景，明确了系统不仅要满足课程问答，还要同时支持资料管理、教师审核、学习分析与平台配置等需求。随后，从功能需求和非功能需求两个角度说明了课程管理、知识库构建、可信问答、审核回流、多模态处理、可扩展性、安全性和稳定性等设计要求。

在总体设计部分，本章给出了系统的分层架构思路，说明了前端交互层、后端业务层、数据与存储层以及 AI 与知识处理层之间的关系，并重点阐明了课程边界约束、双路径知识引擎、模块化后端与可切换模型适配架构等关键设计原则。同时，本章还梳理了资料上传入库、RAG 问答、审核回流和学习分析等核心业务流程，并对数据库实体与接口模块进行了整体性说明。这些内容为下一章进一步展开系统关键模块的设计与实现奠定了结构基础。

## 第四章 AI助教平台关键模块设计与实现

### 4.1 课程资源管理与知识库构建模块设计与实现
#### 4.1.1 模块功能概述

课程资源管理与知识库构建模块承担资料接收、解析调度、索引落库与状态维护四项职责，是后续问答、审核回流与学习分析的数据入口。数据层采用关系模型与半结构化字段结合的方式组织。`Material` 以 `id` 为主键，通过 `class_id` 与 `Class` 关联；`KBSpace` 以课程和班级为作用域，通过 `(course_id,class_id)` 描述某一教学空间的知识库状态；`FileParseTask` 同时通过 `material_id` 关联资料、通过 `kb_space_id` 关联知识空间。该结构将资料元数据、知识空间状态与解析任务分离，支持异步任务调度、失败重试与过程回溯。

从处理链路看，模块包含存储适配层、解析适配层与知识引擎适配层三部分。存储层负责原始文件持久化，解析层负责文本抽取与内容结构化，知识引擎层负责切片索引、知识实体同步以及增强检索框架接入。业务数据库保存摘要化结果与状态元数据，RAG-Anything 工作目录保存增强索引与内部图结构，两者通过资料标识与班级标识形成映射关系。

#### 4.1.2 课程资料上传与任务创建流程

资料上传流程可表示为
$$
\mathcal{P}_{ingest}: u \rightarrow auth \rightarrow store(f) \rightarrow hash(f) \rightarrow create(m,t) \rightarrow enqueue(t),
$$
其中 $u$ 表示上传用户，$f$ 表示原始文件，$m$ 表示资料记录，$t$ 表示解析任务。流程起始于课程访问控制：后端校验用户对课程与班级的可访问性，然后调用存储适配器写入原始文件。当前实现支持本地文件系统与 MinIO 两类后端，前者适用于开发与轻量部署，后者适用于对象化集中存储。

文件落盘后创建 `Material` 记录，并检查对应 `(course_id,class_id)` 的 `KBSpace` 是否存在；若不存在，则自动创建知识空间。随后生成或更新 `FileParseTask`。资料去重采用 SHA-256 哈希，计算复杂度为 $O(n)$，其中 $n$ 为文件字节数。哈希值写入 `extra_data.ingest.file_hash`，并在同一班级范围内执行重复检测；若检测到相同内容，可复用既有任务状态，避免重复解析。

异步入库由 Celery 执行，调用 `index_parse_task.delay` 提交后台任务，并在任务元数据中记录 `queue_task_id`、`queue_status` 以及 `queue_prepare`、`queue_submit`、`queue_submitted` 等历史事件。当前实现使用统一索引队列，尚未进一步配置队列优先级。对于体量较小的文件，也可直接走同步索引路径，以缩短交互等待时间。

#### 4.1.3 解析结果结构化落库设计

解析结果落库采用“关系表 + JSON 字段”的混合模式。`Material` 保存资料元数据，`KBSpace` 保存知识空间统计信息，`FileParseTask` 保存面向检索链路的核心中间结果，其中 `summary` 保存摘要，`extracted_text` 保存抽取文本，`chunks` 保存切片结果，`extra_data` 保存关键词、内容项、质量标记、执行历史与告警数据。该设计避免为每一种版面元素单独建立物理表，同时保留足够的结构化信息用于问答与监控。

本地回退解析器由 `SimpleParserProvider` 实现。解析器读取文件可用文本后，对文本进行长度为 500、重叠长度为 80 的滑动窗口切分，再提取 Top-12 关键词，最终生成 `text`、`table`、`equation`、`figure` 四类内容项，并统一规范化到 `content_items_schema = v1`。其中，表格内容通过 Markdown/CSV 模式识别，公式内容通过正则模式抽取，图像文件则保留图像类内容项与可读文本后备描述。

增强路径由 RAG-Anything 负责。当前配置中 `parser=mineru`、`parse_method=auto`、`query_mode=mix`。复杂文档先在 RAG-Anything 工作目录中完成解析与索引，再由业务层同步摘要、切片与内容项到数据库。由此形成两层持久化：一层面向增强检索框架，保存其内部索引与图组织结果；另一层面向业务接口，保存可查询、可监控的摘要化结构。

RAG-Anything 在框架内部采用双图组织策略：一类图用于表达文本块、表格、图像、公式等多模态内容单元及其结构关联，另一类图用于表达语义实体及其关系。业务数据库并不直接复制其全部内部图结构，而是仅保存课程知识关联原型与解析摘要结果。这种分层方式减少了业务表设计复杂度，并保留了外部框架的增强检索能力。

#### 4.1.4 动态知识更新与版本管理机制

知识库状态由资料状态与任务状态联合描述。资料状态集合记为
$$
S_m=\{pending, processing, indexed, failed\},
$$
任务状态集合记为
$$
S_t=\{pending, processing, completed, failed\}.
$$
`Material.kb_status` 表示资料索引进度，`FileParseTask.status` 与 `extra_data.ingest` 共同保存尝试次数、错误类别、队列标识、自动重试轮次、下一次重试时间与事件历史。

索引失败后的重试策略分为两层。任务调度层采用固定冷却时间机制，当前配置为 `KB_QUEUE_RETRY_COOLDOWN_SECONDS=30` 秒，最大自动重试轮数为 2，最大解析尝试次数为 2。若任务在冷却期内，则 `_can_retry_task_now` 返回不可重试状态；若超过最大轮次或达到最大尝试次数，则触发管理员告警。模型接口层采用指数退避机制处理上游暂时性错误，延迟满足 $t_n=2^n$ 秒，用于应对 `429/5xx` 等瞬时异常。两层策略分别作用于“任务重排队”和“模型请求重试”两个阶段。

为保证知识更新的可追踪性，任务元数据维护了按时间追加的历史事件序列
$$
H_t=\{e_1,e_2,\dots,e_n\},
$$
其中事件类型包括 `attempt_start`、`attempt_done`、`auto_retry_scheduled` 等。管理员接口可据此统计队列深度、平均等待时延、执行时延和重试成功率。

知识关联原型采用增量合并方式更新。重复出现的知识实体不会被覆盖，而是更新其 `provenance`、`source_span` 与置信度。当前实体置信度采用加权融合：
$$
c_{new}=\min(0.99,\max(0.4,0.8c_{old}+0.2c_{in})).
$$
关系权重则按共现次数逐步增加，上限为 5.0。该机制使得多份资料共同出现的知识点能够获得更高的证据密度。

#### 4.1.5 本模块实现效果分析

现有实现已经覆盖资料上传、哈希去重、索引调度、失败重试、状态监控与知识同步的完整链路。文本型文件可由本地回退解析器直接处理，复杂 PDF 与图文混排资料可经由 RAG-Anything 与 MinerU 进入增强路径；存储层支持本地目录与 MinIO 两类后端；后台索引支持同步执行与 Celery 异步执行两类模式。

当前边界主要体现在三个方面：其一，数据库未对版面块、表格单元与公式区域做细粒度独立建模，版面级精确检索能力仍受限；其二，本地回退解析器更强调稳定性与轻量性，多模态深度理解主要依赖增强路径；其三，RAG-Anything 内部双图结构保存在框架工作目录中，业务侧保存的是摘要化投影而非完整内部索引。这些边界不影响课程问答主链运行，但会影响更细粒度的版面分析与跨模态结构查询。

### 4.2 检索增强问答模块设计与实现
#### 4.2.1 问答模块总体处理流程

问答主链可划分为四个阶段：查询处理阶段、检索召回阶段、重排序阶段与生成阶段。设用户查询为 $q$，历史对话为 $H$，附件集合为 $A$，课程知识空间为 $\mathcal{K}$，则问答函数可表示为
$$
\operatorname{Ans}(q)=\mathcal{G}\big(q,H,A,\operatorname{Rank}(\operatorname{Retrieve}(q,\mathcal{K}))\big),
$$
其中 $\mathcal{G}$ 表示生成器。处理入口位于对话服务层：系统先创建或加载 `ChatSession`，将用户消息写入 `ChatMessage`，再同步记录学习行为与高频主题，随后根据管理端模型配置选择知识引擎与模型路由快照，最终返回带引用来源、建议问题和置信度的回答结果。

若结果置信度低于阈值 $\theta_c=0.7$，则回答被自动标记为待审核，并生成 `ReviewItem`。因此，问答链路并非单次模型调用，而是“会话管理 - 检索 - 证据组织 - 回答生成 - 质量分流”的复合流程。

#### 4.2.2 问题处理与上下文组装

查询处理阶段先确定课程边界，再组装与查询相关的运行上下文。上下文项包括最近若干轮会话历史、用户角色、附件信息以及当前模型路由。代码中保留了 `role` 参数，使学生端与教师端能够共用同一检索主链而承载不同的交互任务。

若开启查询改写，原始查询 $q$ 会扩展为查询集合
$$
Q=\{q^{(1)},q^{(2)},\dots,q^{(m)}\},
$$
其中 $m \le 3$。改写模式由配置项 `RAG_QUERY_REWRITE_MODE` 控制，当前支持 `simple`、`compact` 与 `keywords` 等方式。该机制主要用于缓解学生提问表述与教材术语不一致的问题。

对于图像附件，增强路径会先调用视觉语言模型生成附加描述 $d_{img}$。提示词设计并未采用复杂链式模板，而是直接要求模型提取图像中的题干内容、可见文本、图示结构与求解线索。该描述作为扩展上下文参与后续检索与生成，从而将纯文本查询扩展为图文联合查询。

#### 4.2.3 课程知识检索与候选证据生成

本地回退引擎的候选集合记为
$$
C=\{c_1,c_2,\dots,c_n\},
$$
其中每个 $c_i$ 来自已完成索引的 `FileParseTask.chunks`。对任一查询变体 $q^{(j)}$ 与候选片段 $c$，定义词项集合 $T(\cdot)$，则词法重合度为
$$
Sim_{lex}(q,c)=\max_j \frac{|T(q^{(j)}) \cap T(c)|}{\max(|T(q^{(j)})|,1)}.
$$
若多个查询变体命中同一片段，则进一步计算覆盖率
$$
Cov(q,c)=\frac{|\{j \mid T(q^{(j)}) \cap T(c)\neq \varnothing\}|}{m},
$$
并叠加覆盖增益项 $B_{cov}$。当前实现中，覆盖增益上限为 0.16。

课程知识图谱原型记为 $G=(V,E)$，其中 $V$ 对应 `KnowledgeEntity`，$E$ 对应 `KnowledgeRelation`。查询图扩展并未执行基于边的 BFS 深度搜索，而是采用实体名称匹配生成扩展词集合。定义
$$
V_q=\{v \in V \mid T(v)\cap T(q)\neq \varnothing\},
$$
再构造图扩展词集合
$$
T_G(q)=\bigcup_{v\in V_q}T(v).
$$
图增益项定义为
$$
B_g(q,c)=\min(0.5,0.07\cdot |T_G(q)\cap T(c)|).
$$
据此，三类检索策略的综合得分分别为
$$
Sim(q,c)=
\begin{cases}
Sim_{lex}(q,c)+B_{cov}, & \text{lexical}\\
B_g(q,c)+0.1Sim_{lex}(q,c)+B_{cov}, & \text{graph}\\
Sim_{lex}(q,c)+B_g(q,c)+B_{cov}, & \text{hybrid}
\end{cases}
$$
候选集按综合得分降序排列，取前 `RAG_RETRIEVAL_CANDIDATE_K=12` 个进入下一阶段。

增强路径由 RAG-Anything 执行。当前查询模式为 `mix`，对纯文本请求调用 `aquery` 或 `query`，对带图像请求优先调用 `aquery_with_multimodal`。增强路径中的多模态检索依赖框架内部双图结构与嵌入检索，不直接暴露其全部中间得分，但返回结果会统一归一化为业务层可消费的来源列表与置信度。

#### 4.2.4 重排序与证据组织机制

重排序阶段以候选集 $C$ 为输入，输出重排后的候选序列 $C'=\operatorname{Rank}(q,C)$。当前实现支持 `mock`、`none`、`api`、`local` 四类重排序后端。若未启用重排序，则默认以检索得分作为排序依据；若启用重排序，则使用重排序模型重新计算候选相关性，并保留原始检索得分作为辅助特征。

证据组织阶段从 $C'$ 中选取前 `RAG_ANSWER_TOP_K=3` 个片段构造回答上下文，同时提取 `source_name`、`page`、`source_type`、`chunk_id` 与得分字段，形成结构化证据集合
$$
E_q=\{e_1,e_2,e_3\}.
$$
该阶段的输出既用于回答生成，也用于后续引用落库与审核追踪。

#### 4.2.5 答案生成、引用来源与置信度输出

本地回退引擎的生成阶段主要基于证据摘要组装回答文本；增强路径则优先采用 RAG-Anything 返回的生成结果。两条路径均输出统一的 `RAGResult` 结构，其中包含 `answer`、`sources`、`confidence`、`suggestions` 与 `meta`。回答写入 `ChatMessage` 后，系统会为每条来源额外创建 `ChatCitation`，形成“回答 - 证据”二元映射。

模型接入采用分层路由。生成模型、嵌入模型、视觉模型与重排序模型分别独立配置，后端可在 `api`、`local` 与 `mock` 等后端之间切换。其中，增强路径要求生成后端与嵌入后端均为非 `mock`。这种拆分方式允许使用本地部署模型承担推理任务，也允许通过 OpenAI 兼容接口调用远程模型，从而在部署成本、响应时延与回答质量之间进行权衡。

回答质量控制由置信度与反馈共同驱动。低置信结果直接进入审核队列；若用户后续提交 `dislike` 反馈，消息同样会被标记为待审核。由此形成自动质量分流机制。

#### 4.2.6 教师端与学生端问答差异化支持

差异化支持主要体现在交互入口与任务模板，而不是后端检索主链的完全分叉。学生端更侧重课程问答、复习提示与资料推荐，教师端更侧重教学辅助、反馈处理与学情分析。后端将 `role` 一并写入学习行为记录与 RAG 查询事件，用于后续统计与实验分析。

这种设计使共享知识底座与差异化业务入口能够并存。检索、重排序、引用与审核逻辑在服务层复用，角色差异则由页面入口、快捷问题模板与教师审核流程承担。

### 4.3 教师审核与知识回流模块设计与实现
#### 4.3.1 审核触发机制设计

审核触发来自两类事件：低置信回答与用户负反馈。当回答置信度满足 $conf(q)<\theta_c$ 且 $\theta_c=0.7$ 时，系统自动创建 `ReviewItem`；当用户对回答提交 `dislike` 反馈时，若该消息尚无审核项，则补建新的审核记录。两类事件分别对应模型侧不确定性与用户侧质量否定，能够覆盖课程问答中的主要风险来源。

#### 4.3.2 审核记录与状态管理设计

审核模块采用双状态对象建模。`ReviewItem` 负责描述人工审核生命周期，其状态集合记为
$$
S_r=\{pending, resolved, dismissed\};
$$
`ReviewSyncRecord` 负责描述知识回流生命周期，其状态集合记为
$$
S_s=\{pending, synced, failed\}.
$$
若将两者组合，可得到审核与同步的联合状态空间
$$
S=S_r \times (S_s \cup \{\varnothing\}).
$$
其中 $\varnothing$ 表示尚未产生同步记录。

`ReviewItem` 保存问题内容、原始 AI 回答、教师修正答案、触发原因、处理教师与处理时间等信息；`ReviewSyncRecord` 保存最终答案、同步状态、同步说明与同步时间。该拆分将“教师是否已处理”和“知识是否已写回”区分为两个可观测阶段。

#### 4.3.3 教师修正答案提交与处理流程

教师提交修正答案后，状态迁移函数 $\delta$ 可表示为
$$
\delta((pending,\varnothing), resolve)=(resolved,pending).
$$
实现上，`resolve_review` 会先将 `ReviewItem.status` 更新为 `resolved`，写入 `teacher_answer`、`reviewed_by` 与 `reviewed_at`，随后立即创建 `ReviewSyncRecord`，并将其初始状态设为 `pending`。

若教师选择不写回知识库，则同步记录保持 `pending`，并附加 `Sync skipped by teacher choice` 说明；若选择写回，则进入知识同步阶段，状态进一步迁移为
$$
\delta((resolved,pending), sync\_ok)=(resolved,synced),
$$
或
$$
\delta((resolved,pending), sync\_fail)=(resolved,failed).
$$
该设计将人工修正与知识同步明确拆分，便于后续排查回流失败原因。

#### 4.3.4 知识回流与动态知识补充机制

知识回流接口统一由 `rag.add_qa_pair(class_id, question, answer)` 执行，但在不同引擎下落到不同实现。对于本地回退引擎，教师修正问答会写入 `KBSpace.extra_data.manual_qa`，并保留最近 20 条人工问答，用作后续检索补充证据。对于 RAG-Anything 适配器，教师修正答案先通过 `insert_content_list` 写入增强索引，再继续调用父类逻辑同步到 `manual_qa`。因此，回流后的教师答案能够同时作用于增强路径与回退路径。

从技术链路看，教师修正答案属于增量知识写入，而非全量重建。其写入粒度为问答对级别，不触发资料重解析，但能够改变后续检索候选集与回答依据。该过程可表示为
$$
\mathcal{K}_{t+1}=\mathcal{K}_t \cup \{(q_i,a_i)^{teacher}\},
$$
其中 $(q_i,a_i)^{teacher}$ 表示经教师确认的问答知识单元。

#### 4.3.5 用户反馈驱动的系统迭代优化机制

每次问答结束后，系统都会记录 `RAGQueryEvent`，其字段包括引擎类型、查询模式、是否使用回退路径、响应时延、来源数量、置信度以及模型路由信息。若用户提交负反馈，则额外写入 `LearningRecord` 并触发审核流程。由此可将反馈机制表示为
$$
F=\{event, feedback, review, sync\},
$$
其中 `event` 对应问答指标采集，`feedback` 对应用户评价，`review` 对应教师修正，`sync` 对应知识回流。

当前实现已经完成反馈采集、审核分流与知识回写三部分，但尚未形成基于反馈数据的自动参数更新流水线。因而，模块实现的重点应表述为“支持基于教师修正的持续知识更新”，而非“已完成模型再训练”。

### 4.4 学习分析与个性化支持模块设计与实现
#### 4.4.1 学习行为记录与数据采集

学习分析以行为日志为输入。设学生行为日志集合为
$$
L=\{l_1,l_2,\dots,l_n\},
$$
其中每个 $l_i$ 由 `user_id`、`class_id`、`activity_type`、`ref_id`、`extra_data` 与时间戳组成。当前日志类型覆盖提问、负反馈、闪卡复习等关键学习事件。日志采集与问答、反馈、复习等业务过程同步执行，因此学习分析并非离线附属模块，而是运行时数据基础设施。

在问答链路中，用户提问会记录为 `ask_question`；负反馈会记录为 `message_dislike`；闪卡复习会记录为 `flashcard_review`。这些记录共同构成后续画像计算、课程分析与个体报告的基础样本。

#### 4.4.2 学生画像构建机制

学生画像可表示为向量
$$
P_u=(C_u, Q_u, D_u, T_u, A_u, W_u, S_u, t_u),
$$
其中 $C_u$ 为偏好课程集合，$Q_u$ 为总提问数，$D_u$ 为负反馈次数，$T_u$ 为任务完成率，$A_u$ 为活跃度得分，$W_u$ 为弱项主题集合，$S_u$ 为强项主题集合，$t_u$ 为最近活跃时间。当前实现中，活跃度得分计算为
$$
A_u=\min\left(100,\ 2.5Q_u+4N_u+1.2|L_u|\right),
$$
其中 $N_u$ 为已完成任务数量，$L_u$ 为学习记录集合。

弱项主题主要来源于学生审核记录与提问记录中的高频词，强项主题主要来源于评分不低于 4 的闪卡复习标签。画像结果写入 `StudentProfile`，并作为周报、月报与推荐生成的输入。

#### 4.4.3 周报、月报与课程学情分析实现

学习报告生成采用规则引擎。对任意学生 $u$，报告函数可表示为
$$
Rep_u=f(L_u,P_u,M),
$$
其中 $L_u$ 为时间窗口内日志，$P_u$ 为画像，$M$ 为模型路由上下文。系统按 7 天或 30 天窗口统计问题数量、任务提交数、闪卡复习数与学习事件数，并生成 `summary`、`metrics`、`highlights` 三类输出。

课程学情分析由 `compute_course_analytics` 完成，输出课程提问总量、差评问题数量、活跃学生数、任务完成率、待审核问题数与高频主题分布。学生周报与月报中的推荐焦点由规则直接生成：
$$
R=\{r_1,r_2,\dots,r_k\}=Top3(W_u)\cup Top2(S_u),
$$
其中前 3 项来自弱项主题，后 2 项来自强项主题。系统还会保留当前模型路由切片，便于实验比较不同模型组合对报告结果的影响。

#### 4.4.4 错题本与闪卡复习机制实现

错题本由 `StudyMistake` 持久化，记录题目内容、章节、个人答案、标准答案、解析、错误次数与掌握状态。练习动作会增加错误次数并更新时间，掌握动作会更新状态位，由此形成针对错题的持续跟踪序列。

闪卡复习采用类间隔重复启发式，而非完整 SM-2 算法。设第 $n$ 次复习前的间隔为 $I_{n-1}$、难度系数为 $E_{n-1}$、评分为 $r_n \in \{1,2,3,4,5\}$，则更新规则为
$$
(I_n,E_n)=
\begin{cases}
(1,\max(1.3,E_{n-1}-0.2)), & r_n \le 2 \\
(\max(2,I_{n-1}+1),E_{n-1}), & r_n = 3 \\
(\max(3,\operatorname{round}(I_{n-1}E_{n-1})),\min(3.0,E_{n-1}+0.05)), & r_n = 4 \\
(\max(4,\operatorname{round}(I_{n-1}(E_{n-1}+0.3))),\min(3.2,E_{n-1}+0.1)), & r_n = 5
\end{cases}
$$
并设置下一次复习时间 $next\_review\_at = now + I_n$ 天。该规则能够体现“掌握越充分，复习间隔越长”的基本思想。

#### 4.4.5 个性化资料推荐实现

个性化支持当前以规则驱动为主。推荐函数可表示为
$$
\mathcal{R}(u)=g(P_u,L_u,M),
$$
输出为推荐集合
$$
R=\{r_1,r_2,\dots,r_k\}.
$$
后端将推荐结果写入学生报告中的 `recommended_focus` 与 `recommendation_context` 字段，其输入主要包括弱项主题、强项主题、活跃度与模型路由上下文。

前端学生助教页面进一步基于关键词匹配展示资料建议，但这一部分主要承担展示与引导作用。排序核心仍以后端规则分析结果为依据，而非独立深度推荐模型。

### 4.5 前后端交互与可视化实现
#### 4.5.1 学生端课程页与AI助教组件实现

前端采用 React 19、Vite、TypeScript 与 Tailwind CSS 组合实现，页面路由由 `react-router-dom` 管理。学生课程页采用组件化组织方式，将课程资料、任务、错题、闪卡、图谱弹窗与 AI 助教拆分为相对独立的视图单元。状态管理以 React Hooks 为主，局部交互状态包括输入消息、附件列表、来源弹窗与推荐区域显示状态等。

学生端 AI 助教组件与后端之间采用 RESTful 接口通信，核心接口包括会话列表、消息发送、消息反馈、课程资料与图谱查询等。后端基于 FastAPI 暴露接口并自动生成 OpenAPI 描述，便于前后端联调。界面侧已实现附件识别、消息来源展示、反馈提交和推荐信息展示等交互逻辑。

#### 4.5.2 教师端课程页与教师AI助教实现

教师端课程页复用相同的组件化架构，但任务入口偏向资料治理、教学辅助与审核处理。教师 AI 助教组件支持附件上传、快捷问题、会话展示与引用来源呈现；审核相关交互则围绕教师查看负反馈、填写修正答案与确认处理状态展开。

当前界面层对部分教师待办流程使用浏览器本地存储维持临时状态，以验证交互路径；后端真实审核接口已提供 `/chat/reviews`、`/chat/reviews/{review_id}/resolve` 等资源。由此形成“前端工作流验证 + 后端审核能力落地”的并行实现方式。

#### 4.5.3 知识图谱展示实现

课程知识图谱由后端接口 `/api/v1/kb/courses/{course_id}/graph` 输出，返回节点集合与边集合。节点包含实体名称、类型、置信度、来源资料与来源片段信息，边包含关系类型、权重、置信度与证明信息。后端输出结构可形式化表示为
$$
\mathcal{G}_{api}=(V_{api},E_{api}),
$$
其中 $V_{api}$ 与 $E_{api}$ 分别对应前端可视化所需的节点和边。

前端课程页已实现图谱弹窗与节点展示逻辑，用于呈现课程知识关联原型。该部分当前主要完成核心显示链路和交互容器，负责把后端图结构投影为页面可视化对象；更复杂的布局算法与大规模图交互仍可继续扩展。

#### 4.5.4 学情报表与推荐结果展示实现

学生端与教师端仪表盘承担不同的数据呈现职责。学生侧聚焦个人报告、课程进度与推荐关注方向，教师侧聚焦课程统计、待审核问题与活跃度分布。统计结果由后端 `/students/me/reports/weekly`、`/students/me/reports/monthly`、`/analytics/courses/{course_id}/analytics` 等接口提供。

前端可视化层的职责是将结构化结果映射为卡片、列表与统计图表，后端负责提供画像、课程统计与推荐结果。该分工保证了分析逻辑集中在服务层，可视化逻辑集中在界面层。

### 4.6 多模态资料处理与扩展能力实现
#### 4.6.1 音频资料处理流程

音频处理目前处于架构预留阶段，接口设计可容纳 ASR 转写结果作为后续知识单元输入，但代码中尚未形成独立音频解析流水线。因此，音频能力在当前实现中应界定为可扩展接口，而非已落地主链。

#### 4.6.2 视频资料处理流程

视频处理同样保留了扩展空间，可在后续接入音轨转写与关键帧抽取模块后进入统一知识库链路。当前版本尚未实现独立视频解析服务，故不纳入已完成能力统计。

#### 4.6.3 多模态内容入库与问答引用支持

当前已实现的多模态能力集中在复杂文档与图像附件两个方向。资料入库阶段，本地解析器会将结果统一组织为 `text`、`table`、`equation`、`figure` 四类内容项；增强路径则通过 RAG-Anything 调用 MinerU 完成复杂文档解析，并将 `raganything_status` 与 `raganything_quality` 等质量标记回写数据库，用于后续诊断与分析。

图像问答阶段采用“图像描述文本化”策略。对图像附件 $img$，视觉语言模型先生成描述文本 $d_{vlm}$；若解析链路能够获得可读文本，则记为 $d_{txt}$。两者按早期融合方式合并为
$$
d_{multi}=[d_{vlm};d_{txt}],
$$
再与原始查询共同进入检索流程。该策略将图像理解结果映射为统一文本语义空间，便于复用现有检索与生成链路。

VLM 提示词采用直接任务指令形式，要求模型抽取题干内容、可见文本、图示结构以及求解线索。该设计接近受控的信息抽取，而非开放式图像描述。生成后的图像语义描述会写入问答上下文，随后与文本证据一起进入回答生成与引用落库流程。

#### 4.6.4 当前实现边界与扩展方向

多模态能力的已实现部分包括：复杂文档增强解析入口、图像附件 VLM 理解、表格与公式内容项抽取、多模态质量标记落库以及图像描述参与检索。尚待扩展的部分包括音频 ASR、视频关键帧抽取、更细粒度的版面块级结构持久化以及更强的跨模态对齐机制。

当前实现状态可概括为：文档与图像场景已形成可运行链路，音视频场景已预留扩展接口。

### 4.7 本章小结

本章围绕课程资源管理、检索增强问答、教师审核回流、学习分析、前后端交互以及多模态处理六个方面，对平台关键模块的实现链路进行了展开。章节重点不在界面功能罗列，而在于说明资料如何进入知识空间、证据如何被检索与重排、教师修正如何写回知识库、学习行为如何转化为画像与报告，以及多模态信息如何并入统一检索流程。

从实现结果看，平台已经形成较完整的课程知识处理主链：资料上传后经过去重、解析、索引与状态维护进入知识空间；用户查询经过候选召回、重排序与回答生成后输出可追踪引用；低置信结果与负反馈进入教师审核，再以增量方式回流知识库；行为日志进一步支撑学习报告与个性化支持。上述实现为下一章的系统测试、性能评估与实验分析提供了基础。

## 第五章 系统测试与结果分析

第五章的目标是验证平台原型是否已经形成可运行、可观测、可纠偏的完整业务链路。测试内容不局限于单个页面或单个接口，而是围绕课程资料上传、知识库构建、检索增强问答、教师审核回流、学习分析与管理观测等关键流程展开。分析依据主要来自三个来源：一是基于 `pytest + FastAPI TestClient` 的自动化集成测试；二是围绕典型业务链路执行的补充功能验证；三是系统内置的指标采集与离线实验报告导出能力。

### 5.1 实验环境与部署配置

#### 5.1.1 软件与硬件环境

本章测试基于当前开发机与本地运行环境完成。硬件与软件配置如表 5-1 所示。

表5-1 系统测试环境配置表

| 类别 | 配置项 | 说明 |
| --- | --- | --- |
| 操作系统 | Microsoft Windows 11 家庭中文版 | 版本 `10.0.26200` |
| 处理器 | AMD Ryzen 7 6800H with Radeon Graphics | 8 核移动平台处理器 |
| 内存 | 15.19 GB | 本地开发机物理内存 |
| Python | 3.11.7 | 后端运行与测试环境 |
| Node.js | v22.14.0 | 前端构建与本地运行环境 |
| 前端框架 | React 19 + Vite + TypeScript + Tailwind CSS | 页面实现与构建 |
| 后端框架 | FastAPI + SQLAlchemy | 接口与数据访问层 |
| 异步组件 | Redis + Celery | 异步索引与任务调度 |
| 数据库存储 | SQLite | 开发测试环境默认数据库 |
| 对象存储 | 本地文件系统 / MinIO | 当前测试以本地存储为主 |

当前环境以开发验证为目标，重点检验流程完整性和模块协同，不以 GPU 推理吞吐或分布式部署能力作为主要考察对象。

#### 5.1.2 系统运行配置

测试阶段采用项目默认开发配置。关系数据库使用 `sqlite:///./ai_tutor.db`，Redis 默认地址为 `redis://localhost:6379/0`，Celery broker 与结果后端分别使用 Redis 的 1 号库和 2 号库。对象存储支持本地目录与 MinIO 两类后端，本轮测试以本地文件存储为主，MinIO 作为可切换部署选项保留。

知识处理配置方面，增强解析链路采用 `parser=mineru`、`parse_method=auto`、`query_mode=mix`；本地检索链路默认候选片段数为 `RAG_RETRIEVAL_CANDIDATE_K=12`，回答证据截断数为 `RAG_ANSWER_TOP_K=3`，低置信审核阈值为 $\theta_c=0.7$。知识库任务调度层采用 30 s 固定冷却时间，解析最大尝试次数为 2，自动重试最大轮数为 2。上述配置保证了测试过程能够同时覆盖同步索引、异步入队、失败重试与回退问答等典型路径。

#### 5.1.3 测试方法与测试依据

本章采用黑盒功能测试、接口集成测试、典型案例分析与评估方案设计相结合的方法。

1. 接口集成测试以 `pytest` 和 `FastAPI TestClient` 为基础，结合 `seed_data()` 初始化的示例教师、学生与管理员数据，对关键接口进行端到端验证。
2. 功能测试围绕课程问答主链展开，检查“上传-索引-检索-回答-审核-回流-分析”是否能够顺序完成。
3. 典型案例分析用于展示代表性业务闭环，包括资料入库、引用问答、教师修正与动态知识更新。
4. 用户评估部分当前以评估设计为主，问卷与访谈数据留待后续实验补充。

代表性自动化测试共选取 6 组测试文件，覆盖知识库、问答、审核、学习分析、通知与管理接口，共执行 16 个测试用例，结果全部通过，执行时间为 11.12 s。若以测试通过率表示整体功能验证结果，则

$$
PassRate=\frac{N_{pass}}{N_{total}}=\frac{16}{16}=100\%.
$$

这一结果表明，所选关键流程在当前原型环境下能够稳定运行。

### 5.2 系统功能测试

#### 5.2.1 用户与课程管理功能测试

用户与课程管理测试主要验证认证鉴权、课程列表获取、课程详情访问以及管理端配置接口。测试结果表明，教师、学生和管理员均能够通过统一认证接口完成登录；教师和学生均可访问各自课程列表与课程详情；管理员可访问平台概览、模型配置、模型路由、实验结果、检索消融与个性化路由指标接口。

在管理侧验证中，模型配置接口支持读写操作，路由快照接口能够返回生成模型、嵌入模型、视觉模型与重排序模型的当前配置状态。这说明管理端已经具备运行时观察与配置调整能力，可为后续实验比较提供支撑。

#### 5.2.2 资料上传与知识库构建功能测试

资料管理与知识库构建测试重点验证资料上传、重复检测、解析任务追踪、异步入队、重试调度和图谱输出。测试结果显示，教师上传相同字节内容的文本资料两次时，第二次上传返回 `deduplicated=True`，且复用第一次上传生成的 `file_id` 与 `parse_task_id`，说明 SHA-256 去重机制已经生效。

在异步索引路径下，上传接口可返回 `queue_task_id`，任务详情接口能够查询该任务的队列标识与状态；管理员端可进一步访问索引队列状态接口与队列指标接口，查看 `totals`、`queue`、`retry` 和 `latency_ms` 等指标。相关测试同时验证了强制重试、异步重试、冷却窗口控制和失败告警逻辑。对多模态内容项的分析接口返回 `content_items_schema = v1`，并能输出规范化后的 `text`、`table`、`formula`、`image` 等模态信息。

#### 5.2.3 AI问答与审核回流功能测试

问答功能测试围绕“带来源问答”和“低置信审核触发”两类场景进行。上传包含 TCP 慢启动说明的资料后，学生以“`What is slow start in TCP?`”发起提问，接口能够返回带 `sources` 的回答结果，说明资料检索、证据组织与引用落库链路可以正常工作。

当学生输入低信息量问题“`??`”时，接口返回结果被标记为 `needs_review=True`，教师端可在待审核列表中观察到对应审核项。进一步补充执行的功能验证表明，教师提交修正答案并选择写回知识库后，接口返回 `status=resolved` 且 `sync_status=synced`，同步说明为“教师答案已同步到回退知识库”。该结果表明，审核状态迁移与知识回流链路能够在当前原型中闭合。

#### 5.2.4 闪卡、推荐与分析功能测试

学习支持测试包括闪卡复习、学生画像、课程统计、周报月报、通知与错题本等功能。测试结果显示，学生能够获取闪卡列表并提交复习反馈，接口返回的 `review_count` 会递增；学生画像接口能够返回 `activity_score`；课程分析接口能够返回 `question_count` 与 `task_completion_rate` 等核心指标。

在报告与推荐功能方面，周报、月报与 CSV 导出接口均能正常返回数据，其中周报结果包含 `summary`、`experiment_context.model_routing` 与 `highlights.recommendation_context` 字段，说明学习报告已经与模型路由和推荐上下文打通。通知发布、错题新增、错题标记掌握与再次练习接口也已通过验证。

表5-2 系统主要功能测试结果汇总表

| 测试模块 | 关键验证点 | 结果 |
| --- | --- | --- |
| 用户与课程管理 | 登录、课程列表、课程详情、管理端配置接口 | 通过 |
| 资料上传与知识库构建 | 文件上传、SHA-256 去重、解析任务、异步入队、图谱输出 | 通过 |
| AI问答与审核回流 | 带来源回答、低置信审核触发、教师修正同步 | 通过 |
| 学习分析与推荐支持 | 闪卡复习、学生画像、课程统计、周报月报、CSV 导出 | 通过 |
| 管理与实验观测 | 索引队列指标、RAG 性能、检索消融、个性化路由统计 | 通过 |

### 5.3 典型案例分析

#### 5.3.1 课程资料上传与知识库生成案例

以课程资料 `tcp_notes.txt` 为例，文件内容包含“TCP slow start increases cwnd quickly.”。教师上传资料后，接口返回 `parse_task_id`，随后课程知识库状态接口与图谱接口均可正常访问，解析任务详情显示任务类型为 `file_parse`。图谱结果中，节点与边均带有 `confidence`、`source_span` 和 `provenance` 字段，说明课程资料已经从原始文件转化为可追踪的知识片段与关系结构。

该案例验证了“原始资料-解析任务-知识空间-知识关联原型”的完整生成路径。对于后续检索问答而言，这一步保证了回答不再直接依赖通用模型记忆，而是能够建立在课程资料的局部证据之上。

#### 5.3.2 学生提问与引用回答案例

在资料入库完成后，学生以“`What is slow start in TCP?`”发起提问。问答接口返回 200 状态码，回答结果中包含 `sources` 字段，说明系统已经完成候选召回、证据组织和引用输出。由于查询内容与已上传资料主题高度相关，返回结果能够由课程资料提供证据支撑，而不是生成无来源的自由回答。

该案例表明，课程边界约束已经进入问答主链。对教学场景而言，回答附带引用来源比仅输出自然语言段落更有利于教师核验与学生追溯。

#### 5.3.3 教师审核与知识回流案例

当学生提交低信息量问题“`??`”时，问答结果被自动标记为待审核。教师随后通过待审核接口读取审核项，并提交修正答案“请补充具体课程知识点后再提问”。实际运行结果显示，审核提交接口返回 `status=resolved`、`sync_status=synced`，说明该修正答案已经成功写回知识库。

该案例验证了审核模块并非单纯的人工备注机制，而是能够把教师修正结果转化为后续可复用的知识条目。由此，问答链路与教师纠偏链路形成了持续优化关系。

#### 5.3.4 动态知识更新案例

动态知识更新可从两类场景观察。第一类是重复资料上传场景：当相同内容资料再次上传时，系统返回 `deduplicated=True`，并复用已有任务标识，避免重复构建。第二类是增量资料扩充场景：针对两份相关资料执行连续入库后，图谱节点的 `provenance.source_material_ids` 可同时包含两份资料标识，节点置信度保持大于 0，关系边同样保留来源与置信度信息。

由此可见，知识更新并不依赖“删除旧库后重建”的粗粒度方式，而是通过去重、增量合并与来源累计完成。这一机制适合课程资料逐步完善、教师持续补充的教学场景。

#### 5.3.5 学习分析与推荐支持案例

学生完成一次闪卡复习后，系统返回更新后的 `review_count`、`interval_days` 与 `next_review_at`；随后访问学生画像接口，可获得 `activity_score` 等画像指标；访问周报接口，则可获得 `summary`、`recommended_focus` 与 `recommendation_context`。同时，教师发布课堂通知后，学生端通知列表能够及时收到消息。

该案例表明，学习支持模块已经能够将问答、复习、通知与报告统一纳入同一数据链路。学习行为不再是孤立事件，而是能够持续沉淀为个体画像与推荐输入。

### 5.4 性能与稳定性分析

#### 5.4.1 接口响应与主流程执行情况

当前原型尚未进行大规模并发压测，因此本节不虚构高并发吞吐指标，而是基于代表性功能运行结果与内置观测指标进行分析。选取的 16 个代表性集成测试覆盖登录、课程访问、资料上传、问答、审核、报告、通知与管理接口，全部通过，整体执行时间为 11.12 s。该结果说明，在开发环境下关键业务接口能够稳定完成调用，主流程不存在阻塞性错误。

与此同时，管理端提供 `/api/v1/admin/rag-performance` 接口，用于统计查询数量、成功率、回退率、响应时延和不同路由切片下的分布信息；离线实验脚本 `run_research_benchmark.py` 可进一步导出 JSON 与 Markdown 格式实验报告，并支持在有标注集时计算检索评测结果。因而，平台不仅能运行，还具备了后续性能实验的数据采集基础。

#### 5.4.2 解析任务状态流转情况

知识库解析任务围绕 `pending`、`processing`、`completed`、`failed` 四个核心状态流转。异步上传测试表明，上传接口能够返回 `queue_task_id`，任务详情接口可以查询排队状态，管理员端能够进一步获取队列状态与延迟统计。冷却窗口、自动重试和失败告警相关测试均已通过，说明队列调度层具备基本可恢复性。

从稳定性角度看，固定 30 s 冷却时间和最大尝试次数约束可避免失败任务在短时间内频繁重复执行；管理员告警去重逻辑则避免相同任务在相同失败轮次下重复推送通知。该机制虽然尚未达到生产级任务编排复杂度，但对于课程原型系统已经具备足够的过程可观测性。

#### 5.4.3 批量资料处理与稳定性分析

批量资料处理能力主要体现在三个方面。第一，SHA-256 去重机制能够降低重复上传造成的冗余索引开销。第二，异步队列与同步索引两种路径并存，使小文件能够快速入库，大文件和批量文件则可交由后台处理。第三，RAG-Anything 增强路径不可用时，系统能够自动回退到本地回退引擎，从而维持问答主流程可用。

当前稳定性分析基于原型环境与功能测试结果，尚未覆盖高并发用户访问、海量 PDF 批量入库或长时间在线运行等更严格场景。因此，本节结论应理解为“平台原型已具备稳定完成关键业务流程的能力”，而非“已完成生产级性能验证”。

### 5.5 用户评估与实用性分析

#### 5.5.1 用户评估设计

用户评估部分当前以评估设计为主，真实问卷与访谈数据留待后续实验补充。评估对象拟包括学生用户和教师用户两类，其中学生主要体验课程问答、学习报告、错题与闪卡功能，教师主要体验资料上传、审核回流、学情查看与管理功能。

评估流程可设计为四个步骤：一是向受试者说明平台功能与使用任务；二是安排其完成指定任务，如上传资料、提出课程问题、查看报告或处理审核项；三是填写结构化问卷；四是对部分用户进行半结构化访谈，以补充定性意见。该设计既能够得到量化评价，也能够收集具体改进建议。

#### 5.5.2 评估指标与维度

评估指标可分为功能性、易用性、实用性和满意度四个维度，建议采用 5 级 Likert 量表进行评分。单项指标均值可表示为

$$
\bar{x}=\frac{1}{n}\sum_{i=1}^{n}x_i,
$$

其中 $x_i \in \{1,2,3,4,5\}$ 为第 $i$ 名受试者的评分。建议重点考察如下指标。

表5-3 用户评估指标说明表

| 维度 | 评价指标 | 说明 |
| --- | --- | --- |
| 功能性 | 回答相关性、资料引用完整性、审核流程有效性 | 衡量功能是否满足课程任务 |
| 易用性 | 页面理解难度、操作步骤清晰度、学习成本 | 衡量交互复杂度 |
| 实用性 | 对答疑效率的帮助、对复习支持的帮助、对教师管理的帮助 | 衡量教学支持价值 |
| 满意度 | 整体满意度、再次使用意愿、推荐意愿 | 衡量原型接受度 |

除问卷外，还可统计任务完成时间、完成率和错误操作次数等客观指标，以增强评估可信度。

#### 5.5.3 预期评估结果与分析

从平台当前实现情况看，预期学生用户会对“带来源课程问答”“周报月报生成”“错题与闪卡管理”给出较高评价，教师用户会对“资料上传后可形成知识库”“低置信回答可进入审核”“修正答案可回流知识空间”给予肯定。相对而言，多模态深度能力、图谱精细度和推荐准确性可能成为评价波动较大的部分。

后续若完成正式评估，可采用均值、标准差和分角色对比的方式分析结果，并将定性访谈意见与量化评分结合解释。当前阶段更适合将本节定位为“评估方案已设计，待实验补充数据”的状态。

### 5.6 系统局限性分析

#### 5.6.1 知识图谱构建仍偏启发式

当前课程知识关联原型主要依赖关键词抽取与共现关系构建，实体置信度和关系权重也采用启发式融合方式更新。该方法能够满足课程知识关联展示与图辅助检索的基本需求，但尚未引入更严格的课程本体约束、实体消歧策略和深层图推理能力。

#### 5.6.2 多模态处理仍处于基础支持阶段

当前多模态能力已经覆盖复杂文档增强解析、图像附件理解、表格与公式内容项抽取，但音频 ASR、视频关键帧抽取和更细粒度的版面结构建模尚未完成。因而，多模态支持仍应界定为“文档与图像能力已形成基础链路”，而不是完整覆盖全部教学资源形态。

#### 5.6.3 RAG增强路径与本地回退路径并存

增强路径能够提供更强的复杂文档与多模态处理能力，但其运行依赖解析组件、模型配置与外部包环境；当增强路径不可用时，系统会自动回退到本地回退引擎。双路径设计提高了稳定性，但也意味着不同运行条件下的回答质量和多模态深度可能存在差异，后续仍需进一步统一评测基准。

#### 5.6.4 推荐与用户评估仍有进一步完善空间

当前推荐主要基于规则引擎与学生画像生成，尚未引入独立排序模型或长期反馈学习机制。用户评估部分也仍处于方案设计阶段，缺少更大样本量的问卷和访谈结果支持。因此，推荐精度与实用性结论仍需结合后续实验进一步验证。

### 5.7 本章小结

本章从实验环境、功能测试、典型案例、性能与稳定性分析、用户评估设计以及系统局限性六个方面，对平台原型进行了系统验证。测试结果表明，课程资料上传、知识库构建、带引用问答、低置信审核、教师回流、学习报告与管理观测等关键链路已经能够在当前环境下稳定运行；代表性自动化测试 16 项全部通过，说明主要功能具备较好的可验证性。

与此同时，分析结果也表明，平台当前仍属于面向课程场景的原型系统：其优势在于主流程完整、反馈链路清晰、增强路径与回退路径并存；其不足在于知识图谱仍偏启发式、多模态能力尚未全面展开、用户评估数据仍需补充。这些结论为第六章的总结与展望提供了直接依据。

## 第六章 总结与展望

### 6.1 全文总结

#### 6.1.1 主要工作总结

研究工作围绕“基于大语言模型和检索增强生成的助教平台设计”展开，针对课程资料分散、通用模型缺乏课程边界约束、回答证据不可追踪以及错误结果缺少持续修正机制等问题，完成了课程场景下 AI 助教平台的需求分析、总体架构设计、关键模块实现与原型验证。技术路线以“大语言模型 + RAG + 教师反馈回流”为主线，在保证课程知识约束的前提下，将资料入库、证据检索、答案生成、人工纠偏与学习支持组织为统一的数据链路。

架构层面，平台采用前后端分离设计，前端基于 React、Vite 与 TypeScript 实现学生、教师、管理员三类入口，后端基于 FastAPI、SQLAlchemy、Celery 与 Redis 组织业务逻辑与异步任务。知识处理层采用“双路径”结构：一条路径以 RAG-Anything 为增强型多模态 RAG 主链，负责复杂文档解析与多模态检索；另一条路径以本地回退引擎为稳定支撑，保证在增强环境不可用时仍可完成基础课程问答。

模块实现层面，研究工作重点完成了以下内容：一是实现课程资料上传、SHA-256 去重、任务调度、状态追踪与增量索引更新；二是实现面向课程边界的检索增强问答链路，使回答能够关联来源证据与置信度；三是构建课程知识关联原型，用于图辅助检索与图结构展示；四是实现教师审核、修正答案写回与知识回流机制；五是实现学习行为采集、学生画像、周报月报、错题与闪卡等学习支持能力；六是完成面向平台运维的模型路由配置、索引队列监控与性能指标采集接口。

#### 6.1.2 系统实现成果总结

从原型实现结果看，平台已经形成较完整的课程知识服务闭环。课程资料在上传后能够经过存储、解析、切分、内容项归一化与知识空间更新进入课程知识库；用户查询能够经过候选召回、重排序、回答生成与引用落库输出可追踪结果；低置信回答与负反馈可进入教师审核队列，审核后又能以增量方式写回知识空间；学习行为日志进一步支撑学生画像、学习报告与规则推荐。

测试与结果分析表明，原型环境下关键业务流程已经能够稳定运行。代表性自动化测试全部通过，说明用户认证、课程访问、资料上传、知识库构建、带来源问答、教师审核回流、学习报告与管理观测等核心能力具备可验证性。更重要的是，平台并未将大语言模型作为孤立问答器使用，而是将其置于课程知识检索、证据约束与人工纠偏机制之中，从而提升了课程场景下的可控性与可追踪性。

### 6.2 不足分析

#### 6.2.1 当前系统不足

尽管平台已经完成主流程原型，但仍存在若干技术层面的不足。第一，课程知识关联原型主要依赖关键词抽取与共现关系构建，关系类型仍较单一，尚未形成具备课程本体约束、实体消歧与深层语义关系推断能力的成熟知识图谱。第二，多模态能力当前主要覆盖复杂文档与图像场景，音频转写、视频关键帧抽取以及更细粒度的版面块级结构建模仍处于扩展预留阶段。第三，本地回退引擎的检索以词法、图扩展和可选重排序为主，尚未接入独立向量数据库，在资料规模持续扩大时，检索效率与语义召回能力仍有提升空间。第四，个性化推荐目前以规则驱动为主，尚未形成基于长期行为数据的自适应推荐模型。

此外，增强路径与回退路径并存虽然提高了工程稳定性，但也引入了结果一致性问题。不同运行条件下，多模态解析深度、检索粒度与回答质量可能出现差异，后续需要进一步统一路径间的评估基准与质量控制方式。

#### 6.2.2 当前论文工作的局限

论文工作的局限主要体现在实验规模、数据条件与评估范围三个方面。第一，实验环境仍以开发机与原型部署为主，验证重点是流程完整性与模块协同，而非生产级高并发性能。第二，测试结果主要来源于自动化集成测试、代表性案例与平台内置指标采集，尚未建立覆盖更大规模课程资料与更复杂用户行为的长期实验数据。第三，用户评估部分目前完成了量表设计与评估方案规划，但缺少真实课堂场景下更大样本量的问卷、访谈与学习效果对比结果。

因此，当前结论更适合界定为“平台原型已验证关键链路可行”，而不宜外推为“已完成成熟教学平台的全面实证验证”。这一界定能够保持研究结论与实现状态的一致性。

### 6.3 未来工作展望

#### 6.3.1 更真实的ASR与VLM接入

后续可在现有多模态入口基础上进一步接入更真实的 ASR 与 VLM 能力，将课堂录音、录播视频、实验演示和图像题目统一纳入课程知识处理链路。对于音频与视频资料，可通过语音转写、时间戳切分与关键帧抽取构建跨时间轴的可检索知识单元；对于图像资料，可通过更强的视觉语言模型提升图示理解、公式识别与结构化描述质量。

#### 6.3.2 更强的向量检索与重排序机制

当前回退路径尚未引入独立向量数据库，未来可进一步接入 `pgvector`、Milvus 或 FAISS 等向量检索组件，将语义嵌入检索与当前词法检索、图扩展检索进行更深度融合。同时，可引入效果更强的重排序模型，构建“粗召回 - 精排”两阶段检索架构，以提升大规模课程资料场景下的召回率、排序精度与响应稳定性。

#### 6.3.3 更成熟的知识图谱抽取与更新机制

知识图谱部分可从启发式关键词共现升级为基于大语言模型或信息抽取模型的实体关系抽取机制。后续可引入课程本体、实体消歧、关系类型约束和时序版本管理，使图谱不仅能够表示“相关”，还能够表达“先修”“包含”“应用”“因果”等更具教学意义的语义关系。同时，可将教师审核结果与新资料入库结果共同纳入图谱增量更新策略，形成更加稳定的课程知识演化机制。

#### 6.3.4 更系统的离线评测与用户研究

评测层面可进一步构建课程问答标注集、检索评测集与多模态样例集，并采用 Recall@k、MRR、命中率、引用正确率、回答一致性与人工评分等多维指标开展系统化离线评估。用户研究层面可在真实课堂环境中组织学生与教师试用，结合问卷、访谈、行为日志与学习效果变化开展综合评估，从而使系统实用性结论建立在更充分的实证数据基础上。

#### 6.3.5 模型性能迭代优化方向

教师审核与用户反馈已经形成可积累的数据来源，后续可将这些高质量修正样本整理为偏好优化数据、问答数据或重排序训练样本，用于提示词优化、模型蒸馏、轻量微调与路由策略更新。同时，还可联合考虑准确率、时延与调用成本，构建面向课程场景的动态模型路由机制，使不同问题类型自动匹配更合适的生成模型、嵌入模型与视觉模型。

总体来看，现有平台已经验证了“大语言模型 + 检索增强生成 + 教师反馈回流”在课程教学辅助场景中的可行性。后续工作若能在多模态接入、检索能力、知识图谱质量、评测体系与模型优化机制上持续推进，平台将更接近真实教学场景中的长期运行要求。
