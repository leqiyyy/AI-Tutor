下面按**本科毕业论文（系统设计/实现类）\**标准给你逐章改。按这个口径看，你的论文不需要达到硕士论文那种“方法创新+严密对比实验”的强度，但必须把\**系统目标、需求分析、架构设计、关键实现、测试验证、结论边界**写得一致、清楚、可信。你现在最大的问题不是“做系统不行”，而是**摘要、意义和结论中用了偏研究型、偏效果证明型的表述，但第五章实际主要是功能测试，支撑不了“提高准确性、验证有效性、保证不丢失多模态数据”等较强结论**。你的摘要目前直接写到“提高检索增强生成的准确性”“分析系统对教育场景问答的有效性”“保证多模态数据不丢失”，但第五章主要列的是功能模块测试和页面结果展示。

------

## 总体修改方向

本科论文可以定位为：

> **本文设计并实现了一个面向高校课程场景的AI助教原型系统，系统集成大语言模型、检索增强生成、多模态资料解析、课程知识图谱和教师审核回流机制，实现了课程资料管理、知识库构建、课程问答、引用溯源和反馈修正等功能，并通过功能测试验证系统主要模块能够正常运行。**

这个定位是安全的。不要再把自己包装成“提出新方法”“证明提升准确性”“保证多模态信息不丢失”。本科系统论文可以有技术先进性，但不要把**使用现有框架**写成**理论贡献**。你的题目本身也是“本科毕业论文（设计）”和“AI助教设计”，所以按系统设计论文处理是合理的。

------

# 摘要与英文摘要修改建议

## 当前问题

摘要目前有三个风险点。

第一，摘要中“提高课程答疑的及时性、准确性与迭代优化能力”可以保留，但需要改成“提升系统支持能力”而不是直接证明“准确性提高”。第二，摘要中“引入基于图的检索增强生成技术，提高检索增强生成的准确性和知识点关联性”表述过强，因为第五章没有提供准确率、召回率、证据命中率等数据。第三，“跨模态图构建机制也保证了检索增强生成过程中不丢失重要的多模态数据”风险很高，因为你没有做多模态信息保真度实验，不能用“保证”。

## 修改目标

把摘要从“研究验证型”改成“系统实现型”。摘要应回答四件事：
**为什么做、做了什么、怎么做、测试结果是什么。**

## 建议改法

摘要中的强结论建议替换如下：

原表达：

> 引入了基于图的检索增强生成技术，提高检索增强生成的准确性和知识点关联性。

建议改为：

> 系统在检索增强生成流程中引入图结构索引与向量检索相结合的方式，用于支持课程知识点之间的关联检索和证据组织。

原表达：

> 分析了系统对教育场景问答的有效性。

建议改为：

> 通过功能测试验证了系统在资料上传、知识库构建、知识图谱展示、课程问答、引用溯源和教师审核回流等环节的可用性。

原表达：

> 保证了检索增强生成过程中不丢失重要的多模态数据。

建议改为：

> 系统通过将图片、表格、公式等内容转换为文本化描述，为多模态课程资料参与检索提供了实现路径。

英文摘要也要同步降调。尤其是 “The results demonstrate the effectiveness...” 建议改成：

> The test results show that the main functional modules can operate as expected in the constructed course scenario.

------

# 第一章 绪论修改建议

## 当前主要问题

第一章整体方向是对的，背景能够说明课程资料分散、教师答疑压力、通用大模型幻觉和不可溯源等问题，也能自然引出RAG和知识图谱。文中已经把课程资料、结构化知识和教师补充内容作为回答依据，这个逻辑成立。

但第一章存在两个明显问题。

第一，**“研究目的”写得过宽**。你写了课程知识库、RAG问答、知识图谱、教师审核回流、学生画像和个性化推荐五个技术模块，但后文真正展开最充分的是知识库、图谱、问答和审核，学习画像/推荐更像辅助功能。

第二，**“理论意义”表述不适合本科系统论文**。你现在写“在理论层面具有重要意义”，并说验证了RAG-Anything复杂文档解析与跨模态检索的适用性。这个说法容易被质疑：你没有独立理论建模，也没有严格实验验证，所以不宜强调“理论层面”。

## 具体修改建议

### 1.1 研究背景

保留现有背景，但建议增加一段“本文系统要解决的具体问题”，不要只写宏观背景。建议写成三点：

1. 课程资料格式多，包括PDF、PPT、图片、表格和公式，普通关键词检索难以覆盖；
2. 学生问题重复度高，教师答疑内容难以复用；
3. 通用大模型回答缺少课程边界和引用来源，不能直接用于课程答疑。

这一段要直接服务你的系统设计，不要扩展到泛泛的智慧教育。

### 1.2 国内外研究现状

你现在的研究现状分成LLM教育应用、RAG课程问答、多模态教学资源解析三部分，结构可以保留。但每一节最后必须加一小段**归纳不足**，否则会像文献堆砌。

建议每节末尾增加这种句式：

> 综上，现有LLM教育应用能够支持自然语言交互和教学辅助，但在课程资料约束、回答依据展示和教师可控审核方面仍存在不足，因此需要结合课程知识库和人工审核机制进行约束。

> 综上，RAG技术能够缓解通用大模型的知识边界和事实依据问题，但普通向量检索对课程知识点之间的结构关系表达不足，因此本文在系统中引入图结构索引和知识图谱展示功能。

> 综上，多模态资料解析能够扩展系统对课件、图片、公式和表格的支持范围，但在实际教学系统中仍需要将多模态内容转换为可检索、可引用的知识单元。

这样第一章的文献综述就能自然导向你的系统，而不是单纯罗列论文。

### 1.3 研究目的与意义

建议把“研究目的”改成“设计目标”。本科系统论文中，“研究目的”可以保留，但内容必须贴近系统。

建议结构改为：

> 本文的目标是设计并实现一个面向高校课程场景的AI助教原型系统。系统应满足以下目标：
> （1）支持课程班级管理、资料上传、任务通知等基础教学管理功能；
> （2）支持课程资料解析、知识库构建和资料状态管理；
> （3）支持基于课程知识库的检索增强问答，并展示引用来源；
> （4）支持课程知识图谱展示，辅助学生浏览知识结构；
> （5）支持学生反馈、教师审核和修正答案回流，实现问答内容的持续维护。

注意：**个性化推荐建议不要作为核心目标**。你可以把它放到辅助功能或展望中，因为实现和测试都不够充分。

“研究意义”建议改成“应用意义”和“技术实践意义”。不要写“理论层面具有重要意义”。可改为：

> 在应用意义上，系统为高校课程答疑提供了一个可运行的AI助教平台原型。
> 在技术实践意义上，系统将RAG-Anything、知识图谱、向量检索和教师审核机制集成到课程问答流程中，为同类教学辅助系统的设计与实现提供参考。

这样更符合本科论文口径。

### 1.4 论文组织结构

这里有一个必须修正的问题：你在组织结构中写第四章包括“学习分析与个性化支持模块”，但目录和第四章核心小节只到教师审核与知识回流，学习分析没有作为独立小节充分展开。

建议两种处理方式二选一：

**方案A：弱化学习分析。**
把1.4中第四章描述改为：

> 第四章介绍AI助教平台关键模块的设计与实现，包括班级空间基础功能、课程知识库构建、知识图谱构建、检索增强生成问答、教师审核与知识回流等模块。

**方案B：补充学习分析独立小节。**
在第四章增加“4.6 学习画像与资料推荐模块实现”，第五章增加对应测试。否则前后不一致。

我建议选方案A，更稳。

------

# 第二章 相关理论与技术修改建议

## 当前主要问题

第二章内容总体可用，但现在有两个倾向：一是大语言模型原理写得偏泛，和系统实现关系不够紧；二是对LightRAG、RAG-Anything的介绍较强，容易让评审觉得你把框架说明当成自己的贡献。你的第二章中确实详细介绍了LightRAG的双路检索和RAG-Anything多模态扩展，这对后文有帮助，但必须明确它们是**系统技术基础与选型依据**，不是本文原创方法。

## 具体修改建议

### 2.1 大语言模型相关技术

建议删减“代表性模型、涌现能力”这类泛泛内容，把重点放在三点：

1. 本系统为什么需要LLM；
2. 本系统中LLM承担哪些任务；
3. LLM为什么不能单独作为课程问答系统。

可以加一个表：

| LLM在系统中的用途 | 对应模块       | 输入                | 输出            |
| ----------------- | -------------- | ------------------- | --------------- |
| 问答生成          | AI助教问答模块 | 用户问题+检索证据   | 带引用回答      |
| 查询理解          | 问题预处理     | 用户问题+历史上下文 | 独立问题/关键词 |
| 实体关系抽取      | 知识图谱构建   | 文本块/多模态描述   | 实体、关系      |
| 图片/公式描述     | 多模态解析     | 图片、公式、表格    | 文本化描述      |

这样第二章就和第四章直接连接。

另外，“2.1.2 提示工程与与任务型问答机制”标题有明显重复，“与与”必须改成：

> 2.1.2 提示工程与任务型问答机制

### 2.2 检索增强生成技术

你可以保留RAG基本流程，但要加一句边界：

> 本文并未对RAG算法本身进行理论改进，而是在课程场景下对RAG流程进行系统集成与应用适配。

这句话很重要。它能避免评审按算法论文要求追问创新性。

### 2.2.2 LightRAG 与 2.2.3 RAG-Anything

建议增加一个“技术选型理由表”：

| 技术         | 选择原因                     | 在本文中的作用             | 局限                 |
| ------------ | ---------------------------- | -------------------------- | -------------------- |
| LightRAG     | 支持图结构检索和向量检索     | 提供双路检索基础           | 依赖实体抽取质量     |
| RAG-Anything | 支持复杂文档和多模态内容解析 | 处理课件、图片、表格、公式 | 依赖解析器和模型API  |
| MinerU       | 支持文档版面、公式、表格解析 | 文档解析                   | 复杂版面可能失败     |
| Qdrant       | 支持向量存储和召回           | 语义检索                   | 需要维护索引         |
| Neo4j        | 支持图谱存储和查询           | 知识图谱展示               | 图谱质量依赖抽取结果 |

这个表对本科论文非常有用，能体现你知道自己为什么选这些技术。

### 2.3 知识图谱技术

现在知识图谱部分较概念化。建议补充“课程知识图谱的实体和关系类型”。

例如：

| 实体类型 | 示例                    |
| -------- | ----------------------- |
| 概念     | TCP、拥塞控制、滑动窗口 |
| 算法     | Dijkstra算法、慢启动    |
| 公式     | 吞吐量计算公式          |
| 资源     | 某页课件、某张图        |
| 例题     | 课件中的计算题          |

| 关系类型     | 含义                   |
| ------------ | ---------------------- |
| contains     | 章节包含知识点         |
| prerequisite | 前置依赖               |
| related_to   | 相关概念               |
| explains     | 资料解释某概念         |
| derived_from | 公式或结论来源于某资料 |

这样第三章和第四章的知识图谱实现就会更具体。

### 2.4 本章小结

目前第二章小结过短，只写“本章系统梳理……”。建议改成：

> 本章介绍了系统实现所依赖的大语言模型、检索增强生成、LightRAG、RAG-Anything和知识图谱技术。大语言模型用于问题理解、实体关系抽取和答案生成；RAG用于将课程资料作为回答依据；LightRAG和RAG-Anything为图结构检索和多模态资料解析提供支撑；知识图谱用于课程知识结构展示和关系扩展。上述技术共同构成后续AI助教平台的技术基础。

------

# 第三章 需求分析与总体设计修改建议

## 当前主要问题

第三章是你目前相对较强的一章。用户角色、功能需求、非功能需求、页面设计、系统架构和核心流程都有覆盖。第三章已经说明系统分为学生端、教师端、管理端，并且设计了班级空间、课程知识库、知识图谱、检索问答和教师审核回流等模块。

但第三章还存在三个问题。

第一，功能需求表太“宽”，包含学习记录、错题本、闪卡、推荐、报告等功能，但后文并没有都充分实现和测试。第二，非功能需求写到了可观测性指标，例如检索耗时、生成耗时、置信度、来源数量等，但第五章没有系统报告这些指标。第三，系统总体架构中技术组件很多，但缺少“数据流”和“权限边界”说明。

## 具体修改建议

### 3.1 用户角色与应用场景分析

保留学生、教师、管理员三类用户，但建议对管理员降权。管理员不是核心，写太多会分散重点。

建议改为：

> 本系统以学生和教师为主要用户，管理员主要负责用户管理、课程管理和系统配置，不作为本文关键实现对象。

这样第四章和第五章不需要花大量篇幅解释管理员功能。

### 3.2 功能需求分析

建议把功能分成**核心功能**和**辅助功能**。

核心功能包括：

1. 用户认证与班级管理；
2. 课程资料上传与知识库构建；
3. 课程知识图谱展示；
4. 基于RAG的AI问答与引用溯源；
5. 学生反馈与教师审核回流。

辅助功能包括：

1. 通知与任务；
2. 学习记录；
3. 学习画像；
4. 推荐与闪卡；
5. 管理端配置。

这样后面测试也能分层，避免功能太散。

你现在表3-1里“学习支持”包含学习记录、错题本、闪卡、推荐、报告，但第四章和第五章对这些功能展开不足。建议删掉“错题本、报告”，保留“学习记录、学习画像、资料推荐”即可。

### 3.3 非功能需求分析

这节建议不要只写自然语言，增加表格：

| 非功能需求 | 设计措施                         | 验证方式        |
| ---------- | -------------------------------- | --------------- |
| 可用性     | 前端状态提示、上传进度、引用展示 | 页面功能测试    |
| 可靠性     | 解析失败记录、任务重试、错误提示 | 异常用例测试    |
| 安全性     | JWT鉴权、角色权限、班级成员校验  | 越权访问测试    |
| 可扩展性   | 模型、向量库、图数据库配置化     | 配置说明        |
| 可观测性   | 记录检索耗时、生成耗时、引用数量 | 日志截图/统计表 |

如果第五章没有测试“耗时、置信度”，第三章就不要写得太强。可以改成“系统设计上预留记录字段”。

### 3.4 功能页面设计

这节建议补一张“页面—功能—对应接口/模块”表。比如：

| 页面     | 用户      | 功能                   | 对应后端模块     |
| -------- | --------- | ---------------------- | ---------------- |
| 班级首页 | 教师/学生 | 查看班级信息           | class service    |
| 课程资料 | 教师      | 上传资料、查看索引状态 | material service |
| AI助教   | 学生/教师 | 提问、查看引用、反馈   | chat/rag service |
| 知识图谱 | 学生/教师 | 搜索节点、查看关系     | graph service    |
| 教师审核 | 教师      | 处理负反馈、回流知识   | review service   |

本科评审很看重“设计是否落地”，这个表能增强工程完整性。

### 3.5 系统总体架构设计

建议补充“系统数据流”。现在你说前端、后端、AI知识处理层、数据基础设施层，但还不够直观。建议新增一段：

> 用户问题从前端进入后端问答接口，后端根据班级权限确认访问范围，再调用AI知识处理层完成问题预处理、课程知识检索和回答生成。检索结果来自向量数据库、图数据库和业务数据库，生成答案后，系统将回答、引用来源、用户反馈和审核状态写入关系数据库。

这段能把架构讲清楚。

### 3.6 核心模块设计

这里建议统一每个模块的小节结构：

1. 模块目标；
2. 输入；
3. 处理流程；
4. 输出；
5. 异常情况。

例如“课程知识库构建模块”应写：

- 输入：教师上传文件、班级ID、文件元数据；
- 处理：权限校验、文件保存、解析任务创建、内容解析、索引构建、状态更新；
- 输出：资料记录、解析状态、向量索引、图谱节点；
- 异常：解析失败、文件格式不支持、任务超时。

这样第三章会更规范。

------

# 第四章 AI助教平台关键模块设计与实现修改建议

## 当前主要问题

第四章是本科论文的核心章。你写了较多实现细节，包括注册登录、JWT、班级创建、资料上传、解析任务、知识图谱、双路检索、重排序、教师审核回流等。整体内容够做本科系统论文。

但第四章目前有四个严重问题。

第一，章节编号错误。目录中第四章从4.5后直接跳到4.8，没有4.6、4.7。这个必须修。

第二，部分实现描述过像“代码说明”，缺少模块级抽象。比如认证、班级、任务管理写得很细，但RAG问答的关键算法流程反而不够结构化。

第三，公式残缺。原子单元形式化部分出现“本文将第 个原子单元表示为：其中， 表示……”这种变量缺失，必须修正。

第四，存在明显拼写问题，比如学生画像快照写入`tudent_profiles`，应为`student_profiles`。

## 具体修改建议

### 4.1 班级空间基础功能实现

这一节现在细节较多，建议压缩“注册登录”的普通实现，把篇幅让给AI助教核心模块。

保留内容：

- 用户注册与登录；
- JWT鉴权；
- 教师创建班级；
- 学生邀请码加入；
- 班级权限校验；
- 学习行为记录。

建议删除或压缩：

- 验证码生成过多细节；
- 每个字段如何写入表；
- 过细的REST接口描述。

建议增加一张“班级空间权限矩阵”：

| 功能     | 学生 | 教师 | 管理员 |
| -------- | ---- | ---- | ------ |
| 加入班级 | 是   | 否   | 否     |
| 创建班级 | 否   | 是   | 是     |
| 上传资料 | 否   | 是   | 是     |
| AI提问   | 是   | 是   | 否     |
| 查看图谱 | 是   | 是   | 是     |
| 审核回答 | 否   | 是   | 是     |

这比大段文字更清楚。

### 4.1 中“学生画像和资料推荐”

这个模块风险较大。你有实现说明，但它不是论文主线。建议处理方式：

- 不要放在4.1里作为基础功能的一部分；
- 若保留，单独列为“4.6 学习画像与资料推荐模块实现”；
- 若不想补测试，就改成“辅助功能实现”。

目前你在目录中第四章没有4.6，但正文里又写了学生画像和资料推荐，结构会显得乱。最稳的处理是：

> 4.6 辅助学习功能实现
> 4.6.1 学习行为记录
> 4.6.2 学习画像统计
> 4.6.3 资料推荐规则

然后把“4.8 本章小结”改为“4.7 本章小结”。

### 4.2 课程知识库构建模块

这节是论文亮点之一，应加强。

你现在已经写了资料上传、状态流转和原子单元解析。资料状态从pending、processing、indexed、failed的设计是合理的。

建议补充三项内容。

第一，增加“资料处理状态机”表：

| 状态       | 含义             | 触发条件          | 前端显示     |
| ---------- | ---------------- | ----------------- | ------------ |
| pending    | 已上传，等待解析 | 上传完成          | 待解析       |
| processing | 后台解析中       | worker开始处理    | 解析中       |
| indexed    | 索引完成         | 向量/图谱写入成功 | 已索引       |
| failed     | 解析失败         | 文件异常/API异常  | 失败，可重试 |

第二，修正原子单元公式。建议写成：

[
u_i=(id_i,m_i,c_i,d_i,p_i,e_i)
]

其中：

- (id_i)：原子单元编号；
- (m_i)：模态类型，包括text、table、formula、image；
- (c_i)：原始内容或结构化内容；
- (d_i)：用于检索的文本化描述；
- (p_i)：来源位置信息，包括文件ID、页码、段落或区域坐标；
- (e_i)：扩展元数据，包括标题、上下文、解析置信度等。

再写：

[
t_i = g(c_i,m_i,p_i)
]

其中 (g(\cdot)) 表示文本化转换函数，用于将不同模态内容转换为可嵌入和可检索的文本片段。

第三，补一张“不同资料类型处理方式”表：

| 文件类型     | 解析方式               | 进入索引的内容                   | 引用来源    |
| ------------ | ---------------------- | -------------------------------- | ----------- |
| PDF/PPT/Word | MinerU/LibreOffice解析 | 段落、标题、表格、公式、图片描述 | 文件名+页码 |
| Markdown/TXT | 直接读取文本           | 文本块、表格文本                 | 文件名+段落 |
| 图片         | OCR+视觉描述           | OCR文本、图像描述                | 图片文件    |
| 音视频       | 转写/关键帧预处理      | 转写文本、关键帧描述             | 时间戳/文件 |

注意：如果音视频没有实际进入核心RAG检索，表里要写“预处理支持，核心测试未覆盖”，不要写成完整支持。

### 4.3 知识图谱构建模块

这节现在有文本图谱、跨模态图谱、融合和可视化，结构可以保留。你已经说明跨模态图谱不是把图片直接作为节点，而是将解析描述、结构化内容和来源元数据纳入图谱构建，这个说法比较合理。

建议补充三项内容。

第一，增加“图谱实体与关系类型定义”。否则图谱看起来像框架自动生成，缺少你的设计。

第二，说明“底层索引图谱”和“业务展示图谱”的区别。你已经写到了这两个概念，建议用表格固定下来：

| 类型         | 作用                  | 存储位置             | 使用场景 |
| ------------ | --------------------- | -------------------- | -------- |
| 底层索引图谱 | 支持RAG检索和实体扩展 | LightRAG/Neo4j       | 问答检索 |
| 业务展示图谱 | 支持前端可视化        | 业务数据库/Neo4j投影 | 图谱页面 |

第三，补充“图谱质量控制”。例如：

- 过滤过短实体；
- 合并同名实体；
- 去除技术性节点；
- 保留课程知识点、公式、算法、章节；
- 关系重复出现时提升权重。

你现在已经写了以实体名称合并、以源实体/目标实体/关系类型合并关系，这很好，但可以再整理成“融合规则表”。

### 4.4 检索增强生成问答模块

这是整篇论文最关键的小节，必须写得最清楚。当前你写了用户问题预处理、关键词提取、双路检索、证据重排和回答生成，但建议改得更结构化。

建议增加一张完整流程表：

| 阶段       | 输入                   | 处理                    | 输出       |
| ---------- | ---------------------- | ----------------------- | ---------- |
| 问题接收   | 用户问题、班级ID、模式 | 权限校验、保存消息      | 会话记录   |
| 问题预处理 | 原始问题、历史上下文   | 判断追问、查询改写      | 独立问题   |
| 关键词提取 | 独立问题               | 提取高层/低层关键词     | 查询关键词 |
| 证据召回   | 查询关键词             | local/global/hybrid检索 | 候选证据   |
| 重排序     | 候选证据               | reranker排序、去重      | 核心证据   |
| 答案生成   | 问题+证据              | LLM生成                 | 回答正文   |
| 引用保存   | 回答+证据              | 来源结构化              | 引用记录   |
| 反馈处理   | 点赞/点踩              | 创建审核项              | 审核队列   |

再加一个伪代码，形式如下：

```text
Algorithm 1 基于课程知识库的问答流程
Input: 用户问题 q，班级编号 c，回答模式 mode，会话历史 h
Output: 回答 a，引用集合 R

1. 校验用户是否属于班级 c
2. 保存用户问题 q
3. 判断 mode 与问题类型
4. 若问题不需要课程证据，则调用LLM生成普通回答
5. 否则根据 h 将 q 改写为独立问题 q'
6. 提取高层关键词 K_high 和低层关键词 K_low
7. 使用 local 路径召回实体和文本块
8. 使用 global 路径召回关系和关联文本块
9. 合并候选证据，去重并补充来源信息
10. 使用重排序模型选择Top-k证据
11. 将 q'、历史上下文和Top-k证据组装为提示词
12. 调用LLM生成回答 a
13. 保存回答、引用来源和检索追踪信息
14. 返回 a 和 R
```

本科论文很适合放伪代码。它能显著提升第四章的“技术含量”。

另外，你要解释三种回答模式：

| 模式     | 是否检索课程资料 | 适用场景                 | 风险                 |
| -------- | ---------------- | ------------------------ | -------------------- |
| 快速模式 | 否               | 常识性、非课程问题       | 可能无课程依据       |
| 检索模式 | 是               | 课程概念、作业、课件内容 | 依赖资料索引质量     |
| 自动模式 | 系统判断         | 普通使用                 | 分类错误可能影响答案 |

第五章已经测试了自动、快速、检索三种模式，所以第四章必须先定义清楚。

### 4.5 教师审核与知识回流模块

这一节目前基本合理，但需要把“回流后如何被后续检索利用”写具体。否则审核只是流程，不是知识库优化。

建议补充：

1. 教师修正答案保存到哪张表；
2. 回流内容如何转成补充知识；
3. 补充知识是否进入向量库；
4. 是否与原问题形成问答对；
5. 后续相似问题如何优先命中教师修正内容。

可以这样写：

> 教师选择回流后，系统将“学生原问题—教师修正答案—班级编号—审核来源—创建时间”组织为补充问答知识单元。该知识单元进入课程知识库的增量索引流程，生成向量表示并保存来源标识。后续用户提出相似问题时，系统在检索阶段可召回该补充知识，并在引用来源中标识为“教师修正答案”。

这样逻辑才闭合。

### 4.6/4.7 章节编号修复

当前目录显示第四章是4.1、4.2、4.3、4.4、4.5，然后直接4.8。必须改。

建议改成：

- 4.1 班级空间基础功能实现
- 4.2 课程知识库构建模块
- 4.3 知识图谱构建模块
- 4.4 检索增强生成问答模块
- 4.5 教师审核与知识回流模块
- 4.6 辅助学习功能实现
- 4.7 本章小结

如果不保留学习画像，则改成：

- 4.6 本章小结

------

# 第五章 系统测试与结果分析修改建议

## 当前主要问题

第五章是目前最需要加强的一章。它不是不能作为本科论文测试章，而是现在过于“截图展示”，缺少测试用例、测试数据、测试过程和结果分析。

你已经写了实验环境，包括Ubuntu、Docker、FastAPI、SQLite、Celery、Redis、React、RAG-Anything、MinerU、Qdrant、Neo4j、Qwen、bge-m3、reranker等，这部分比较完整。

功能测试表也覆盖了用户认证、课程管理、资料上传、索引状态、AI问答、回答模式、知识图谱、学生反馈、教师审核、任务发布、学习画像等功能。

但第五章现在有三个问题：

1. 测试结果几乎全是“成功”，缺少测试条件；
2. AI问答测试没有具体问题样例、引用来源样例和正确性判断；
3. 小结说分析了“索引质量、检索质量、性能稳定性”，但正文没有对应充分数据。

## 具体修改建议

### 5.1 实验环境与部署配置

这一节可以保留，但建议加一段“测试数据集说明”。

本科系统论文至少要说明你用什么资料测：

> 本文以“计算机网络”课程为测试对象，选取课程讲义PDF、PPT课件、Markdown复习资料和图片资料作为测试数据。测试资料共X份，其中PDF X份、PPT X份、Markdown X份、图片X张。系统对上述资料进行上传、解析、索引和问答测试。

你需要补真实数字。没有真实数字，测试可信度会下降。

建议增加表：

| 资料类型      | 数量 | 用途               |
| ------------- | ---- | ------------------ |
| PDF讲义       | X    | 文本解析、引用溯源 |
| PPT课件       | X    | 课程知识点解析     |
| Markdown资料  | X    | 文本分块           |
| 图片资料      | X    | OCR/视觉描述       |
| 表格/公式资料 | X    | 多模态解析         |

### 5.2 系统功能测试

功能测试表可以保留，但要扩展成更规范的测试用例表。不要只写“成功”。

建议每个模块至少有：

- 测试编号；
- 测试目标；
- 前置条件；
- 输入数据；
- 操作步骤；
- 预期结果；
- 实际结果；
- 是否通过。

例如AI问答测试：

| 编号    | 测试目标         | 输入问题                     | 回答模式 | 预期结果                       | 实际结果      |
| ------- | ---------------- | ---------------------------- | -------- | ------------------------------ | ------------- |
| T-AI-01 | 检索课程资料回答 | “TCP三次握手的过程是什么？”  | 检索模式 | 返回课程资料相关回答并显示引用 | 通过          |
| T-AI-02 | 快速模式回答     | “帮我总结一下复习计划”       | 快速模式 | 不检索资料，直接生成建议       | 通过          |
| T-AI-03 | 自动模式路由     | “课件中拥塞控制有哪些算法？” | 自动模式 | 自动进入知识库检索             | 通过          |
| T-AI-04 | 无资料命中       | “课件中没有的概念X是什么？”  | 检索模式 | 提示未找到充分依据             | 通过/部分通过 |

尤其要加“失败/异常用例”，否则测试看起来像展示页面。

### 5.2.1 班级基础功能

建议补充越权测试：

| 测试项                   | 预期       |
| ------------------------ | ---------- |
| 未登录访问班级资料       | 返回未认证 |
| 非班级成员访问资料       | 返回无权限 |
| 学生尝试上传课程资料     | 拒绝       |
| 学生尝试进入教师审核页面 | 拒绝       |

这能支撑第三章安全性需求。

### 5.2.2 知识库构建功能

除了截图，你要加“资料解析结果统计”。

建议表格：

| 文件                | 类型 | 大小 | 解析状态 | 文本块数 | 实体数 | 关系数 | 耗时 |
| ------------------- | ---- | ---- | -------- | -------- | ------ | ------ | ---- |
| 计算机网络第1章.pdf | PDF  | X MB | indexed  | X        | X      | X      | X s  |
| TCP拥塞控制.pptx    | PPT  | X MB | indexed  | X        | X      | X      | X s  |
| 复习资料.md         | MD   | X KB | indexed  | X        | X      | X      | X s  |

你不一定要做严谨性能实验，但必须有基本统计。

### 5.2.3 知识图谱查看功能

你现在写了以“计算机网络”课程为测试对象，展示图谱节点和关系，这很好。

建议补充：

1. 图谱节点总数；
2. 关系总数；
3. 主要实体类型；
4. 搜索一个节点的结果；
5. 点击节点后显示哪些来源。

示例表：

| 指标           | 数值                                 |
| -------------- | ------------------------------------ |
| 实体节点数     | X                                    |
| 关系边数       | X                                    |
| 可展示节点数   | X                                    |
| 支持搜索关键词 | TCP、拥塞控制、路由协议              |
| 节点详情字段   | 名称、类型、描述、来源资料、关联节点 |

再加一段分析：

> 测试结果表明，知识图谱页面能够展示课程知识点之间的关联关系，并支持节点搜索和一跳关系高亮。但图谱质量依赖文档解析和实体抽取结果，个别实体可能存在粒度不一致或命名重复的问题。

这样更真实。

### 5.2.4 AI助教问答功能

这是第五章最重要的测试，必须加强。

你现在只说系统能返回Markdown回答并展示引用来源，不够。

建议新增一个“小规模问答测试”。本科不需要很大，但至少要有20个问题。

建议问题类型如下：

| 类型               | 数量 | 示例                         |
| ------------------ | ---- | ---------------------------- |
| 概念解释           | 5    | “什么是滑动窗口？”           |
| 过程描述           | 5    | “TCP三次握手过程是什么？”    |
| 对比分析           | 5    | “TCP和UDP有什么区别？”       |
| 公式/表格/图片相关 | 3    | “图中协议栈各层作用是什么？” |
| 资料外问题         | 2    | “课程资料中是否提到某概念？” |

评价方式可以简单，但要有：

| 指标             | 说明                     |
| ---------------- | ------------------------ |
| 是否返回回答     | 系统是否正常生成         |
| 是否展示引用     | 回答下方是否有来源       |
| 引用是否相关     | 引用资料是否与问题相关   |
| 回答是否基本正确 | 人工判断是否符合课程资料 |
| 是否触发教师审核 | 对低质量回答是否能反馈   |

结果表可以写：

| 指标         | 结果 |
| ------------ | ---- |
| 测试问题数   | 20   |
| 成功返回回答 | 20   |
| 展示引用来源 | 18   |
| 引用基本相关 | 16   |
| 回答基本正确 | 17   |
| 需要教师审核 | 3    |

你要填真实数据。即使数据不完美也没关系，比“全部成功”可信。

注意：不要写“准确率提升”。你可以写：

> 在本组测试中，系统能够对多数课程相关问题返回带引用来源的回答，但对资料中未明确出现或依赖复杂推理的问题，仍可能出现引用不足或回答泛化的问题。

这很稳。

### 5.2.5 教师审核与知识回流功能

你现在已经展示学生点踩、教师审核、答案修正和知识回流流程。

建议增加“回流前后测试”：

| 步骤 | 操作                        | 结果                 |
| ---- | --------------------------- | -------------------- |
| 1    | 学生提出问题Q，AI回答不准确 | 学生点踩             |
| 2    | 教师提交标准答案A           | 审核状态变为已解决   |
| 3    | 教师选择回流知识库          | 生成补充知识记录     |
| 4    | 另一学生提出相似问题Q'      | 系统召回教师修正答案 |
| 5    | 回答引用显示教师修正来源    | 回流生效             |

这个测试能证明“知识回流”不是只保存记录，而是真正影响后续问答。

### 新增5.3 性能与响应时间测试

你目录现在从5.2直接到5.6，这是格式问题。建议补充5.3、5.4、5.5。

建议：

> 5.3 系统性能测试
> 5.4 问答效果分析
> 5.5 异常情况与局限分析
> 5.6 本章小结

5.3可以很简单，记录响应时间：

| 测试项      | 平均耗时 | 说明        |
| ----------- | -------- | ----------- |
| 登录        | X ms     | 普通接口    |
| 资料上传    | X s      | 不含解析    |
| PDF解析入库 | X s      | 后台任务    |
| 普通问答    | X s      | 快速模式    |
| 检索问答    | X s      | 检索+生成   |
| 图谱加载    | X s      | 节点/边返回 |

### 新增5.4 问答效果分析

不要求硕士级实验，但要做“样例分析”。

建议选3个代表性样例：

1. 检索命中充分，回答较好；
2. 检索命中但答案组织一般；
3. 检索不足，需要教师审核。

每个样例写：

- 用户问题；
- 检索到的引用；
- 生成结果摘要；
- 人工评价；
- 问题分析。

### 新增5.5 异常情况与局限分析

本科论文写这个反而加分。建议列：

| 异常情况     | 系统处理方式             | 局限                   |
| ------------ | ------------------------ | ---------------------- |
| 资料解析失败 | 状态变为failed，记录错误 | 需要教师重新上传或重试 |
| 检索不到证据 | 提示依据不足             | 仍可能生成泛化回答     |
| API调用超时  | 返回错误提示             | 依赖外部模型服务       |
| 图谱实体重复 | 合并同名实体             | 语义同义词仍可能重复   |
| 教师未审核   | 保持待审核状态           | 回流无法立即生效       |

### 5.6 本章小结

现在小结说“索引质量、检索质量、性能稳定性”等方面进行了分析，但正文主要是功能展示。

如果你不补5.3—5.5，小结必须改弱：

> 本章对AI助教平台的部署环境和主要功能进行了测试，测试结果表明系统能够完成用户认证、课程管理、资料上传、知识库索引、知识图谱展示、AI问答、引用来源展示、学生反馈和教师审核回流等功能。测试也表明，系统在多模态资料解析、外部模型调用延迟和复杂问题回答方面仍存在一定局限。

如果你补了5.3—5.5，就可以保留“性能稳定性”和“检索质量分析”。

------

# 第六章 总结与展望修改建议

## 当前主要问题

第六章比前面更稳，因为你在不足分析中已经承认音视频没有进入核心检索增强流程、依赖外部工具/API、响应延迟、图谱质量受解析和模型影响、推荐规则简单等问题。这个态度是正确的。

但第六章仍有两个问题：

1. 总结中“结合多模态解析、向量检索、双图构建、重排序、引用溯源、教师审核和学习分析”容易显得功能过满；
2. 展望中如果提出“完善RAG效果评估体系”，会反向提醒评审：你当前评估体系不足。所以要写得更自然。

## 具体修改建议

### 6.1 全文总结

建议按“完成了什么”写，不要写“证明了什么”。

建议结构：

> 本文围绕高校课程答疑场景，设计并实现了一个基于大语言模型和检索增强生成的AI助教平台。系统主要完成了以下工作：
> （1）完成学生、教师和管理员三类角色的需求分析，设计了前后端分离的系统架构；
> （2）实现课程资料上传、解析任务管理和知识库构建流程；
> （3）基于RAG-Anything和LightRAG实现课程资料检索、证据组织和带引用回答生成；
> （4）构建课程知识图谱展示功能，支持知识点搜索、节点查看和关系浏览；
> （5）实现学生反馈、教师审核和修正答案回流机制；
> （6）通过功能测试验证系统主要模块能够正常运行。

这个总结安全、清楚、符合本科。

### 6.2 不足分析

你现在的不足分析可以保留，但建议拆成更清晰的五点：

1. **多模态支持仍不充分**：文档和图片处理较完整，音视频只做预处理，未作为核心检索证据充分测试；
2. **问答效果缺少大规模评估**：当前以功能测试和小规模样例为主，缺少系统性问答数据集；
3. **依赖外部模型和解析工具**：API延迟、解析失败和服务稳定性会影响使用体验；
4. **知识图谱质量受抽取结果影响**：存在实体粒度不一致、关系重复或缺失问题；
5. **个性化推荐较简单**：主要基于规则统计，尚未结合更复杂的学习行为建模。

注意不要写得太严重，不要说“核心功能不完善”。要写“仍存在一定局限”。

### 6.3 未来工作展望

建议与不足一一对应：

1. 完善问答效果评估，构建课程问答测试集；
2. 加强音视频资料处理，使转写文本和关键帧更稳定地进入检索；
3. 优化知识图谱实体合并和关系过滤；
4. 增加本地模型部署或缓存机制，降低外部API依赖；
5. 改进学习画像和推荐策略。

不要写太宏大的“构建智慧教育生态”之类内容。

------

# 格式与细节必须立即修改

这些是会直接影响观感的硬伤。

## 1. 修正章节编号

当前目录中第四章从4.5跳到4.8，第五章从5.2跳到5.6。

建议改为：

- 4.6 本章小结，或增加4.6辅助学习功能、4.7本章小结；
- 5.3 性能测试；
- 5.4 问答效果分析；
- 5.5 异常情况与局限分析；
- 5.6 本章小结。

## 2. 修正错别字和术语

必须查全文：

- “提示工程与与任务型问答机制” → “提示工程与任务型问答机制”；
- “tudent_profiles” → “student_profiles”；
- “涉及提供参考” → “设计提供参考”；
- “延申” → “延伸”；
- “图谱”有时被误写成“图表/图谱”，统一术语。

## 3. 修正公式残缺

原子单元公式现在变量缺失，必须重写。

建议使用我上面给出的：

[
u_i=(id_i,m_i,c_i,d_i,p_i,e_i)
]

并逐个解释变量。

## 4. 图表编号统一

现在图编号有“图5. 10”这种格式，建议统一为：

- 图5-10 学生反馈提交测试结果
- 图5-11 教师审核与答案修正测试结果

全文统一“图X-Y”“表X-Y”。

## 5. 结论性词语降调

全文搜索并替换以下高风险词：

| 高风险表达           | 建议替换                       |
| -------------------- | ------------------------------ |
| 证明了系统有效性     | 验证了系统主要功能的可用性     |
| 保证准确性           | 提高回答依据的可追溯性         |
| 保证不丢失多模态数据 | 为多模态内容参与检索提供支持   |
| 显著提升             | 在测试场景中表现出较好的可用性 |
| 理论意义             | 技术实践意义                   |
| 创新性提出           | 设计并实现                     |

------

# 最优修改优先级

## 必须修改，否则本科盲审也可能被扣较多分

1. **摘要、意义、结论降调**：不要声称证明准确性、有效性和多模态保证。
2. **第五章补测试数据**：至少补测试资料统计、问答样例、引用相关性、响应时间。
3. **修正章节编号**：4.8、5.6编号跳跃必须解决。
4. **修正原子单元公式和错别字**：这是明显格式硬伤。
5. **统一“系统设计类论文”定位**：不要再按方法创新论文写。

## 建议修改，能明显提升质量

1. 第三章增加“核心功能/辅助功能”划分。
2. 第四章给RAG问答流程加伪代码。
3. 第五章增加“回流前后对比测试”。
4. 第二章增加“技术选型表”。
5. 第六章把不足和展望一一对应。

## 可选优化

1. 增加一张数据库ER图或核心表关系图。
2. 增加接口表，例如认证接口、资料接口、问答接口、审核接口。
3. 增加系统安全性说明，如JWT、角色权限、班级成员校验。
4. 增加用户体验测试，例如邀请3—5名同学试用并填写反馈，但这不是必须。

------

# 最后给你一个本科论文的安全版主线

你后续修改时，全文要围绕这条主线写：

> 课程资料分散、教师答疑重复、通用大模型缺少课程依据 → 设计AI助教平台 → 系统包括班级管理、课程知识库、知识图谱、RAG问答、教师审核回流 → 通过RAG-Anything和LightRAG实现资料解析、图结构检索和带引用回答 → 通过功能测试和样例测试验证系统主要功能可用 → 承认问答效果评估、多模态支持、API依赖和个性化推荐仍有不足。

按这个方向改，你的论文作为**本科系统设计类毕业论文**是可以成立的。关键是不要让评审觉得你在用“研究创新”的话术掩盖“系统实现”的本质。